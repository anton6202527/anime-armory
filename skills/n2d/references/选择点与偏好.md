# 制漫剧 n2d · 选择点与偏好约定（本线自包含）

> 本线（n2d）私有的偏好/选择点约定。原 `skills/n2d/references/选择点与偏好.md` 已按线拆分，本线自带这一份，
> 不依赖任何公共层。通用机制对所有线一致；下方「选择点目录」只列本线的选择点。

# 偏好约定（所有自定义 skill 共用 · 通用机制）

> 这是**共享代码侧**的通用约定，不含任何具体用户的选择。
> 用户的实际选择是**私有的**，落在用户自己的空间，绝不写进 skill 源码。
> 任何 skill 涉及「让用户选」的地方，都按本约定读写，不要把某个平台/后端写死成唯一路径。

## 三句话原则
1. **skill 通用**：不写死平台/后端/分辨率；这些是「选择点」，由用户私有文件决定。
2. **选择私有**：用户的选择存在用户自己的空间（见下），不外发、不进共享 skill。
3. **持续记录 · 最新为准**：选择点首次遇到 → 问一次 → 写入 `_设置.md` → 同项目之后**沉默沿用**；用户随时可改（说一声或直接改文件）。**用户在任何一句输入里显式点名某选择点的值（哪怕之前已设过、哪怕在流程中途、哪怕和当前任务无关）→ 立刻以新值覆盖 `_设置.md` 对应行 + 追加一条 `## 记录`，最新选择永远优先于旧值 / 全局默认 / 缺省。** 听到即落档，不要"等下次用到该选择点再写"。

## 候选项刷新与用户输入口

选择点目录里的可选值只是**候选快照**，不是永久真值，也不是对用户输入的封死枚举。

- **可刷新来源**：候选项可来自项目 references、已有生产数据/投放回灌、模型/平台官方文档、实时搜索、以及 AI 代理的专业知识库。模型、平台、价格、规格、法规、榜单、API 能力等高变化信息，在正式花钱/不可逆/合规相关步骤前必须按需要核验；刷新时写清核验日期和来源，不能联网或不能确认时明确标注“未核验”。
- **用户输入口**：每个选择点都保留 `自定义` / `manual` / 直接手输值的入口。`skills/n2d/_lib/settings.py` 的 schema 只用于安全校验和常见别名归一，不得把用户的合理新选择挡在 skill 外；确属实验值时用 `python3 skills/n2d-settings/scripts/settings_cli.py set <作品根> <选择点> <值> --force` 落档，并在 `## 记录` 说明原因。
- **菜单不等于执行逻辑**：菜单文案服务于人类决策；脚本执行时必须先把值归一成能力、参数、后端入口和回退/保真实现策略。不要在任一 stage 里写 `if 菜单文案 then 唯一路径` 的硬绑定。
- **机检与刷新工具**：易变候选清单都在 `skills/n2d/_lib/freshness.py:CANDIDATE_SOURCES` 登记并带 `采集日期：YYYY-MM-DD` 戳记；`python3 skills/n2d/_lib/freshness.py` 机检谁过期；`本线 `_lib/refresh.py`` skill 跑「搜索核验 → 改候选 → bump 日期 + 落 provenance」的刷新流程。新增易变清单 → 登记 + 加戳，否则机检看不到。

## 适配层原则

不同选择带来的差异由适配层消化，产线正文只消费统一契约。

- **模型指代铁律（设计宪法 C5）**：凡指名「由什么**生成**这张图/这段视频」，指代必须落到**具体模型名+版本**（GPT Image 2 / Seedream 4.5 / Seedance 2.0 / Veo 3.1），**不写 agent/渠道/厂商/壳**（Codex / 即梦 / 渠道商 / 某后端）。模型(`生图模型`/`生视频模型`)与渠道(`生图AI`即渠道/`生视频渠道`)**两个字段分列**；route/记账/per-run 证据以**模型为主键、渠道为副**。模型名仍带采集日期、可刷新（C2），但不准退化成渠道壳。
- **剧情/质量先行（设计宪法 C6）**：分镜要不要多人同框、近景给多大表情、镜头多长，由**剧情与质量**决定，**不为迁就后端短板而下调/回避创作决策**。后端只决定**实现路径与补偿**（换更强模型/分别出图+分区合成/首尾双帧/拆段接力/人审），不决定「镜头能不能存在」。质量硬闸拦的是「没做对」，不是「难」。
- **标准先行**：skill 标准只写“必须达到什么”，不写“某后端怎么做”。选择点不得降低标准；只能改变实现路径、成本、速度和需要的弥补措施。
- **归一化**：把用户选择映射为 canonical key、能力标签（如多参考/主体库/原生音画/最大时长/分辨率上限）、实际 CLI/API/网页入口、参数包、fallback/degrade plan、合规 gate 和输出 schema。
- **专门适配**：用户选定某模型/平台/风格/规格后，下游 prompt、分镜约束、镜头时长、首尾帧/参考图策略、字幕/交付规格等都应按适配结果生成，不只是在 `_设置.md` 里记一个名字。
- **生图后端适配**：生图统一标准在 `skills/n2d/_lib/image_backend_standards.py`；生成轴的真值是 `生图模型`（默认 GPT Image 2·见 C5），访问入口是 `生图AI`(渠道)，两者统一走 `skills/n2d/_lib/image_backend_adapter.py`（adapter 每条登记 `model`+`channel` 两字段，记账/证据以模型为主键）。每次正式付费出图前先跑 `python3 skills/n2d/_lib/image_backend_adapter.py scan --json`：若扫描出多个可自动落 PNG 的官方/已登录后端，必须让用户选一个并写回 `_设置.md`；若一个都没有，停止并提示“当前无可用生图后端，请准备好可以生图的生图后端”；`needs_confirmation_backends` 只列为待人工确认，不当作可用。选定后再查官方文档/实时资料与本机 CLI/API help，把生成、编辑、多参考预算、主体库/Character ID、掩码、输出 schema、额度/价格等能力写入 per-run 证据：`python3 skills/n2d/_lib/image_backend_adapter.py record-refresh <作品根> --backend "<生图AI>" --source "<官方文档或CLI/API证据>" --source-url "<链接或留空>" --evidence-kind official_docs --exact-model-id "<本次实际模型id；不可见则写空并在 note 说明>" --note "<本次能力结论>"`。`image_preflight` 只认当天结构化能力证据；缺 `capability_assertions` 或每项能力缺 source/source-url/evidence-kind/observed_text 就停，不用旧菜单猜 API。后端原生能力不足时，适配层自动输出标准补偿：参考组/脸嵌入、主体槽位、分层合成、文字 overlay、整图重抽、full QC、人审队列等。
- **生视频后端适配**：视频静态能力档在 `skills/n2d/_lib/n2d_platform_profiles.py`，执行适配层在 `skills/n2d/_lib/video_backend_adapter.py`。出视频前由 `n2d-model-router` 写 `video_model_routes.json`，再把 route 中实际会用的 primary/fallback 逐个查官方文档、本机 CLI/API help 或最小 smoke test，记录 per-run 证据：`python3 skills/n2d/_lib/video_backend_adapter.py record-refresh <作品根> --backend "<后端>" --channel "<生视频渠道>" --source "<官方文档或CLI/API证据>" --source-url "<链接或留空>" --evidence-kind official_docs --note "<本次能力结论>"`。`video` gate 只认当天结构化证据；缺证据、缺 `capability_assertions` 或每项能力缺 source/source-url/evidence-kind/observed_text 就停，不用旧模型矩阵猜单 Clip 上限、首尾/多帧消费、原生音画/口型、身份绑定或输出 schema。中段锚帧执行也读 `anchor_consumption_plan`：`native_multiframe` 才能一次消费首/中/尾，`split_relay` 要拆段，`reference_only_qc` 不得假装时间轴锚。
- **透明失败**：适配层缺能力、缺凭证、缺官方入口或候选项已过期时，停下列明缺口和可选替代；不要静默改用另一个后端，也不要把旧候选清单当作当前事实。
- **解耦边界**：候选项目录、用户私有选择、执行适配、质量/合规闸门分开维护。新增模型/平台优先补适配表或 capability profile；只有真正影响用户决策的项目才进入本文件的选择点目录。

## AI 代理交互约定 (Interactive Flow)

**原则：把枯燥的脚本命令藏在背后，让用户只做决策。**

当一个工作流包含**「机器负责结构计算（如拆分 Clip），紧接着需要大模型负责语义理解（如写 Prompt）」**的组合操作时，**绝对不要**把两段割裂的 CLI 脚本硬塞给用户让他们自己去敲。

- **正确的做法**：在第一个脚本跑完后，建立明确的**【AI 代理交互节点】**。
- **用户体验**：AI 代理应主动弹出高亮提示，或者用自然语言询问：“已完成基础切分。是否需要我启动 XXX 引擎为你自动补全提示词？”
- **后台执行**：用户回答“是”之后，AI 代理在后台默默**跑脚本 → 读取提示词 → 调用自身的 LLM 生成 JSON 结果 → 注入项目**，一气呵成。使用户感受到的是一个连贯、智能的“创作助理”，而不是一本枯燥的代码说明书。

## 两层私有存储

| 层 | 路径 | 谁写 | 作用 |
|---|---|---|---|
| **作品级** | `<作品根>/_设置.md` | skill 自动读写，用户可手改 | 本作品/曲/小说的实际选择（**权威**，覆盖全局） |
| **全局默认** | 私有全局默认 `创作偏好-默认.md`（仓库根或 `.agents/` / `.codex/` 等用户空间） | 用户维护 | 跨项目的个人默认（如「生图后端 / 视频后端 / 720p / CosyVoice / Suno」），开新项目时**预填** `_设置.md` |

- `<作品根>`：`创作区/制漫剧/<剧名>/`，与 `_进度.md` 同级。
- 全局默认使用工具中立路径：优先读仓库根 `创作偏好-默认.md`，再读 `.agents/创作偏好-默认.md`、`.codex/创作偏好-默认.md`；旧项目的 `.claude/创作偏好-默认.md` 仅作兼容读取，不作为新的共享入口。skill 文本引用「全局默认偏好文件」即指这些私有候选文件。

## 解析顺序（每次要用某选择点时）
0. **（最高优先）用户本轮输入显式指定了该选择点** → 立刻按新值覆盖 `_设置.md` + 记一行，**用新值**，跳过下面的读取。这条对**所有**选择点恒成立，且不限于"正要用到"时——只要用户一句话点名某选择，当场落档（见三句话原则 #3）。
1. 读 `<作品根>/_设置.md` 里该选择点 → 有就**直接用，不问**。
2. 没有 → 读全局默认 `创作偏好-默认.md` → 有就用其值**预填**，并**首次告知一句**（"默认 X，要改说一声"），随即写进 `_设置.md`。
3. 全局也没有 → **问用户一次**，写进 `_设置.md`。
4. 之后同项目同选择点：**沉默沿用**，不再问。
5. 无作品根的独立调用：能定位项目就读其 `_设置.md`，否则用全局默认 / 现问，不强行落文件。

> 例外：**合规/不可逆/花钱多**的点（真人嗓授权、版权来源、删除回流）即便记录过也要确认——安全 > 省事。

## `_设置.md` 格式（人类可读 · 可手改）
```markdown
# _设置 · <作品名>   （你的私有选择；skill 自动读写，也可手改。改主意随时说）

## 选择
- <选择点>: <值>        # <可选值>
...

## 记录
- 2026-06-03 初始化（继承全局默认）
- 2026-06-03 生视频模型 Seedance 2.0→Kling 3.0（用户指定）
```


## 选择点目录（各家族暴露给用户的选择 + 缺省回退）

> 缺省值仅作**全局默认的初始建议**；真正生效的是 `_设置.md` 里的值。关键首跑选择点（如 `制作模式`、`基础视觉风格`）必须在项目 `_设置.md` 里带来源留痕：`# source=explicit_user` 或 `# source=cli` 才算已显式确认；`source=global_default` 或旧项目无来源只作为预选，不能跳过首次确认。


### 制漫剧 n2d 系列
- `制作模式`: 混合自动路由 \| 配音先行 \| 原生音画 \| 先出视频后配音　（默认 **混合自动路由**：先做项目级声音选角和无 WAV 文本估时，再逐镜选择 `performance_audio_first`、`base_video_then_post_lipsync`、`rough_timing_final_dub_later/post_dub`、`picture_first` 或 `native_av`。对白近景/口型可见镜头需要表演轨或独立后期口型；旁白、口外音、动作、空镜和蒙太奇不必为推进流程制造占位配音。音色定妆通过后才批量生成 final 音轨。其余三项是显式整项目兼容模式。首跑给四模式菜单并落 `_设置.md`；用户只说“后配音”仍默认混合，只有明确要求整集画面先行才选 `先出视频后配音`。）
- `合成阶段`: 跳过 \| 启用　（默认 **跳过**：`视频` 列完成只表示 `clip_delivery_complete`，不自动进入 `n2d-compose` / `n2d-review`。用户需要母带、BGM、烧字幕、屏幕文案 overlay、系统面板数值层、交付矩阵、release manifest 或发布证据包时，设为 `启用`；直接调用 `n2d-compose` 也视为用户选择本集进入合成尾段。旧项目若 `成片/验收` 已开始，路由会保留该尾段，不因默认值跳过已做工作；母版交付需继续过 release/readiness、production locks、creative governance 和人工签收。）
- `合成缓存保留`: 手动清理 \| 成片后清理 \| 保留7天　（默认 **手动清理**，不在首跑菜单强问。`_work` 与 `_clipcache` 都是可重建缓存，持久 timeline/OTIO 已独立落 `生产数据/timelines/`；`成片后清理` 只在成片存在且 cache doctor 无阻断时自动删两类缓存，`保留7天` 由 cache policy 在超过 7 天后清理。任何档都不删除正式 Clip、母版、签收、合同或 QC 证据。）
- `项目规模`: 短 demo \| 单集 \| 多集长线 \| 长篇量产 \| 自定义　（**首跑必问**，影响一致性投入而不改变剧情。`短 demo/单集` 可从 GPT Image 2 经 Codex/OpenAI 的参考派生起步；`多集长线/长篇量产` 必须在首跑就提醒主体库/Face Lock/face_embedding/LoRA 的一致性税，核心/常驻角色默认推荐可注册主体的官方后端或等效身份锁。此选择点只决定默认后端建议和 gate 提前预警，不允许静默替用户换后端；正式付费前仍由 `image_preflight` 和 per-run evidence 决定。）
- `题材`: 系统流 \| 穿越 \| 修仙 \| 都市 \| 宫斗 \| 赘婿 \| 战神 \| 自定义　（**弱选择点·检测预填可覆盖**——非铁律。n2d-script 在拆集/分镜阶段跑 `motif_detector.py`，按 `n2d_const.GENRE_KEYWORDS` 自动识别题材并**预填建议值**，不静默生效：识别到后给"我判定为 X 题材、发现 N 处母题桥段（系统面板出现/升级…），要启用母题增强吗"的菜单一次，用户确认/改写后落 `_设置.md`，同项目沉默沿用。题材本身不烧钱、不必每次重确认；但它驱动的母题增强若注入出图/出视频，那一步仍走 `生成粒度` 每次确认。题材决定哪些复现桥段套用母题模板，见 `n2d-script/references/题材母题框架.md`）
- `母题增强`: 开启 \| 关闭 \| 仅建议　（默认 **仅建议**：检测到复现母题桥段（系统面板/升级/签到/抽奖/爆装备）时只在 `生产数据/motif_plan_第N集.md` 给增强建议（场景/道具/台词/成长档位/overlay 文字层），**人确认后**才把 `motif_id`/`template:system_panel` 注回 `storyboard.json` 并写 `出图/共享/motif_registry.json`；`开启`=检测到即注入建议态（仍不直接花钱出图）；`关闭`=不做母题增强。系统面板是首个落地母题样板，混合渲染：AI 出锁色锁形的发光光幕底框（`VFX_系统面板`，禁烤文字），等级/属性数值由 n2d-compose 期 Pillow overlay 叠清晰文字层。见 `题材母题框架.md`）
- `基础视觉风格`: 冷灰写实3D国风漫剧 \| 真实3D人物质感 + 电影叙事镜头感 \| 写实电影感 \| 国漫写实角色审美 + 电影级布光与镜头语言 \| 国漫写实 \| 二次元赛璐璐 \| 水墨国风 \| 厚涂幻想 \| 赛博霓虹 \| Q版轻喜 \| 韩漫精致清透 \| 日漫剧场版光影 \| 3D卡通电影感 \| 动态漫画条漫风 \| 暗黑悬疑写实 \| 古风乙女清雅 \| 热血少年战斗番 \| 美漫硬线阴影 \| 低多边形玩具感 \| 纸片剪影 / 定格动画 \| 自定义　（默认 **冷灰写实3D国风漫剧**，但它只是预选，不是 skill 铁律。此点决定 `设定库/global_style.md`、`storyboard.json.style_contract`、出图/出视频总览「本集基础视觉风格契约」和逐镜 prompt 的画风词/负面词。**新作品首次写 global_style 前必须给菜单**：若 `_设置.md` 已有或用户本轮明说则直接用；若只有全局默认，把默认标为预选但仍展示菜单；菜单额外提供 `参考图片/视频自动识别` 临时入口——用户选它时先上传参考图或视频，AI 抽帧/看片判断后再归一成上方预设或 `自定义（...）` 六字段契约，**不要把临时入口原样写入 `_设置.md`**；选后写 `_设置.md`，同项目沉默沿用。旧项目里的 `cinematic_contract` /「本集真实电影感契约」可兼容，但下次改故事板/总览时迁到 `style_contract` /「本集基础视觉风格契约」。菜单与派生规则见 `n2d/references/visual_styles.md`）
- `拆集节奏`: 前长后短 \| 均衡 \| 快节奏 \| 长集 \| 自定义（**内部软默认/高级覆盖项，不列入首跑菜单；旧键 `单集时长` 兼容读取**）　（默认 前长后短：第1集更充分立世界观/主角欲望/系列总钩，后续更碎快追更。默认粗拆不锚字数，按章/场景/强钩候选出粗胚；边界必须优先保证剧情连贯、剧情丰满与 **冲突→爽点/反转→钩子** 闭环，不得为省时长删掉必要镜头/铺垫/动机/承接。真实时长只用于配音/原生音画后的节奏复核；`--target` 仅作显式高级报告参考，不参与默认切点决策。新作品不再向用户询问“每集多长”；只有用户主动要求更快/更长/自定义节奏时才写入 `_设置.md`）
- `主线剪枝`: 保守 \| 突出主线 \| 激进精简 \| 自定义（**内部软默认/高级覆盖项，不列入首跑菜单；缺省=advisory**）　（编剧级"主线提炼 + 支线剪枝"强度，作用在**拆集前**的全书改编策略层，落 `开发包/story_spine.json`（`story_spine.py scaffold/check`）。`保守`=保留全部支线，story_spine 仅建议不阻断；`突出主线`（**推荐给新作品**）=允许**有连通性证明地**压缩/折叠/裁剪偏离主线的旁枝，缺 confirmed 且过校验的 story_spine 时 `run.py` 在 `script_stage1` 写词前阻断；`激进精简`=更狠地砍旁枝、只保主线与强钩支线（校验规则同 突出主线）。**忠实底线（硬约束）**：只对**已确认**的 `设定库/source_comprehension.json` 因果链/伏笔账 操作，任何 cut/compress/fold 必须证明**无孤儿伏笔**（`connectivity.no_orphan_proof`）、伏笔/因果有承接（`connectivity.payoff_reroute`）、主线仍衔接，受保护功能（人物动机/选择后果/伏笔兑现/状态变化/系统规则）不得被裁，**禁止臆造** id。带 `do_not_drop_reason` 的伏笔若唯一承载线程被 cut 且无 reroute → block。**gate 语义**：`_设置.md` 未写此键或写 `保守` 时按 advisory（仅建议不阻断，兼容已拆集老项目）；写 `突出主线/激进精简` 时校验失败才阻断。被 spine 显式 `cut/compress/fold` 且给了 `cut_keywords/source_spans` 的支线，其源文内容在 `source_adaptation_audit` 里按"全书级有账剪枝"免账，不必再逐句登记 `adaptation_triage`。**cut 决策直驱拆集（P4·2026-07-24）**：cut 线程的 `source_spans` 写成严格机读的「第X章 / 第X-Y章」（或整数 `source_chapters: [4, 7]`，机器优先）时，`split_novel.py` 会在拆集阶段把这些章**整章剔除出集内容**——enforce 档（突出主线/激进精简）真剔，advisory（缺省/保守）只预览记账；与主线 spine 锚 / 非 cut 线程锚 / 有实质 issue 的 continuity_fix 锚冲突的章**宁可保留不剔**（记 conflicts），不可机读的 span fail-closed 不剔（`story_spine check` 提醒补锚）。只有 `decision=cut` 参与整章剔除——compress/fold 的内容部分保留，压缩发生在写词阶段。剔除逐章记账进 `split_plan.json.spine_pruning` + `_拆集机器索引.md`（章号/归属线程/源单元号/字数，绝不静默删），源单元轴保持完整、集 span 在剔除处标 `contains_spine_cut_gaps`；`--no-spine-cuts` 为显式逃生口（仍预览记账）。见 `n2d-script/references/拆集法.md` §主线剪枝）
- `片段经济`: 保守 \| 紧凑 \| 极简 \| 自定义（**内部软默认/高级覆盖项，不列入首跑菜单；缺省=保守=advisory**）　（治「简单叙事被拆成太多付费视频 clip」的**集级生成次数**强度档，作用在阶段2定稿后的 `clip_economy_planner.py`。预算是**复杂度感知**的：叙事广度（场景数+角色数）定基准档（simple 6 / standard 9 / complex 12 次每分钟），动作/奇观镜按 0.5/镜加成（打斗合理需要更多镜位，不把「一段打斗拆成几个 clip」误判成叙事复杂）。`保守`（默认/未设置）=仅 warn 透出、不阻断，兼容老项目；`紧凑`=当每分钟预计生成次数**超本集复杂度预算**且存在可采纳的相邻合并/单拍多镜(`take_policy=single_take_multishot`)省次数时，`run.py` 在出图/出视频前经 `clip_economy_gate` **阻断**，先把简单叙事的逐节拍独立 clip 合并再进贵工位；`激进精简`同义写法 `极简`=预算再收紧一档（×0.8）后同 `紧凑`。**只阻断「有可采纳省次数」的超预算**（可执行、不死锁）：已无合并候选的天然高密度镜不挡；**已生成视频的 Clip 按沉没成本处理**（2026-07-23）——不进省次数候选、不触发阻断（此刻合并=废弃已付费产物重生成），进行中的集不被追溯返工，确要返工走 `n2d-update` 最小重制。合并/补 take_policy 是签收产物变更，仍由编剧确认后改 storyboard，脚本不自动改写（宪法 B10）。与 `story_economy_audit`（单 Clip 时长预算）、`主线剪枝`（拆集前全书支线剪枝）正交叠加：前者压单镜时长，本档压集级次数，`主线剪枝` 从源头砍掉不该成戏的支线。见 `n2d-script/scripts/clip_economy_planner.py`）
- `视频证据强度`: 标准 \| 严格（**内部软默认/高级覆盖项，不列入首跑菜单；缺省=标准**）　（视频阶段五个后验证据维度——运动质量(MOT1)/相机空间轨迹(CAM1)/主体视频一致(S2V)/视频语义一致(VSEM)/高动态成片证据(SPECV)——的升级档，作用在 `n2d-review/gate_core.py` 的 strict-advisory 升级判定。`标准`（默认/未设置）=保持既有行为：这五维只在 `evidence_missing`、同维重复≥2、关键场景或交付边界(compose/review)时升 BLOCK；`严格`=**video 阶段**这五维的任何 WARN 直接升 BLOCK——身份/运动/语义漂移在整批 clip 付费前就挡下，而不是等 compose/review 才硬化，适合返工成本高的量产期或对一致性极敏感的项目。**净效果只加 BLOCK 不松任何既有闸**（B11 对偶方向安全；charter `_strict_advisory_should_block` 条目已留痕 2026-07-24 增补）；`consistency_advisory_signoff` 签收逃生口照旧（确认可接受的孤例可签收降回 WARN）。别名 `视频证据门`/`视频门槛`）
- `变现模式`: 免费 \| 付费 \| 海外（**内部软默认/高级覆盖项，不列入首跑菜单**）　（默认 **免费**：拆集结构的商业轴，决定**剧级追更骨架**的断点策略与付费/AD 卡点定位——`免费`(红果/番茄·完播率导向·全剧延续性·断点平均强·消灭连续弱钩集群) / `付费`(小程序剧·前十集卡点峰值·付费墙集须最强 cliffhanger) / `海外`(ReelShort/TikTok·更碎更狠·前3集立钩)。只在用户明确变现模式或目标平台时落 `_设置.md`；不改 P0→P6 单集边界铁律本身，只改整部断点强度的分布目标。驱动 `boundary_audit.py` 的「剧级追更骨架」段，见 `n2d-script/references/追更骨架.md`）
- `生视频模型`: Seedance 2.5 \| Seedance 2.0 \| Veo 3.1 \| Kling 3.0 \| Hailuo 02/2.3 \| Runway Gen-4 \| Luma Ray3.2 \| Pika 2.5 \| HunyuanVideo 1.5 \| Wan 2.2 \| LTX-2.3 \| manual（Seedance 2.5 已于 2026-07 上线：单段原生 30s / 50 全模态参考，对 single_take_multishot 合并上限直接利好；执行时长档以 per-run cli_snapshots 证据为准）　（**n2d-video 阶段选择点**：默认 **Seedance 2.0** 只是普通镜/兜底建议，不是首跑必选，也不是静默固定。Sora 只保留旧项目/manual 兼容，自动路由不再选它；如用户显式固定 Sora，需人工确认可用性。只有用户明确固定、`视频模型路由=固定生视频模型`、账号/交付只能单后端，或 n2d-video 的 router/probe 找不到可执行后端时，才在出视频前问并落档。旧项目的 `生视频AI` 兼容读取为模型/后端 fallback，但新项目不要再写这个合并字段。**能力证据分两层**：freshness 只说明候选快照日期没过期；`n2d_platform_profiles.capability_confidence` 说明证据质量，`conservative/manual_required/deprecated` 不能直接进入付费视频生成，必须当天 per-run 刷新证据或换后端。）
- `生视频渠道`: 即梦/Dreamina \| 豆包 \| 海螺AI \| 可灵/Kling \| Google Gemini API \| Runway API \| Luma Dream Machine \| Pika \| 本地/开源 \| manual　（**n2d-video 阶段选择点**：默认 **即梦/Dreamina** 只是调用入口偏好/兜底。渠道/产品和模型分开：例如即梦或海螺可能调用 Seedance 2.0；Google Gemini API 可能调用 Veo 3.1。自动路由时实际执行入口由 `n2d-model-router` + CLI/API 探测 + 适配层决定；只有固定模式、用户指定、账号/交付约束，或 n2d-video preflight 找不到可执行后端时才需要用户选择）
- `视频模型路由`: 自动按镜头路由 \| 固定生视频模型　（默认 **自动按镜头路由**：新作品首跑不问具体视频模型/渠道；生视频后端选择延后到 n2d-video 出视频前。`生视频模型` 只作为普通镜/兜底模型，不再固定每个 Clip。打斗/追逐/对话反打/真相揭示/公开对质/关系转折/飞行/空镜/法术爆发/亲密互动/多人站位由 `n2d-model-router` 按镜头类型、专项模板、身份注册层、原生音画策略、后端能力和时长上限选择 primary/fallback；`固定生视频模型` 仅用于账号/预算/交付约束明确要求单模型时，仍必须写每 Clip 路由字段和 fallback/degrade plan。旧值 `固定生视频AI` 兼容）
- `投放时效`: 实时 \| 隔夜批量　（**成本轴·与质量档正交·默认 实时**。部分视频生成 API/渠道提供 batch/flex/隔夜折扣；Sora batch 只作历史案例，当前 Sora 已 legacy/manual-only，不作为新项目成本轴依据。**非赶投放窗口的量产放量集**可设 `隔夜批量`，`n2d-model-router` 写 `urgency_tier=batch_24h`，dashboard 按 `provider#batch_24h` 拆账回看省了多少；首集打样/赶投放窗口保 `实时`（默认·绝不静默延迟）。**诚实边界**：实际 async batch endpoint 由视频后端能力决定，属执行适配层 follow-up（后端 batch 通道接入后才真省）——本选择点先产路由意图 + 成本拆账，通道一通即生效。详见 `n2d/references/模型矩阵.md` 视频 § + n2d-model-router）
- `后端Smoke硬闸`: 否 \| 是　（默认 **否**，但 production profile、`合规用途=paid_distribution`、`投放时效=隔夜批量/batch_24h` 会自动按“是”执行。它要求 image/video gate 在付费/放量前读取 `生产数据/backend_smoke/` 的最近可运行证明：`backend_smoke.py probe` 真探活，或 `record --status pass --output-asset <真实产物路径>` 手录但附可核验产物。只刷新官方文档不等于当前账号/渠道能跑；无产物的手录 pass 也不算严档证活性。内部 demo 如需关闭自动硬闸，可设环境变量 `N2D_REQUIRE_BACKEND_SMOKE=0` 明确留痕。）
- `视频备用后端`: 无 \| 即梦 \| 可灵 \| Seedance \| Veo \| Runway \| manual　（默认空=由路由器写理论 fallback。Sora 不再作为自动 fallback，只能旧项目/manual 人工确认。仅 `视频模型路由=固定生视频模型` 时可设 `无`，表示本机/本项目只允许默认生视频 CLI/渠道，路由表不得把不可执行后端写成可用 fallback；失败两次后应暂停询问，不自动切后端。旧值 `固定生视频AI` 兼容）
- `生图模型`: GPT Image 2 \| Seedream 5.0/4.5 \| Nano Banana Pro(=Gemini 3 Pro Image) \| Flux 2 Pro \| 可灵主体库模型 \| 自定义官方模型　（**生成轴=具体模型·设计宪法 C5 铁律**：默认 **GPT Image 2**。「由什么生成这张图」必须指认到**具体模型名**，不写 `Codex`/`某后端`/`渠道商` 这类壳——壳是会变的访问入口（`codex` 实际调的就是 GPT Image 2），把壳当生成者会让能力/路由/记账/审计漂离真实模型。GPT Image 2 是 2026 第一梯队的高保真多参考/编辑模型，支持强上下文一致性和 4K 级尺寸；当前官方文档未证实可注册服务端持久主体 ID/handle，不能按主体库使用。模型名是**带日期、可刷新的候选快照**（C2），但**指代必须到模型这一层**（C5）——两条叠加。访问入口由 `生图渠道` 决定（默认 GPT Image 2 经 Codex CLI）。生产闸门只阻断两类：① 同一作品/同一集**混用多个生图模型**（跨镜漂移真凶）；② 第三方逆向/未授权出图路径（含糊的 `同视频AI`/`某后端`、web 自动化仍禁）。所选模型不可用时停下报告，不偷偷换模型兜底。**长线起点推荐（G-I1·2026-06-24）**：GPT Image 2 默认只适合单集/短 demo/配角；**≥3 集 / 核心常驻角色 / 多人同框吃重 → 推荐默认起点切到可注册主体 ID 模型**（可灵主体库 / 即梦角色库 / Seedream Universal Reference，注册一次按 ID 跨镜跨集引用，先于 LoRA），hero/反复崩脸角色叠 **max-lock 栈**（主体 ID + PuLID + 低强度角色 LoRA~0.6 + ControlNet）。设计宪法 C4 不许私自写死后端，故 **不静默 auto-flip**：`image_preflight` 的「长线弱后端」BLOCK 现携带此推荐文案（`gate.IMAGE_IDENTITY_LOCK_RECOMMENDATION`），由 n2d-image 选择点带 ② 推荐向用户摆「换后端=整集重做定妆的一致性税」知情权衡。旧 `生图AI: Codex` 兼容读取为「渠道=Codex CLI、模型=GPT Image 2」，新项目改写为 `生图模型` + `生图渠道`。）
- `生图AI`（=`生图渠道`/访问入口）: Codex CLI \| OpenAI Images API \| Seedream 官方 API \| 可灵 \| Nano Banana/Gemini API \| Sora Cameo(旧项目/manual) \| 自定义官方渠道　（默认 **Codex CLI / GPT Image 2**。全项目生图优先 Codex/OpenAI；Dreamina/即梦官方 CLI 和其它非 Codex/OpenAI 后端只能作为用户签核例外，签核写 `<作品根>/合规/image_backend_override.json`。渠道与模型**分开**；正式生图前扫描本机/当前会话渠道时，不能因为检测到 `dreamina` 可用或视频阶段走即梦就自动改图片后端。**全集统一一个模型 + 一个渠道并能落 PNG**；Sora Cameo 仅旧项目/人工路径。所选渠道不可用时停下报告，不偷偷换。旧项目若写 `生图AI: 同视频AI`，先改成显式渠道名。）
- `同场景批量出图`: 开启 \| 关闭　（默认 **开启**·P2b 默认路径。开启时 n2d-image 建本集分镜图前先跑 `scene_batch.py`，把连续同场景[同 LOC]≥2 镜编成 `scene_coherent_batch`——共享场景定妆/光位锚/风格契约+同源种子，按一段连贯场景 pass 出再拆镜，用生成模型时空一致性根治同场景多镜漂移[DreamShot 思路]。只锁场景/光/风格，不锁人物身份[身份仍走两层定妆库+多人同框空间槽位]；单组<2 镜或跨场景切换自动断组回退逐镜独立出。关闭=全部逐镜独立出图）
- `原生多镜生成`: 自动 \| 开启 \| 关闭　（**默认 `自动`·能力门控**，治「`single_take_multishot` 决定了单拍多镜、却仍被逐镜拆成很多付费 clip」——2026-07 起改默认：`自动`/`开启` 且 primary 后端为 `multishot_native`[Seedance 2.0 / Kling 3.0 / Wan 等，由 `MULTISHOT_NATIVE_BACKENDS` 自动收录]时，`n2d-video/scripts/multishot_plan.py` 把 `n2d-model-router` 已标的 `multishot_groups`[连续接力镜组]**升为激活的一次 co-generate 计划**——一段连续接力镜喂模型一次出，跨镜身份/光照/转场由模型原生主体绑定内部锁、组内接缝由模型消除[`model_handled_seams`，seam/QC 层 `seam_is_model_handled()` 消费 → 组内接力镜不再要求逐镜尾帧焊接]。对标 2026 工业前沿「原生多镜 + 角色数字身份绑定」。**不合并 Clip 记账**：逐 Clip 仍是独立可追踪可重跑单元[模型矩阵立身原则]，组间接缝仍逐镜严格；组大小受 `max_clip_seconds` 上限[router 已切]。关闭=零行为变化，全部逐镜独立出。即梦渠道执行后端 dreamina 非多镜叙事核验渠道 → 不激活）
- `配音后端`: CosyVoice \| GPT-SoVITS \| MiniMax \| 火山 \| 自定义 \| say冒烟　（候选默认 CosyVoice，但真正执行以后端可用性 + `voice_casting.json` 的音色定妆锁为准；缺凭证/不匹配时阻断，不回退 say。前期估时不需要任何配音后端。）
- `出视频规格`: 预算充足 \| 预算一般 \| 预算不够　（默认 **预算充足**，但默认分辨率仍为 720p。三档各预设*分辨率·帧率·每Clip跑几条挑稳·平台质量档*：充足=720p 默认（可单独设 `视频分辨率=1080p`）/30fps/关键镜2-3条·普通镜2条/高质量档；一般=720p/24-30fps/**全部 1 条 + QC 门控重抽**/标准档；不够=720p/24fps/全1条/省积分档。**本档不取消 E1/R1/R2/R3 必需的编辑边界/高风险执行锚，也不强迫普通镜补中帧**：开跑前读 `anchor_plan` 与 v2 `duration_plan`，告知新增边界图、native multiframe/split relay/edit_cut 调用数及后端时长量化尾差。调模型/渠道前告知当前档，选后沉默沿用，可单项覆盖。）
- `视频分辨率`: 720p \| 1080p　（默认 720p；通常由 `出视频规格` 档预设，亦可单独覆盖）
- `英雄镜多版`: 关闭 \| 开启　（**默认 关闭**·**costly 选择点·开启=多花积分，花钱前每次确认**。开启后**英雄镜**——名场面/爽点兑现镜（打脸/逆袭/封神/重逢/告白/复仇/真相大白/决战…）、开场钩第1镜、高潮/真相揭示/对质/关系转折/动作高潮 shot_type——由 `n2d-model-router` 标 `hero_multi_version`：同时跑 `primary + secondary` 两个后端**各出一版**，pooled 进 `出视频/<集>/候选/<clip>/`，由 `video_qc` 选优。理由：IP 改编漫剧初始流量都冲这些高光而来，高价值镜不该赌单后端发挥；2026 共识是"赢在能一处调全部后端"。secondary 取该镜 `fallback_backends` 里第一个异于 primary 的后端；选不出异后端的镜跳过并记 `skipped_no_secondary`。`固定生视频模型` 路由模式下整条线只用单后端、不做跨后端多版。普通镜不受影响——只对英雄镜加版，成本有界。详见 n2d-video「英雄镜多版」节）
- `画幅`: 9:16 \| 16:9　（默认 9:16 竖屏短剧）
- `视频生成音频策略`: 无声视频流 \| 配音对齐口型 \| 低风险环境声 \| 原生音画 \| 自定义　（基础默认仍为 **无声视频流**，避免视频后端擅自生成人声；`制作模式=混合自动路由` 时，逐镜 route 可把已有获批表演轨的口型镜升级为 `配音对齐口型`，或把适合且能力已核验的单镜升级为 `native_av`。没有可信表演轨的口型镜只生成 neutral-mouth base plate，不因项目默认无声而免除后续 lipsync。）
- `对口型`: 对话近景 \| 关闭 \| 配音对齐 \| 后期pass　（项目级默认 **关闭** 表示“不把所有说话镜一刀切做口型”，不覆盖混合 route 的逐镜表演合同。混合模式判断某镜 `mouth_visible` 且有对白后：有获批表演轨则 `配音对齐`；没有则标 `post_lipsync_required`，先出 neutral-mouth base plate，最终必须完成后期 pass。用户明确关闭某个具体镜头的口型只能通过改分镜为侧脸/背身/画外音或写有理由的 route override，不能让正面说话镜无口型交付。）
- `BGM来源`: 占位 \| 文件 \| Suno　（默认 占位）
- `视频原生音轨`: 丢弃 \| 低音量混入环境声 \| 保留原片音轨　（**模式感知默认**：普通/基础视频 route 默认 **丢弃**；`native_av` route 保留，低风险环境声 route 可低音量混入。表演音轨仅作为口型条件时仍丢弃模型返回音轨，成片使用获批 final voice，避免双人声。）
- `后期拟音策略`: 自动 \| 强制叠加 \| 关闭　（默认 **自动**：n2d-compose 仍生成 `foley_plan.json`，但当本集 `video_model_routes.json`/适配层显示路由后端具备原生音画/原生音频能力，且有效 `视频原生音轨` 会保留或混入 clip 原片音轨时，自动把 compose 侧 V2A foley 抑制成静音轨，让模型原生 foley/SFX 站台，避免双层打击声和混音糊；非原生/静默后端照旧由 compose foley 提供 SFX。`强制叠加`=原生 foley 不满意时仍叠 compose foley，等价临时 `FORCE_COMPOSE_FOLEY=1`；`关闭`=只留拟音计划，不渲染 compose foley。gate 在发现原生 AV 音轨已保留却非强制地叠了 compose foley 时给 WARN。）
- `接缝兜底`: 硬切 \| 微溶解 \| 报警　（仅用于执行失败时的人工处置偏好，不替代 `seam_mode`。正常合成严格消费 P2/P3 分类：`dissolve` 才叠化，其余模式按剪辑点直切；`continuous_take_relay` 另验同一边界帧。检测到跳变不得自动抹一层溶解冒充修复，应按模式返工或报警。）
- `目标平台`: 抖音 \| 快手 \| B站 \| 小红书 \| 红果 \| YouTube \| TikTok \| ReelShort \| 跨平台 \| 未定　（默认 未定；影响合规包 `platform_review.targets`、画幅、字幕。正式投放前不得停留在 未定；若跨平台，逐平台写 gate profile，不用一个泛化“全平台通过”代替）
- `发行地区`: 中国大陆 \| 港澳台 \| 北美 \| 东南亚 \| 全球 \| 自定义　（默认 中国大陆；影响 `localization`、字幕语言、平台审核。海外/全球投放必须在合规包里声明本地化状态和目标语言）
- `合规用途`: internal_only \| publish_candidate \| paid_distribution　（默认 **internal_only**：自用 demo / 学习 / 内部预览，不公开发布；用户说“demo学习使用 / 不发布 / 内部预览”等中文说法归一为 `internal_only`。版权/改编权、角色/肖像授权、声音克隆授权是授权域，任何用途和阶段都照常 BLOCK。`internal_only` 只允许内部预览，`platform_review` / `localization` / `regulatory_filing` 发布域降 INFO 并标「转投放前需补」；`publish_candidate` 在 image/video 阶段把这些发布域列 INFO，不阻断付费出图/出视频，compose/review/release 前必须补齐否则 BLOCK；`paid_distribution` 从任意阶段开始都按发布严格口径 BLOCK。）
- `字幕语言`: 中文 \| 中英双语 \| 仅英文　（默认 中文·国内投放；海外投放 TikTok/ReelShort/YouTube/北美短剧 选 中英双语 或 仅英文。**翻译是投放选择，不写死在 skill**——选中英/仅英时 n2d-script 才产 `字幕_英文.srt`，否则只产 `字幕_中文.srt`；海外投放时合规包 `localization.target_languages` 必须覆盖实际字幕）
- `字幕字号`: 小 \| 中 \| 大 \| 自定义　（默认 **小**：竖屏漫剧字幕克制，避免遮脸/遮动作；compose 映射为 `ZH_SIZE=38/EN_SIZE=28`。`中`=46/32，`大`=50/34；自定义时可用环境变量 `ZH_SIZE` / `EN_SIZE` 覆盖。字幕文案应优先短句、少解释，把能靠画面表达的信息留给画面。）
- `AI显式角标`: 开启 \| 仅元数据 \| 关闭　（默认 **仅元数据**：内部预览不在画面角落烤「AI生成」，只写机器可读 AI 元数据；正式投放若平台/地区要求显式可见标识，改为 `开启` 后 compose 会叠角标并回写 manifest。`关闭`=可见角标和元数据都跳过，仅作内部临时文件，发布前必须补齐。AI 标识仍是非阻断发布待办。）
- `一致性增强`: 锚点+参考图 \| 指定参考图 \| image2image_reference_chain \| +LoRA　（默认 锚点+参考图，LoRA 不启用。`image2image_reference_chain` 表示本机 LoRA 过慢/不等待时，优先用同源参考图、多图参考链、脸锚和形体锚控制一致性。**LoRA 训练参数（底模 `--base-model`、训练后端 `--provider`、`--steps/--rank/--lr` 等）刻意不做静默预填选择点**，属 n2d-lora 的算子级 CLI 旋钮，由操作者每次显式传参并落进验证报告/数据集 manifest 审计留痕——它们影响一致性与许可风险，不应被"全局默认沉默沿用"。要不要启用 LoRA 这一开关才是选择点，怎么训是算子决策。）
- `一致性严格度`: demo \| standard \| production \| production_no_cost_image　（默认 demo：草稿/内部批量阶段，噪声型机检保留 WARN；standard：常规项目档；production：正式交付/投放前，gate 会把重复同维度、关键场景或交付边界的轴线/视线、音画同步、节奏密度等 advisory 升级为 BLOCK，并要求 `生产数据/consistency_advisory_signoff_第N集.json` 人工签收才能放行。`production_no_cost_image` 是“不考虑图片生成成本”的生产档：文本期仍 report-only，不把时长升硬门；出图前默认要求 series_bible、无成本参考包、关键镜多候选、performance_signature、多人同框 `regional_construct_required`/region masks 等侧车齐备。也可用 env `N2D_CONSISTENCY_PROFILE=production` 临时覆盖。）
- `脸一致性机检后端`: arcface \| styleid \| 自动按画风　（**n2d-review 作品质检/QC 选择点**，只决定崩脸机检的人脸相似度后端，不影响生图/出视频。默认 **arcface**(insightface buffalo_l)·写实/真人风画面用。**风格化/漫剧/插画脸建议 styleid**(CLIP-L+StyleID LoRA)，包括二次元/国漫/国风/漫剧/韩漫/日漫/厚涂/水墨/Q版/美漫/动态漫画/低多边形/纸片定格等——ArcFace 在风格化脸 TPR 系统性掉到 ~0.76(StyleID 论文 arXiv:2604.21689·2026-04)，按真人阈值判崩脸会误杀/放过。**诚实铁律：裸 CLIP 比 ArcFace 还差**(0.17–0.25)，故 styleid 必须加载真正的 StyleID 权重(`N2D_STYLEID_MODEL` 指向 CLIP-L+LoRA checkpoint)；缺权重时 `face_consistency.py` **自动回退 arcface 并标 `arcface_fallback`**(score 按低精度降权)，绝不静默用裸 CLIP。自标定 flag-band 地板随后端尺度自动移，但 `n2d-score` 的可投放线 KPI(`N2D_CONSISTENCY_RELEASE_LINE`)须随 encoder 重标定。解析顺序：CLI `--encoder` > `_设置.md` 本选择点 > env `N2D_FACE_EMBEDDER` > 默认 arcface。`自动按画风` 目前等价回退 arcface（按本集风格指纹自动选 encoder 是 follow-up，需接 style_consistency 分类）。**DreamSim 仲裁（G-I4·opt-in）**：脸 embedder 落 🟡 边界带时拿不准，置 env `N2D_DREAMSIM_ARBITER=1`（需装 `dreamsim`+torch）开启整图感知仲裁——与角色定妆 bank 比，DreamSim sim≥bank 自标定地板→判同人(ok)、<地板−margin→判异人(block)、带内维持 warn 交人判（2026 共识：CLIP-I/DINO 与人眼偏好相关性差，唯 DreamSim 稳定贴合人类判断）；默认关、缺库行为完全不变、绝不臆造分。详见 n2d-review「角色 DNA 用图判」）
- `重抽预算策略`: 预算充足 \| 预算一般　（默认 **预算充足**。全线图片重抽统一两档：`预算充足`=所有图片严格自检，出到满意为止；`预算一般`=只关键图片严格自检并出到满意为止，普通图无核心错位/身份漂移/硬性禁忌即可收。旧值 `预算不足` / `预算不够` 兼容归并为 `预算一般`。详见 n2d-image「重抽预算铁律」两档表）
- `生成粒度`: 逐个 \| 小批 \| 按场景分批 \| 整集　（默认 **逐个**：每出 1 张图/1 个 Clip 就停下让用户审、可即时重抽/改 prompt 再继续；`小批`=每批 N 个一停，N 默认 5 可改；`按场景分批`=同场景连续镜一组一停；`整集`=旧行为一次过、最后统一报告。**任何档都先过共享定妆硬闸门**。⚠️**例外——每次都问**：本点属花钱/token 敏感点，**每次进出图/出视频前都要把四档菜单念给用户选一次，不沉默沿用**；`_设置.md` 里的值只作「默认建议/上次选择」预选，不替代询问。出图/出视频通用，菜单原话见 n2d-image / n2d-video「生成粒度 + 优先序」段）
- `图片验收模式`: 逐张机器QC+实际目视 \| 按生成粒度验收　（仅图片。通常采用 `按生成粒度验收`；只有用户明确要求把“每张生成→机器QC→执行者实际像素目视→通过才下一张”设为跨项目长期规则时，才写全局 `逐张机器QC+实际目视`。该值把图片 `生成粒度` 固定为逐个、免后续重复粒度菜单，并要求正式 runner 一次只解析一张；不免本次付费授权、合规闸门或真人发布签收。视频仍单独按其生成粒度确认。）
- `生成优先序`: 关键镜优先 \| 分镜顺序 \| 先易后难　（默认 **关键镜优先**：🔑爽点/反转/钩子/封面候选先出，先定基调先锁主角脸；`分镜顺序`=Clip1→N 叙事序；`先易后难`=单人静态/空镜先、复杂打斗/多人镜后。共享定妆库永远排在最前，与本点无关）
- `脚本批次`: 逐集 \| 小批 \| 整批　（默认 **逐集**：n2d-script 量产剧本改编/分镜设计时每 1 集停下报**边界决策 + 物料清单**再继续；`小批`=每 N 集一停 N 默认 5 可改、附 5-10 集边界复核窗口；`整批`=指定范围一次过最后统一报告。文本不烧钱，**问一次后沉默沿用**，不像 `生成粒度` 每次弹菜单；只管"几集一停"不改三层拆集/P0→P6 边界铁律；第1集永远单独逐集。详见 n2d-script「脚本批次」段）
- `中段锚帧默认`: 开启 \| 关闭　（**默认 `关闭`=risk-only**。普通单拍只按首帧/尾帧需求出图；E1 明确镜位切点和 R1/R2/R3 高风险连续动作始终规划执行锚，不受该选择点影响。只有用户显式 `开启` 且执行后端单次请求原生支持 3+ 时间轴帧时，普通 D0 镜才补 `_mid`，并写 `policy.midframe_default_mode=explicit_opt_in`。首尾拆段接力不算原生三帧；`use=qc/reference` 只验收、不进 runner 时间轴。详见 n2d-script「关键帧/编辑切点自动规划」与 n2d-video「帧策略」）
- `非人物特写覆盖`: 启用 \| 仅提示 \| 关闭　（**新项目默认 `启用`**，治「不能时时刻刻都给人镜头」。当本集正文/分镜**确有**系统面板/HUD 或关键道具/物件（令牌/信物/丹药/证物…）这类信息态桥段，却没排一个**专属 insert 特写**（AI 出锁色锁形空光幕底/物件近景细节，文字走 overlay），就应给它专属镜、别只塞进人物反应镜。`启用`=系统面板缺 insert 走 **block**、道具缺 insert 走 **warn**；`仅提示`=全部 warn；`关闭`=静默。**题材+证据双门控**：只有确有该类词面才要求（纯对话集不硬塞空镜）；环境/空镜 cutaway 只作节奏建议、永不进配额。**老项目宽限**：`_设置.md` 无此键时读侧按 `仅提示` 处理、**绝不追溯 block**；要开硬闸显式写 `启用`。口径单一真值源 `skills/n2d/_lib/n2d_insert_coverage.py`，早闸在 `preventive_contracts`(出图 prompt 前)、出片复核在 `n2d-review` gate、分镜建议在 `n2d-script` shot_grammar_audit/director_blocking_pack。）
- `更新重制策略`: 最小 \| 严审刷新　（默认 **最小**：`n2d-update` 只回放到该集已到阶段、按变动文件算最小重制范围，保守不打扰。`严审刷新`=skill 更新后先按最新 skill 刷新便宜文字阶段（分镜/出图 prompt），再用最新 prompt/QC/review 标准严审旧图；凡 block/warn/降级或人工判定不符合最新预期的镜，都应舍弃旧图并排入重出。旧值 `保图刷新` 兼容归一为 `严审刷新`，但不再表达“尽量保住图片”。详见 n2d-update「严审刷新」段）
- `首切范围`: 部分先切 \| 全篇粗切　（默认 **部分先切·前 10 集·推荐**：首次拿到小说时省略 `--limit` 只落地前 10 集 raw 脚手架，同时写 `脚本/split_plan.json`、`脚本/_拆集复核.md` 的全本候选断点估算；先验证压缩率、边界和画风，避免超长书一开局铺出上千个目录。续切用 `--limit N`，全篇粗切必须显式 `--all`，只在边界策略已确认或准备批量推进时使用。**仅首次拆集/续切这一步用**，问一次写回 `_设置.md` 之后沉默沿用；不改三层拆集/P0→P6 边界铁律。详见 n2d-script 第 1 步）
