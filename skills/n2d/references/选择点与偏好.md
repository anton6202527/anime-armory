# 制漫剧 n2d · 选择点与偏好约定（本线自包含）

> 本线（n2d）私有的偏好/选择点约定。原 `skills/n2d/references/选择点与偏好.md` 已按线拆分，本线自带这一份，
> 不依赖任何公共层。通用机制对所有线一致；下方「选择点目录」只列本线的选择点。

# 偏好约定（所有自定义 skill 共用 · 通用机制）

> 这是**共享代码侧**的通用约定，不含任何具体用户的选择。
> 用户的实际选择是**私有的**，落在用户自己的空间，绝不写进 skill 源码。
> 任何 skill 涉及「让用户选」的地方，都按本约定读写，不要把某个平台/后端写死成唯一路径。

## 三句话原则
1. **skill 通用**：不写死平台/后端/分辨率；这些是「选择点」，由用户私有文件决定。
2. **选择私有**：用户的选择存在用户自己的空间（见下），不外发、不进共享 skill。
3. **持续记录 · 最新为准**：选择点首次遇到 → 问一次 → 写入 `_设置.md` → 同项目之后**沉默沿用**；用户随时可改（说一声或直接改文件）。**用户在任何一句输入里显式点名某选择点的值（哪怕之前已设过、哪怕在流程中途、哪怕和当前任务无关）→ 立刻以新值覆盖 `_设置.md` 对应行 + 追加一条 `## 记录`，最新选择永远优先于旧值 / 全局默认 / 缺省。** 听到即落档，不要"等下次用到该选择点再写"。

## 候选项刷新与用户输入口

选择点目录里的可选值只是**候选快照**，不是永久真值，也不是对用户输入的封死枚举。

- **可刷新来源**：候选项可来自项目 references、已有生产数据/投放回灌、模型/平台官方文档、实时搜索、以及 AI 代理的专业知识库。模型、平台、价格、规格、法规、榜单、API 能力等高变化信息，在正式花钱/不可逆/合规相关步骤前必须按需要核验；刷新时写清核验日期和来源，不能联网或不能确认时明确标注“未核验”。
- **用户输入口**：每个选择点都保留 `自定义` / `manual` / 直接手输值的入口。`skills/n2d/_lib/settings.py` 的 schema 只用于安全校验和常见别名归一，不得把用户的合理新选择挡在 skill 外；确属实验值时用 `python3 skills/n2d-settings/scripts/settings_cli.py set <作品根> <选择点> <值> --force` 落档，并在 `## 记录` 说明原因。
- **菜单不等于执行逻辑**：菜单文案服务于人类决策；脚本执行时必须先把值归一成能力、参数、后端入口和降级策略。不要在任一 stage 里写 `if 菜单文案 then 唯一路径` 的硬绑定。
- **机检与刷新工具**：易变候选清单都在 `skills/n2d/_lib/freshness.py:CANDIDATE_SOURCES` 登记并带 `采集日期：YYYY-MM-DD` 戳记；`python3 skills/n2d/_lib/freshness.py` 机检谁过期；`本线 `_lib/refresh.py`` skill 跑「搜索核验 → 改候选 → bump 日期 + 落 provenance」的刷新流程。新增易变清单 → 登记 + 加戳，否则机检看不到。

## 适配层原则

不同选择带来的差异由适配层消化，产线正文只消费统一契约。

- **标准先行**：skill 标准只写“必须达到什么”，不写“某后端怎么做”。选择点不得降低标准；只能改变实现路径、成本、速度和需要的弥补措施。
- **归一化**：把用户选择映射为 canonical key、能力标签（如多参考/主体库/原生音画/最大时长/分辨率上限）、实际 CLI/API/网页入口、参数包、fallback/degrade plan、合规 gate 和输出 schema。
- **专门适配**：用户选定某模型/平台/风格/规格后，下游 prompt、分镜约束、镜头时长、首尾帧/参考图策略、字幕/交付规格等都应按适配结果生成，不只是在 `_设置.md` 里记一个名字。
- **生图后端适配**：生图统一标准在 `skills/n2d/_lib/image_backend_standards.py`，`生图AI` 统一走 `skills/n2d/_lib/image_backend_adapter.py`。每次正式付费出图前先跑 `python3 skills/n2d/_lib/image_backend_adapter.py scan --json`：若扫描出多个可自动落 PNG 的官方/已登录后端，必须让用户选一个并写回 `_设置.md`；若一个都没有，停止并提示“当前无可用生图后端，请准备好可以生图的生图后端”；`needs_confirmation_backends` 只列为待人工确认，不当作可用。选定后再查官方文档/实时资料与本机 CLI/API help，把生成、编辑、多参考预算、主体库/Character ID、掩码、输出 schema、额度/价格等能力写入 per-run 证据：`python3 skills/n2d/_lib/image_backend_adapter.py record-refresh <作品根> --backend "<生图AI>" --source "<官方文档或CLI/API证据>" --note "<本次能力结论>"`。`image_preflight` 只认当天证据；缺证据就停，不用旧菜单猜 API。后端原生能力不足时，适配层自动输出标准补偿：参考组/脸嵌入、主体槽位、分层合成、文字 overlay、整图重抽、full QC、人审队列等。
- **生视频后端适配**：视频静态能力档在 `skills/n2d/_lib/n2d_platform_profiles.py`，执行适配层在 `skills/n2d/_lib/video_backend_adapter.py`。出视频前由 `n2d-model-router` 写 `video_model_routes.json`，再把 route 中实际会用的 primary/fallback 逐个查官方文档、本机 CLI/API help 或最小 smoke test，记录 per-run 证据：`python3 skills/n2d/_lib/video_backend_adapter.py record-refresh <作品根> --backend "<后端>" --channel "<生视频渠道>" --source "<官方文档或CLI/API证据>" --note "<本次能力结论>"`。`video` gate 只认当天证据；缺证据就停，不用旧模型矩阵猜单 Clip 上限、首尾/多帧消费、原生音画/口型、身份绑定或输出 schema。中段锚帧执行也读 `anchor_consumption_plan`：`native_multiframe` 才能一次消费首/中/尾，`split_relay` 要拆段，`reference_only_qc` 不得假装时间轴锚。
- **透明失败**：适配层缺能力、缺凭证、缺官方入口或候选项已过期时，停下列明缺口和可选替代；不要静默改用另一个后端，也不要把旧候选清单当作当前事实。
- **解耦边界**：候选项目录、用户私有选择、执行适配、质量/合规闸门分开维护。新增模型/平台优先补适配表或 capability profile；只有真正影响用户决策的项目才进入本文件的选择点目录。

## AI 代理交互约定 (Interactive Flow)

**原则：把枯燥的脚本命令藏在背后，让用户只做决策。**

当一个工作流包含**「机器负责结构计算（如拆分 Clip），紧接着需要大模型负责语义理解（如写 Prompt）」**的组合操作时，**绝对不要**把两段割裂的 CLI 脚本硬塞给用户让他们自己去敲。

- **正确的做法**：在第一个脚本跑完后，建立明确的**【AI 代理交互节点】**。
- **用户体验**：AI 代理应主动弹出高亮提示，或者用自然语言询问：“已完成基础切分。是否需要我启动 XXX 引擎为你自动补全提示词？”
- **后台执行**：用户回答“是”之后，AI 代理在后台默默**跑脚本 → 读取提示词 → 调用自身的 LLM 生成 JSON 结果 → 注入项目**，一气呵成。使用户感受到的是一个连贯、智能的“创作助理”，而不是一本枯燥的代码说明书。

## 两层私有存储

| 层 | 路径 | 谁写 | 作用 |
|---|---|---|---|
| **作品级** | `<作品根>/_设置.md` | skill 自动读写，用户可手改 | 本作品/曲/小说的实际选择（**权威**，覆盖全局） |
| **全局默认** | 私有全局默认 `创作偏好-默认.md`（仓库根或 `.agents/` / `.codex/` 等用户空间） | 用户维护 | 跨项目的个人默认（如「生图后端 / 视频后端 / 720p / CosyVoice / Suno」），开新项目时**预填** `_设置.md` |

- `<作品根>`：`创作区/制漫剧/<剧名>/`，与 `_进度.md` 同级。
- 全局默认使用工具中立路径：优先读仓库根 `创作偏好-默认.md`，再读 `.agents/创作偏好-默认.md`、`.codex/创作偏好-默认.md`；旧项目的 `.claude/创作偏好-默认.md` 仅作兼容读取，不作为新的共享入口。skill 文本引用「全局默认偏好文件」即指这些私有候选文件。

## 解析顺序（每次要用某选择点时）
0. **（最高优先）用户本轮输入显式指定了该选择点** → 立刻按新值覆盖 `_设置.md` + 记一行，**用新值**，跳过下面的读取。这条对**所有**选择点恒成立，且不限于"正要用到"时——只要用户一句话点名某选择，当场落档（见三句话原则 #3）。
1. 读 `<作品根>/_设置.md` 里该选择点 → 有就**直接用，不问**。
2. 没有 → 读全局默认 `创作偏好-默认.md` → 有就用其值**预填**，并**首次告知一句**（"默认 X，要改说一声"），随即写进 `_设置.md`。
3. 全局也没有 → **问用户一次**，写进 `_设置.md`。
4. 之后同项目同选择点：**沉默沿用**，不再问。
5. 无作品根的独立调用：能定位项目就读其 `_设置.md`，否则用全局默认 / 现问，不强行落文件。

> 例外：**合规/不可逆/花钱多**的点（真人嗓授权、版权来源、删除回流）即便记录过也要确认——安全 > 省事。

## `_设置.md` 格式（人类可读 · 可手改）
```markdown
# _设置 · <作品名>   （你的私有选择；skill 自动读写，也可手改。改主意随时说）

## 选择
- <选择点>: <值>        # <可选值>
...

## 记录
- 2026-06-03 初始化（继承全局默认）
- 2026-06-03 生视频模型 Seedance 2.0→Kling 3.0（用户指定）
```


## 选择点目录（各家族暴露给用户的选择 + 缺省回退）

> 缺省值仅作**全局默认的初始建议**；真正生效的是 `_设置.md` 里的值。


### 制漫剧 n2d 系列
- `制作模式`: 原生音画 \| 配音先行 \| 先出视频后配音　（默认 **原生音画**（2026-06-16 起）：Seedance 2.0/Veo 3 类后端对说话镜一次出同步音画（台词+口型+环境声），绕过前期配音/对口型，**最快看到出图/出视频、原生音轨即成片音轨无重同步返工**；Sora 已是 legacy/manual-only，不进原生音画自动候选；代价是需原生音画后端（image2video-only 后端说话镜会降级，router 写 degrade_plan），少了逐句音色控制；仿真人音色需授权。`配音先行`=**强配音控制时推荐**：真实配音时长驱动镜头/Clip，音画准、逐句音色/语速可控、返工少，追求配音表演或后端不支持原生音画时选。`先出视频后配音`=**快速demo·不推荐**：镜头时长只能按台词文本估算锁死，后期补真实配音对不上 → 音画不同步/语速怪/可能重切重出视频；仅纯视觉demo/比稿用，**或配音素材一时没就绪时先把画面推起来**，过审/补齐后仍要回头补真音重定时。三种流程图 + 完整理由 + 闸门放行方式见 **n2d SKILL「制作模式」节**——选 `先出视频后配音` 时各阶段入口必复述其警告。**⚠️ 例外——首跑必给菜单**：此点是影响全程的出片顺序决策，**不**走"全局默认静默预填"那条；**新作品首次拆集时必须把三模式 + 一句话原因念给用户选一次**（即便全局默认是原生音画），用户选后落 `_设置.md`、之后沉默沿用、随时可改。菜单原话见 n2d SKILL「制作模式 · 首跑选择」段）
- `题材`: 系统流 \| 穿越 \| 修仙 \| 都市 \| 宫斗 \| 赘婿 \| 战神 \| 自定义　（**弱选择点·检测预填可覆盖**——非铁律。n2d-script 在拆集/分镜阶段跑 `motif_detector.py`，按 `n2d_const.GENRE_KEYWORDS` 自动识别题材并**预填建议值**，不静默生效：识别到后给"我判定为 X 题材、发现 N 处母题桥段（系统面板出现/升级…），要启用母题增强吗"的菜单一次，用户确认/改写后落 `_设置.md`，同项目沉默沿用。题材本身不烧钱、不必每次重确认；但它驱动的母题增强若注入出图/出视频，那一步仍走 `生成粒度` 每次确认。题材决定哪些复现桥段套用母题模板，见 `n2d-script/references/题材母题框架.md`）
- `母题增强`: 开启 \| 关闭 \| 仅建议　（默认 **仅建议**：检测到复现母题桥段（系统面板/升级/签到/抽奖/爆装备）时只在 `生产数据/motif_plan_第N集.md` 给增强建议（场景/道具/台词/成长档位/overlay 文字层），**人确认后**才把 `motif_id`/`template:system_panel` 注回 `storyboard.json` 并写 `出图/共享/motif_registry.json`；`开启`=检测到即注入建议态（仍不直接花钱出图）；`关闭`=不做母题增强。系统面板是首个落地母题样板，混合渲染：AI 出锁色锁形的发光光幕底框（`VFX_系统面板`，禁烤文字），等级/属性数值由 n2d-compose 期 Pillow overlay 叠清晰文字层。见 `题材母题框架.md`）
- `基础视觉风格`: 真实3D人物质感 + 电影叙事镜头感 \| 写实电影感 \| 国漫写实角色审美 + 电影级布光与镜头语言 \| 国漫写实 \| 二次元赛璐璐 \| 水墨国风 \| 厚涂幻想 \| 赛博霓虹 \| Q版轻喜 \| 自定义　（默认 **真实3D人物质感 + 电影叙事镜头感**，但它只是预选，不是 skill 铁律。此点决定 `设定库/global_style.md`、`storyboard.json.style_contract`、出图/出视频总览「本集基础视觉风格契约」和逐镜 prompt 的画风词/负面词。**新作品首次写 global_style 前必须给菜单**：若 `_设置.md` 已有或用户本轮明说则直接用；若只有全局默认，把默认标为预选但仍展示菜单；选后写 `_设置.md`，同项目沉默沿用。旧项目里的 `cinematic_contract` /「本集真实电影感契约」可兼容，但下次改故事板/总览时迁到 `style_contract` /「本集基础视觉风格契约」。菜单与派生规则见 `n2d/references/visual_styles.md`）
- `拆集节奏`: 前长后短 \| 均衡 \| 快节奏 \| 长集 \| 自定义（**内部软默认/高级覆盖项，不列入首跑菜单；旧键 `单集时长` 兼容读取**）　（默认 前长后短：第1集更充分立世界观/主角欲望/系列总钩，后续更碎快追更。默认粗拆不锚字数，按章/场景/强钩候选出粗胚；边界必须优先保证剧情连贯、剧情丰满与 **冲突→爽点/反转→钩子** 闭环，不得为省时长删掉必要镜头/铺垫/动机/承接。真实时长只用于配音/原生音画后的节奏复核；`--target` 仅作显式高级报告参考，不参与默认切点决策。新作品不再向用户询问“每集多长”；只有用户主动要求更快/更长/自定义节奏时才写入 `_设置.md`）
- `变现模式`: 免费 \| 付费 \| 海外（**内部软默认/高级覆盖项，不列入首跑菜单**）　（默认 **免费**：拆集结构的商业轴，决定**剧级追更骨架**的断点策略与付费/AD 卡点定位——`免费`(红果/番茄·完播率导向·全剧延续性·断点平均强·消灭连续弱钩集群) / `付费`(小程序剧·前十集卡点峰值·付费墙集须最强 cliffhanger) / `海外`(ReelShort/TikTok·更碎更狠·前3集立钩)。只在用户明确变现模式或目标平台时落 `_设置.md`；不改 P0→P6 单集边界铁律本身，只改整部断点强度的分布目标。驱动 `boundary_audit.py` 的「剧级追更骨架」段，见 `n2d-script/references/追更骨架.md`）
- `生视频模型`: Seedance 2.0 \| Veo 3.1 \| Kling 3.0 \| Hailuo 02/2.3 \| Runway Gen-4 \| Luma Ray3.2 \| Pika 2.5 \| HunyuanVideo 1.5 \| Wan 2.2 \| LTX-2.3 \| manual　（**n2d-video 阶段选择点**：默认 **Seedance 2.0** 只是普通镜/兜底建议，不是首跑必选，也不是静默固定。Sora 只保留旧项目/manual 兼容，自动路由不再选它；如用户显式固定 Sora，需人工确认可用性。只有用户明确固定、`视频模型路由=固定生视频模型`、账号/交付只能单后端，或 n2d-video 的 router/probe 找不到可执行后端时，才在出视频前问并落档。旧项目的 `生视频AI` 兼容读取为模型/后端 fallback，但新项目不要再写这个合并字段）
- `生视频渠道`: 即梦/Dreamina \| 豆包 \| 海螺AI \| 可灵/Kling \| Google Gemini API \| Runway API \| Luma Dream Machine \| Pika \| 本地/开源 \| manual　（**n2d-video 阶段选择点**：默认 **即梦/Dreamina** 只是调用入口偏好/兜底。渠道/产品和模型分开：例如即梦或海螺可能调用 Seedance 2.0；Google Gemini API 可能调用 Veo 3.1。自动路由时实际执行入口由 `n2d-model-router` + CLI/API 探测 + 适配层决定；只有固定模式、用户指定、账号/交付约束，或 n2d-video preflight 找不到可执行后端时才需要用户选择）
- `视频模型路由`: 自动按镜头路由 \| 固定生视频模型　（默认 **自动按镜头路由**：新作品首跑不问具体视频模型/渠道；生视频后端选择延后到 n2d-video 出视频前。`生视频模型` 只作为普通镜/兜底模型，不再固定每个 Clip。打斗/追逐/对话反打/真相揭示/公开对质/关系转折/飞行/空镜/法术爆发/亲密互动/多人站位由 `n2d-model-router` 按镜头类型、专项模板、身份注册层、原生音画策略、后端能力和时长上限选择 primary/fallback；`固定生视频模型` 仅用于账号/预算/交付约束明确要求单模型时，仍必须写每 Clip 路由字段和 fallback/degrade plan。旧值 `固定生视频AI` 兼容）
- `投放时效`: 实时 \| 隔夜批量　（**成本轴·与质量档正交·默认 实时**。2026 视频生成 API 首现「batch/隔夜半价」（Sora2 Batch 24h SLA -50%、Seedance flex -50% 预告）。**非赶投放窗口的量产放量集**可设 `隔夜批量`，`n2d-model-router` 写 `urgency_tier=batch_24h`，dashboard 按 `provider#batch_24h` 拆账回看省了多少；首集打样/赶投放窗口保 `实时`（默认·绝不静默延迟）。**诚实边界**：实际 async batch endpoint 由视频后端能力决定，属执行适配层 follow-up（后端 batch 通道接入后才真省）——本选择点先产路由意图 + 成本拆账，通道一通即生效。详见 `n2d/references/模型矩阵.md` 视频 § + n2d-model-router）
- `视频备用后端`: 无 \| 即梦 \| 可灵 \| Seedance \| Veo \| Runway \| manual　（默认空=由路由器写理论 fallback。Sora 不再作为自动 fallback，只能旧项目/manual 人工确认。仅 `视频模型路由=固定生视频模型` 时可设 `无`，表示本机/本项目只允许默认生视频 CLI/渠道，路由表不得把不可执行后端写成可用 fallback；失败两次后应暂停询问，不自动切后端。旧值 `固定生视频AI` 兼容）
- `生图AI`: Codex \| OpenAI \| Dreamina/即梦官方 CLI \| Seedream \| 可灵主体库 \| Nano Banana \| Sora Cameo(旧项目/manual) \| 自定义官方后端　（默认 **Codex**。正式生图前扫描本机/当前会话能力：多个可用后端 → 给用户菜单和建议，让用户选一个；0 个可用 → 停止并提示准备生图后端；1 个可用且未显式设置 → 可建议使用并落档。生产闸门只阻断两类：① 同一作品/同一集混用多个生图后端；② `同视频AI` 含糊口径、第三方逆向或未授权出图路径。官方 Dreamina/即梦 CLI、官方 Seedream API、官方 OpenAI/Codex、可灵主体库、Nano Banana 等可作为选择点，但必须全集统一一个后端并能落 PNG；Sora Cameo 仅旧项目/人工路径兼容，不作新项目默认推荐。所选后端不可用时停下报告，不偷偷换后端兜底。旧项目若写 `生图AI: 同视频AI`，先改成显式后端名。）
- `同场景批量出图`: 开启 \| 关闭　（默认 **开启**·P2b 默认路径。开启时 n2d-image 建本集分镜图前先跑 `scene_batch.py`，把连续同场景[同 LOC]≥2 镜编成 `scene_coherent_batch`——共享场景定妆/光位锚/风格契约+同源种子，按一段连贯场景 pass 出再拆镜，用生成模型时空一致性根治同场景多镜漂移[DreamShot 思路]。只锁场景/光/风格，不锁人物身份[身份仍走两层定妆库+多人同框空间槽位]；单组<2 镜或跨场景切换自动断组回退逐镜独立出。关闭=全部逐镜独立出图）
- `配音后端`: CosyVoice \| GPT-SoVITS \| MiniMax \| 火山 \| say占位　（默认 CosyVoice，缺凭证回退 say）
- `出视频规格`: 预算充足 \| 预算一般 \| 预算不够　（默认 **预算一般**·对齐既有 720p 默认。三档各预设*分辨率·帧率·每Clip跑几条挑稳·平台质量档*：充足=1080p/30fps/关键镜2-3条·普通镜2条/高质量档；一般=720p/24-30fps/关键镜2条·普通镜1条/标准档；不够=720p/24fps/全1条/省积分档。**调生视频模型/渠道前每次告知当前档**·首次问一次记档之后沉默沿用可随时改；`视频分辨率`等单项可单独覆盖。详见 n2d-video「出视频规格」三档表）
- `视频分辨率`: 720p \| 1080p　（默认 720p；通常由 `出视频规格` 档预设，亦可单独覆盖）
- `画幅`: 9:16 \| 16:9　（默认 9:16 竖屏短剧）
- `对口型`: 关闭 \| 配音对齐 \| 后期pass　（默认 关闭；仅正面说话特写且预算允许时启用。`配音对齐` = `voice_conditioned_lipsync`，把已生成的克隆配音作为口型条件喂给支持音频参考口型的后端，成片音轨仍走配音轨；旧值 `平台原生` 兼容为 `配音对齐`，但新项目不再使用这个含糊命名，避免和 `原生音画` 混淆）
- `BGM来源`: 占位 \| 文件 \| Suno　（默认 占位）
- `视频原生音轨`: 丢弃 \| 低音量混入环境声 \| 保留原片音轨　（默认 **丢弃**：Veo/Seedance 出的 clip 可能自带原生音轨[环境音甚至台词]，n2d-compose 转码时剥掉，音频全由 配音+BGM+SFX 受控链路提供，避免双人声；`低音量混入环境声`=确认无原生台词时压低混作底；`保留原片音轨`=仅无配音/测试预览/明确要原片声时，有配音轨时必先提醒双人声风险。n2d-video 阶段不剥音轨，本点只由 n2d-compose 执行。详见 n2d-compose「clip 原生音频处理」）
- `接缝兜底`: 硬切 \| 微溶解 \| 报警　（默认按"有意硬切硬切、跳变溶解、缺空镜报警"：n2d-compose 按 `故事板.md` 每个接缝的 `转场类型` 决定接法——match cut/动作切/有尾帧接力的硬切=直接硬切；接点没焊住又非有意硬切的明显跳变=加 0.1–0.3s 微交叉溶解兜底；契约要空镜缓冲但缺料=停下报警，不默默糊过。爽点/反转的有意硬切**不加**溶解。详见 n2d-compose「按转场类型接 clip」）
- `目标平台`: 抖音 \| 快手 \| B站 \| 小红书 \| 红果 \| YouTube \| TikTok \| ReelShort \| 跨平台 \| 未定　（默认 未定；影响合规包 `platform_review.targets`、画幅、字幕。正式投放前不得停留在 未定；若跨平台，逐平台写 gate profile，不用一个泛化“全平台通过”代替）
- `发行地区`: 中国大陆 \| 港澳台 \| 北美 \| 东南亚 \| 全球 \| 自定义　（默认 中国大陆；影响 `localization`、字幕语言、平台审核。海外/全球投放必须在合规包里声明本地化状态和目标语言）
- `合规用途`: internal_only \| publish_candidate \| paid_distribution　（默认 publish_candidate；internal_only 只允许内部预览，gate 会跳过平台投放项但保留 WARN；publish_candidate/paid_distribution 必须完成版权、角色授权、声音克隆、平台审核和出海本地化）
- `字幕语言`: 中文 \| 中英双语 \| 仅英文　（默认 中文·国内投放；海外投放 TikTok/ReelShort/YouTube/北美短剧 选 中英双语 或 仅英文。**翻译是投放选择，不写死在 skill**——选中英/仅英时 n2d-script 才产 `字幕_英文.srt`，否则只产 `字幕_中文.srt`；海外投放时合规包 `localization.target_languages` 必须覆盖实际字幕）
- `一致性增强`: 锚点+参考图 \| +LoRA　（默认 锚点+参考图，LoRA 不启用。**LoRA 训练参数（底模 `--base-model`、训练后端 `--provider`、`--steps/--rank/--lr` 等）刻意不做静默预填选择点**，属 n2d-lora 的算子级 CLI 旋钮，由操作者每次显式传参并落进验证报告/数据集 manifest 审计留痕——它们影响一致性与许可风险，不应被"全局默认沉默沿用"。要不要启用 LoRA 这一开关才是选择点，怎么训是算子决策。）
- `一致性严格度`: demo \| production　（默认 demo：草稿/内部批量阶段，噪声型机检保留 WARN；production：正式交付/投放前，gate 会把重复同维度、关键场景或交付边界的轴线/视线、音画同步、节奏密度等 advisory 升级为 BLOCK，并要求 `生产数据/consistency_advisory_signoff_第N集.json` 人工签收才能放行。也可用 env `N2D_CONSISTENCY_PROFILE=production` 临时覆盖。）
- `脸一致性机检后端`: arcface \| styleid \| 自动按画风　（**n2d-review 作品质检/QC 选择点**，只决定崩脸机检的人脸相似度后端，不影响生图/出视频。默认 **arcface**(insightface buffalo_l)·写实/真人风画面用。**风格化/漫剧/插画脸建议 styleid**(CLIP-L+StyleID LoRA)——ArcFace 在风格化脸 TPR 系统性掉到 ~0.76(StyleID 论文 arXiv:2604.21689·2026-04)，按真人阈值判崩脸会误杀/放过。**诚实铁律：裸 CLIP 比 ArcFace 还差**(0.17–0.25)，故 styleid 必须加载真正的 StyleID 权重(`N2D_STYLEID_MODEL` 指向 CLIP-L+LoRA checkpoint)；缺权重时 `face_consistency.py` **自动回退 arcface 并标 `arcface_fallback`**(score 按低精度降权)，绝不静默用裸 CLIP。自标定 flag-band 地板随后端尺度自动移，但 `n2d-score` 的可投放线 KPI(`N2D_CONSISTENCY_RELEASE_LINE`)须随 encoder 重标定。解析顺序：CLI `--encoder` > `_设置.md` 本选择点 > env `N2D_FACE_EMBEDDER` > 默认 arcface。`自动按画风` 目前等价回退 arcface（按本集风格指纹自动选 encoder 是 follow-up，需接 style_consistency 分类）。详见 n2d-review「角色 DNA 用图判」）
- `重抽预算策略`: 预算充足 \| 预算一般　（默认 **预算充足**。全线图片重抽统一两档：`预算充足`=所有图片严格自检，出到满意为止；`预算一般`=只关键图片严格自检并出到满意为止，普通图无核心错位/身份漂移/硬性禁忌即可收。旧值 `预算不足` / `预算不够` 兼容归并为 `预算一般`。详见 n2d-image「重抽预算铁律」两档表）
- `生成粒度`: 逐个 \| 小批 \| 按场景分批 \| 整集　（默认 **逐个**：每出 1 张图/1 个 Clip 就停下让用户审、可即时重抽/改 prompt 再继续；`小批`=每批 N 个一停，N 默认 5 可改；`按场景分批`=同场景连续镜一组一停；`整集`=旧行为一次过、最后统一报告。**任何档都先过共享定妆硬闸门**。⚠️**例外——每次都问**：本点属花钱/token 敏感点，**每次进出图/出视频前都要把四档菜单念给用户选一次，不沉默沿用**；`_设置.md` 里的值只作「默认建议/上次选择」预选，不替代询问。出图/出视频通用，菜单原话见 n2d-image / n2d-video「生成粒度 + 优先序」段）
- `生成优先序`: 关键镜优先 \| 分镜顺序 \| 先易后难　（默认 **关键镜优先**：🔑爽点/反转/钩子/封面候选先出，先定基调先锁主角脸；`分镜顺序`=Clip1→N 叙事序；`先易后难`=单人静态/空镜先、复杂打斗/多人镜后。共享定妆库永远排在最前，与本点无关）
- `脚本批次`: 逐集 \| 小批 \| 整批　（默认 **逐集**：n2d-script 量产剧本改编/分镜设计时每 1 集停下报**边界决策 + 物料清单**再继续；`小批`=每 N 集一停 N 默认 5 可改、附 5-10 集边界复核窗口；`整批`=指定范围一次过最后统一报告。文本不烧钱，**问一次后沉默沿用**，不像 `生成粒度` 每次弹菜单；只管"几集一停"不改三层拆集/P0→P6 边界铁律；第1集永远单独逐集。详见 n2d-script「脚本批次」段）
- `中段锚帧默认`: 开启 \| 关闭　（**默认铁律·能力门控**——三帧契约是默认强制项，**不是 cost/风格可选偏好**：**默认每个 Clip 至少 3 帧（首帧+中段锚帧(`_mid`)+尾帧），不管 cost**（图便宜、视频走 multiframe2video 仍 1 次/Clip；前瞻判断：后端越来越支持多帧、加帧成本只会下降）。**唯一豁免=路由视频后端不支持 ≥3 帧**（first-frame-only，连首尾拆段都钉不住第 3 帧，如 seedance 直连/runway/pika；Sora legacy/manual-only 不作自动豁免依据），由 adapter 层 `n2d_platform_profiles.backend_supports_three_plus_frames` **按后端能力自动判定**——即梦/dreamina(原生多帧)、可灵/Veo/Luma(首尾档·可拆段凑≥3帧)、未知/新后端 全部按"支持"→强制。n2d-script 阶段2 定稿后 `anchor_planner.py` 自动跑：能力支持的后端**强制规划**（覆盖本选择点设为关闭也无效），让每镜拿 `_mid`，打斗/长镜由 R1/R2/R3 出更多中锚（>3 帧）；**极短镜(<3s)与末镜豁免**（写 `midframe_exempt_reason`/本就无尾帧）。**gate 默认强制（铁律）**：读 `storyboard.policy.video_backend` 判后端能力，缺/未知=按强制；契约前定稿的旧集会被 gate 拦、须补跑 `anchor_planner --write`。`关闭` 选择点**只对真不支持 ≥3 帧的后端生效**（能力 adapter 才是真值源·非用户偏好）；不因省成本/纯静态而关——这条**有意不留 cost 逃生舱**（逃生舱=后端能力，由 adapter 路由，符合仓库「路由到能力·不 branch 菜单文字」铁规）。详见 n2d-script 阶段2「中段锚帧自动规划」+ n2d Q&A Q42/Q43）
- `更新重制策略`: 最小 \| 严审刷新　（默认 **最小**：`n2d-update` 只回放到该集已到阶段、按变动文件算最小重制范围，保守不打扰。`严审刷新`=skill 更新后先按最新 skill 刷新便宜文字阶段（分镜/出图 prompt），再用最新 prompt/QC/review 标准严审旧图；凡 block/warn/降级或人工判定不符合最新预期的镜，都应舍弃旧图并排入重出。旧值 `保图刷新` 兼容归一为 `严审刷新`，但不再表达“尽量保住图片”。详见 n2d-update「严审刷新」段）
- `首切范围`: 部分先切 \| 全篇粗切　（默认 **部分先切·推荐**：首次拿到小说时**不**一次性粗切整本，只粗切前 N 集（默认 N=3，可改）→ 精修第1集验证节拍/画风/角色卡 → 满意后重跑 split 加大 `--limit` 续切。粗拆只是脚手架、边界后面还要按节拍重切，先切全本只是徒增一堆待返工的占位目录；先切一小段把第1集打磨对、把全局/角色卡立稳，再批量续切更省返工。`全篇粗切`=一次粗切整本（适合短篇、或边界已在 `_拆集复核.md` 定稿只差落词）。**仅首次拆集这一步用**，问一次写回 `_设置.md` 之后沉默沿用；不改三层拆集/P0→P6 边界铁律。详见 n2d-script 第 1 步）
