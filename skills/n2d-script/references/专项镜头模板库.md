# 专项镜头模板库（复杂镜头）

复杂镜头不要每次从零写 prompt。凡涉及高动作、高空间关系或多人调度的镜头，先在本库选模板，再写 `分镜剧本.md`、`故事板.md` 和 `storyboard.json`。模板的目标不是把画面写死，而是把 AI 最容易失控的变量提前锁住：轴线、站位、动作节拍、起幅落幅、关键帧、负向约束和下游回流点。

## 0. storyboard.json 必填契约

命中本库任一模板的 Clip，必须在 `clips[]` 里写：

```json
{
  "id": "EP01_CLIP04",
  "label": "王敦追入山洞",
  "template": "chase",
  "template_contract": {
    "template_id": "chase",
    "beats": ["起跑", "障碍", "距离变化", "落幅"],
    "blocking": "王敦画面左前向画右深处奔跑，追兵只用影子/脚步声提示",
    "pose_reference_required": true,
    "camera_rule": "跟拍或侧向平移，不越轴，不做无理由旋转",
    "continuity_must": ["出入画方向保持左→右", "衣服发型不变", "洞口光位不跳"],
    "negative": ["不要瞬移", "不要新增人物正脸", "不要让角色换方向"],
    "speed_curve": "起跑加速→障碍半顿→再加速",
    "spatial_path": "画左前景沿洞壁折线到画右深处",
    "camera_path": "侧向跟拍，不环绕",
    "parallax_layers": ["前景石柱快", "洞壁中速", "洞口光慢"],
    "readability_beats": ["障碍前半拍看清方向", "落幅停在转弯点"]
  }
}
```

通用必填字段：

| 字段 | 要求 |
|---|---|
| `template` | 模板 ID，必须取本库模板；旧项目允许 legacy `multi_person_blocking` |
| `template_contract.template_id` | 与 `template` 完全一致 |
| `pose_reference_required` | 【新增硬控】对于高空间要求动作（如打斗、追逐），强迫提供骨架参考（`pose_image_path`）供 ControlNet 锁定 |
| `regional_construct_required` | 【新增硬控】涉及双人贴身、亲密互动、近战交锋时，若后端无原生位置锁定，强制分区合成（先出底图再分左右刷人），禁止端到端单次渲染以防串脸。 |
| `impact_frame_sync` | 【新增硬控】法术对轰/招式碰撞专属。必须在视频生成中规划明确的“受击高光爆发帧（mid-frame）”。 |
| `beats` | 2-6 个动作/表演节拍，按时间顺序写 |
| `blocking` | 人物左右/前后/距离/出入画方向 |
| `camera_rule` | 允许的机位和运镜边界 |
| `continuity_must` | 必须继承到出图/出视频的连续性约束 |
| `negative` | 该模板最容易翻车的负向约束 |
| `keyframe_plan` | 高动态/高奇观镜必填：start / intent_mid / impact_or_apex / result_or_recovery / end |
| `post_cue_points` | 高动态/高奇观镜必填：pre_peak / peak / aftershock_or_hold 的剪辑、音效、闪白、震屏等 cue |
| `physics_guard` | 高动态/高奇观镜必填：身份、轴线/路径、接触点或 VFX 形态、禁止新增动作 |

> gate 会在出图/出视频前检查：复杂镜头缺 `template/template_contract`，或模板字段不全，必须回 `n2d-script` 补。

---

## 0.1 Motion Control 分级

涉及真实接触、遮挡、高速追逐或飞行的模板要先决定是否直接生成，不能留给视频 prompt 临场猜：

| 风险 | 适用模板 | 下游要求 |
|---|---|---|
| required | `fight_exchange`、`chase`、`magic_burst`、`flight`、`mount_ride`、`vehicle_ride`、`vessel_flight`、`road_vehicle`、`stealth_stalk`、`dual_cultivation`、`kiss_or_near_kiss`、`intimate_interaction`、`hug_or_pull`、`multi_character_same_frame`、`ensemble_blocking`、`multi_person_blocking` | `n2d-model-router` 会写 `motion_control.level=required`；出视频前必须有 `motion_control_manifest.json`，状态为 `ready` 或 `degrade_only` |
| none | 普通单人反应、空镜、低幅度动作 | 不需要 Motion Control manifest |

`ready` manifest 代表已有 pose/depth/instance/contact 或 camera_path/spatial_path/parallax_layers 等控制资产。`degrade_only` 代表不直接生成全身复杂接触或长连续高速动作，改成手部特写、过肩反打、正反打追逐、巡航/机动/抵达拆镜等可控短镜。OpenPose/DWPose 只能锁姿态；拥抱、抓腕、双修能量循环、刀剑命中还要写清 `occlusion_order`、`body_part_ownership` 和接触点，必要时用 depth/instance masks/contact_map。追逐/飞行不需要接触点，但必须锁 `camera_path`、`spatial_path` / `parallax_layers`，否则高速镜很容易翻方向、丢距离或变成背景贴图。御兽/坐骑额外需要 `contact_map` 或等价 `mount_contact` 说明；马车/飞舟额外锁载具剪影、轮转/高度和前中后景视差；现代车辆额外锁车道、车流和驾驶控制；尾随/潜入额外锁遮挡层、光影和距离曲线。

---

## 0.1b 高动作编排契约

`fight_exchange`、`chase`、`flight`、`mount_ride`、`vehicle_ride`、`vessel_flight`、`road_vehicle`、`stealth_stalk` 不能只写“精彩打斗/高速追逐/御剑飞行/骑兽狂奔/马车疾驰/飞舟掠空/车流疾驰/暗处尾随”。这些高动作镜头必须把动作拆成可读节拍，写进 `template_contract`，并由 `n2d-video` 继承到 `动作编排契约` 字段。

通用必填：

| 字段 | 要求 |
|---|---|
| `speed_curve` | 明确慢→快→顿→收，或巡航→加速→减速；禁止全程匀速 |
| `spatial_path` | 人/剑/云/追逐路线在画面里的路径，含入画/出画方向 |
| `camera_path` | 跟拍/侧移/快推/固定的路径和速度边界 |
| `readability_beats` | 观众看清命中、距离变化、穿云/转向的半拍或落点 |

模板专属补充：

| 模板 | 额外字段 |
|---|---|
| `fight_exchange` | `force_direction`、`recovery_beat`；`attack_path` 与 `impact_frame` 必须能支持命中帧 |
| `chase` | `parallax_layers`、`overtake_or_escape_beat`；距离曲线只能“追近”或“拉开”选一条主变化 |
| `flight` | `flight_path`、`altitude_curve`、`parallax_layers`、`mount_or_cloud_lock`；御剑/祥云/法器形态必须锁定 |
| `mount_ride` | `mount_contact`、`gait_cycle`、`harness_lock`、`screen_direction`、`parallax_layers`；骑手/坐骑接触、鞍具/缰绳和步态必须锁定 |
| `vehicle_ride` | `vehicle_lock`、`wheel_rotation`、`harness_lock`、`screen_direction`、`parallax_layers`；车体、轮数/轮转和牵引连接必须锁定 |
| `vessel_flight` | `vehicle_lock`、`flight_path`、`altitude_curve`、`screen_direction`、`parallax_layers`；飞舟/法器剪影、高度曲线和航向必须锁定 |
| `road_vehicle` | `vehicle_lock`、`wheel_rotation`、`driver_control_lock`、`lane_lock`、`traffic_flow`、`screen_direction`、`parallax_layers`；现代车体、车道、车流和驾驶控制必须锁定 |
| `stealth_stalk` | `screen_direction`、`distance_curve`、`occlusion_layers`、`light_shadow_lock`、`reveal_or_hide_beat`、`parallax_layers`；尾随距离、遮挡层、明暗和露出/躲开拍必须锁定 |

---

## 0.2 通用 motif（母题）模板形态

上面 §1–§10 是**动作/空间调度**模板。还有一类是**母题/复现场景**模板：穿越/系统流爽文里高频复现、相对固定但每次出现带成长属性的桥段——系统面板出现、主角升级、系统刷新、签到、抽奖、爆装备。它们由 `motif_detector.py` 自动检测、人确认后注入（见 `题材母题框架.md`）。母题模板与动作模板共用 §0 必填契约，但多三条铁律：

1. **固定模板 + 成长递进**：同一母题跨集套同一套视觉语言（机位/构图/色彩/台词腔调），但每次出现要有成长（面板进化、等级只增不减、属性攀升）。成长用 `motif_registry.json` 的 `growth_state_machine.progression`（单调，机检退级=block）+ 绑定 VFX 的 `lifecycle`（forms 即档位）。
2. **混合渲染（关键）**：AI 生图渲染不准清晰文字/数字，而面板恰恰全是数值。所以模板必须声明 `text_layer=overlay`：**AI 只画发光底框/符纹（VFX，锁色锁形），等级/属性/标题等数值由 n2d-compose 期 Pillow overlay 叠上**。模板的 `negative` 必须禁掉 AI 画任何可读文字/数字。
3. **台词母题腔**：系统音机械简短（`narrator_role=系统`→字幕灰小字、配音系统音色），主角反应是爽点释放（可标 `钩子=climax`→暖金大字）。

母题模板专属字段（写进 `template_contract`）：

| 字段 | 含义 |
|---|---|
| `motif_id` | 绑 `motif_registry.json` 的母题（如 `MOTIF_系统面板`） |
| `vfx_asset` | 绑成长 VFX（如 `VFX_系统面板`），从 `asset_registry` 继承 forms/lifecycle/锁色 |
| `text_layer` | `overlay`（数值走合成期文字层）/ `baked`（少数无文字纯特效母题才用） |
| `growth_ref` | 指向 progression 当前快照（at_clip） |

**系统面板是首个落地母题样板（§12）。** 后续 level_up / system_refresh / signin / gacha / loot 复用本形态：面板家族统一套 `system_panel` 模板 + `VFX_系统面板`，只在 `motif_registry` 加配置，**不新增模板代码**。

---

## 1. `fight_exchange` 打斗攻防

**使用场景**：拳脚、刀剑、掌法、法术近身命中、受击反应。完整武打拆招仍按 `打斗分镜.md`，本模板用于把 Clip 级契约写进 `storyboard.json`。

**拆法**：
- 起手：谁先动，力从哪里来。
- 发力：攻击路径清楚，不让 AI 自由编招。
- 命中/错身：命中帧或擦身帧单独锁图。
- 受击/反应：被打者只做一个明确反应。
- 收势：落到可接下一镜的姿态。

**额外必填字段**：`attack_path`、`impact_frame`、`action_scope`、`speed_curve`、`spatial_path`、`readability_beats`、`force_direction`、`screen_direction`(越轴锁·双人近身搏杀是 180° 越轴最高发区)、`recovery_beat`

**战斗精修建议字段（视觉盛宴·缺=WARN 不阻断·让动作/运镜/光/表情在命中帧同帧撞点）**：
- `combat_micro_expression` 表情：命中/受击/发力帧的 FACS 微表情（咬牙/狰狞/痛呼·AU4眉压+AU7睑紧+AU38鼻翼张）——别让打斗脸是空的，表情峰要和命中帧重合。
- `secondary_motion` 动作物理：发丝/衣摆/碎屑/血珠顺攻击方向甩出 + 落地扬尘（治"激烈动作里布料冻住"的假）。
- `apex_light` 光线：命中帧 motivated 闪光 + apex 轮廓光/边缘光（把主体从暗背景拔出）——这是 `scene_consistency.verify_impact_frames`「击中帧 >15% 亮度涌动」检测的**有意目标**，没有它打击感软弱。
> **铁律**：`impact_frame`/`collision_or_apex_frame` 不能只是声明字符串——`anchor_planner` 已 apex-aware，会把 `post_cue_points` 里带命中/砸落/爆发时间戳的那一拍**强制落成一张真关键帧**（use=keyframe），动作(hit-stop)·运镜(推镜/震屏)·光(闪光/轮廓)·表情(狰狞峰)四轴在这一帧汇聚 = 酣畅淋漓。

**提示词规则**：多人混战拆成单人/双人正反打；命中类 Clip 优先首尾双帧；镜头运动只服务打击感，不乱飞。若出现刀剑/手掌/身体接触，出视频前必须 ready Motion Control 或 degrade_only 拆成起手、命中特写、受击反应。

---

## 2. `chase` 追逐

**使用场景**：追杀、逃跑、巷道/山路/洞穴追逐、飞行追逐。仙侠飞行追逐还要同步读 `仙侠场面分镜.md`。

**拆法**：
- 建立方向：先锁屏幕方向和地理关系。
- 距离变化：追近/拉开只能选一个主变化。
- 障碍节拍：门槛、石壁、雨帘、树枝等只放 1-2 个。
- 落幅：停到转弯、回头、跌倒、藏入暗处等可剪点。

**额外必填字段**：`screen_direction`、`distance_curve`、`obstacle_beats`、`speed_curve`、`spatial_path`、`camera_path`、`parallax_layers`、`readability_beats`、`overtake_or_escape_beat`

**提示词规则**：速度感来自背景流动、前景掠过、脚步/衣摆/雨丝，不来自人物变形；同一 Clip 不要同时写追逐、打斗、法术爆发三件事。

---

## 3. `dialogue_shot_reverse` 对话反打

**使用场景**：逼问、谈判、对峙、男女主互相试探、主仆/师徒/敌我正反打。普通台词不必都套；只有需要正反打、过肩、视线对位、权力关系变化时使用。

**拆法**：
- 建立轴线：谁在画左/画右，视线朝向哪里。
- A 面：说话者景别、情绪、压迫感。
- B 面：反打对象反应，不改变左右关系。
- 插入物/手部：必要时用茶杯、剑柄、符纸、手指动作缓冲。
- 落幅：停在眼神或反应，服务下一句台词。

**额外必填字段**：`axis`、`eyeline`、`shot_pairing`、`screen_sides`、`coverage_order`、`camera_coverage`、`lens_height_distance_match`、`crossing_axis_policy`、`buffer_or_reestablishing`

| 字段 | 要求 |
|---|---|
| `axis` | 本场 180° 行动轴线：两人/人与目标之间的线，摄影机默认只在一侧工作 |
| `screen_sides` | A/B 或 CHAR_id 的屏幕左右关系，如 `{"left":"CHAR_A","right":"CHAR_B"}`；反打中不能交换 |
| `eyeline` | 互补视线：画左角色看画右、画右角色看画左；除 POV/破第四墙外不看镜头 |
| `shot_pairing` | A 面/B 面成对关系：clean single、OTS、reaction、insert 怎么互补 |
| `coverage_order` | 建立镜 → A 面 → B 面反打 → 插入物/反应缓冲 → 落幅的顺序 |
| `camera_coverage` | 用 clean single / OTS / insert / reaction 的组合，近景多人默认拆正反打或分层 |
| `lens_height_distance_match` | A/B 反打保持相近镜头高度、距离、景别和焦段；权力关系变化才允许有计划地偏离 |
| `crossing_axis_policy` | 默认禁止越轴；若剧情必须越轴，先用双人建立、中线镜、运动弧线或空镜缓冲重新定向 |
| `buffer_or_reestablishing` | 负责重新定向的镜头：双人建立、道具特写、手部动作、眼神 cutaway、空镜等 |

**提示词规则**：反打不是随机换角度；同场景主光、背景深度、人物左右关系必须守住。若要越轴，必须在 `crossing_axis_policy` 和 `buffer_or_reestablishing` 写明确剧情理由和过渡镜。P-2 `axis_blocking_map.json` 会把本模板物化为 `shot_reverse_patterns[]`；P-3 `continuity_bible.json` 会继承为 `shot_reverse_continuity`，供出图/出视频和审片 gate 消费。

---

## 3.1 `reveal_reaction_chain` 真相揭示 / 身份曝光反应链

**使用场景**：身份曝光、掉马、真相揭示、旧案证据公开、内鬼暴露、身世认亲。重点不是“把信息说完”，而是把证据、知情顺序和反应链拍清楚。

**拆法**：
- 证物/证据入画：血书、密信、信物、伤痕、系统提示、证人口供等先给可读视觉锚。
- 揭示句/动作：谁把真相说出或展示出来。
- 第一反应：最受冲击的人先反应，通常给 CU/ECU。
- 连锁反应：第二、第三个关键人物按知情顺序反应，不能随机切。
- 落幅/断点：停在不可逆选择、半露真相、证物被抢或称谓改变。

**额外必填字段**：`reveal_object`、`knowledge_order`、`reaction_beats`、`cut_point`

**提示词规则**：证据物要稳定，不让 AI 把信物/血书/面板文字画乱；可读文字仍优先走 overlay 或字幕，不烤进图。反应链用正反打和特写，不在同一镜里塞所有人的脸。`knowledge_order` 写清谁已知、谁刚知道、谁假装不知道，避免下游剪辑把反应顺序切乱。

---

## 3.2 `public_confrontation` 公开对质 / 审讯 / 谈判智斗

**使用场景**：公堂、朝堂、宴席发难、当众揭穿、审讯逼问、谈判交易、权谋智斗。它比普通反打多了“现场规则、证据阶梯、权力翻转、围观反应”。

**拆法**：
- 建立现场：裁决者、被质问者、证人/围观者的空间层级。
- 第一筹码：证据、口供、把柄、利益、时间或武力威胁。
- 反扑：被质问者反驳、改口、反咬或转移焦点。
- 权力翻转：证据升级、站队变化、裁决者表态或谈判底线暴露。
- 落幅：定罪、交易、逃脱、灭口、结盟或更大嫌疑。

**额外必填字段**：`stakes`、`evidence_ladder`、`power_shift`、`crowd_reaction_order`

**提示词规则**：人群不要人人清脸；主角、对手、裁决者和关键证人分层。证据阶梯要能被镜头看见：手中文书、桌上物证、证人出列、系统面板、令牌等。围观反应只点 1-3 个代表，不把镜头切成无意义群像。

---

## 4. `magic_burst` 法术爆发

**使用场景**：灵力爆发、符阵亮起、剑气冲出、雷落、火焰/光束/护盾炸开、斗法撞点。

**拆法**：
- 蓄力：颜色、形状、来源位置先锁。
- 释放：光效路径明确。
- 爆点/撞点：爆发帧或命中帧单独出图。
- 余波：尘、光点、衣摆、地面裂纹等二级动态。
- 落幅：爆后角色状态和场景状态。

**额外必填字段**：`charge_frame`、`release_frame`、`effect_asset`、`energy_path`、`collision_or_apex_frame`、`power_shift`、`vfx_asset_lock`、`intensity_curve`、`speed_curve`、`spatial_path`、`camera_path`、`screen_direction`(越轴锁·斗法对轰双方法术轴线)、`readability_beats`、`degrade_plan`、`keyframe_plan`、`post_cue_points`、`physics_guard`
> **战斗精修建议字段**同 `fight_exchange`（§1）：`combat_micro_expression`/`secondary_motion`/`apex_light`——爆发/撞点帧让表情·二级运动·apex 光效与命中关键帧同帧汇聚。

**提示词规则**：高频复现法术/阵法/剑气必须进 `出图/共享/图片/特效定妆`；不要每镜换颜色、形状和轨迹。爆发只允许照亮既有场景，不新增无关奇观。

---

## 5. `flight` 飞行

**使用场景**：御剑、腾空、坠落、空中掠过、飞檐走壁、云海穿行。

**拆法**：
- 起飞/入画：姿态和方向先锁。
- 巡航：人物姿态尽量固定。
- 机动：只做一个转向、俯冲或急停。
- 抵达/落地：落到明确接点。

**额外必填字段**：`pose_lock`、`background_motion`、`altitude_path`、`speed_curve`、`flight_path`、`altitude_curve`、`camera_path`、`parallax_layers`、`mount_or_cloud_lock`、`readability_beats`

**提示词规则**：飞行核心是“锁姿态，动背景”。人物脸、发型、衣服不要被速度拉变形；速度感来自云、山、雨、前景掠过和镜头跟随。

---

## 5.1 `mount_ride` 御兽 / 坐骑

**使用场景**：骑灵兽、骑马、骑狼、骑虎、骑龙、兽背奔跑或踏云载人。若坐骑本身飞行且重点是骑手和坐骑接触，用 `mount_ride`；若重点是大型载具飞舟外景，用 `vessel_flight`。

**拆法**：
- 建立坐骑：先交代坐骑体型、骑手位置、鞍具/缰绳和屏幕方向。
- 接触锁定：骑手臀部/大腿/脚蹬/手部与坐骑的接触点、遮挡顺序写清。
- 步态循环：只选一种主步态（奔跑/跃步/踏云），速度交给尘土、草木、披风、鬃毛和前景遮挡。
- 转向或抵达：转弯、越障、急停或落地作为落幅，不在同镜里连续变招。

**额外必填字段**：`mount_contact`、`gait_cycle`、`harness_lock`、`speed_curve`、`spatial_path`、`camera_path`、`screen_direction`、`parallax_layers`、`readability_beats`、`degrade_plan`

**提示词规则**：坐骑必须入 `BEAST/MOUNT` 资产库，锁体型、角/鳞/毛色、鞍具和标志纹样。骑手姿态尽量稳定，不要让人浮在兽背或腿穿进兽身。不稳时拆成坐骑特写、骑手握缰反应、侧向奔跑、停步/抵达。

---

## 5.2 `vehicle_ride` 马车 / 载具行进

**使用场景**：马车、兽车、车驾、轮式法器、押送车队、驾驶载具行驶。若载具离地飞行且重点是空中航行，用 `vessel_flight`。

**拆法**：
- 载具建立：锁车厢剪影、车轮数量/位置、牵引兽、车夫和缰绳。
- 轮转/蹄步：轮子旋转、马蹄/兽蹄节奏、尘土和路面反馈。
- 侧向跟行：车体外形不变，背景和前景遮挡后掠。
- 颠簸/急停/抵达：只做一个转弯、越障、急停或到达落幅。

**额外必填字段**：`vehicle_lock`、`wheel_rotation`、`harness_lock`、`speed_curve`、`spatial_path`、`camera_path`、`screen_direction`、`parallax_layers`、`readability_beats`、`degrade_plan`

**提示词规则**：`vehicle_lock` 锁车体，`wheel_rotation` 锁轮转，`harness_lock` 锁车辕/缰绳/牵引兽连接，三者不要混写。车内人物反应和车外高速行进优先拆镜；不稳时用轮子、马蹄、车夫手、车帘和车内反应镜拼出行驶。

---

## 5.3 `vessel_flight` 飞舟 / 御物飞行

**使用场景**：灵舟、云舟、飞梭、飞行法器、大型御物载人飞行。普通单人御剑/腾云仍用 `flight`。

**拆法**：
- 入画/起航：飞舟或法器剪影出现，阵纹亮起，航向先锁。
- 巡航：船体姿态稳定，云层、山河、前景遮挡制造速度。
- 机动：穿云、转向、俯冲、悬停只选一个动作。
- 抵达/显景：停到山门、秘境或战场前，交代目的地和比例。

**额外必填字段**：`vehicle_lock`、`flight_path`、`altitude_path`、`altitude_curve`、`speed_curve`、`spatial_path`、`camera_path`、`screen_direction`、`parallax_layers`、`readability_beats`、`degrade_plan`

**提示词规则**：飞舟/法器必须入 `VEHICLE/PROP` 资产库，锁剪影、船舷/桅帆/阵纹和拖光颜色。巡航时船体少动，速度交给云海、山河和前景遮挡；乘员清脸拆甲板反应镜，不在飞舟外景里硬看清多人表演。

---

## 5.4 `road_vehicle` 现代车辆 / 车流行进

**使用场景**：出租车、网约车、警车、救护车、摩托、电动车、公交、高架车流、公路追车、停车场急刹、城市道路行驶。古代马车、兽车、车驾仍用 `vehicle_ride`。

**拆法**：
- 建立车型：锁车型、车身颜色、车灯、轮组、车牌是否可见和驾驶位关系。
- 车道方向：锁 `lane_lock`、`screen_direction`、路口/高架/停车场的空间轴线。
- 交通运动：车体少变，速度交给轮转、路面线、护栏、街灯、车流和窗外视差。
- 结果落幅：变道、刹车、转弯、擦肩、追近或抵达只选一个。

**额外必填字段**：`vehicle_lock`、`wheel_rotation`、`driver_control_lock`、`lane_lock`、`traffic_flow`、`speed_curve`、`spatial_path`、`camera_path`、`screen_direction`、`parallax_layers`、`readability_beats`、`degrade_plan`

**提示词规则**：长期复用车辆进 `VEHICLE` 资产库；车牌/金额/导航文字走 overlay 或后期模糊，不让 AI 烤字。追车不稳时拆轮胎、后视镜、驾驶员反应、窗外掠过、抵达停住。若核心是人物跑巷/追逃距离，用 `chase`；若核心是现代道路车辆，用 `road_vehicle`。

---

## 5.5 `stealth_stalk` 尾随 / 潜入 / 暗走廊

**使用场景**：尾随、跟踪、潜入、躲藏、窥视、门缝观察、停车场暗处、楼梯间、档案室、手电光扫过、目标消失。

**拆法**：
- 建立距离：谁在前后、隔几米、沿哪个方向移动。
- 遮挡逼近：门框、货架、车柱、窗帘、雨伞、玻璃反光只选 1-2 层主遮挡。
- 光影锁定：灯源、暗区、手电光范围、剪影边缘写清。
- 露出或躲开：只做一个 `reveal_or_hide_beat`，别在同镜里再追打。

**额外必填字段**：`screen_direction`、`distance_curve`、`occlusion_layers`、`light_shadow_lock`、`speed_curve`、`spatial_path`、`camera_path`、`parallax_layers`、`reveal_or_hide_beat`、`readability_beats`、`degrade_plan`

**提示词规则**：悬疑感来自可控的“看见一部分”，不是把画面写黑。若开始奔跑追逐，切 `chase`；若进入审讯/对质，切 `public_confrontation`；若证物露出，接 `evidence_search`。

---

## 5.6 `screen_insert` 手机 / 电脑 / 监控屏幕

**使用场景**：聊天记录、短信、来电、通话界面、定位、转账、付款码、邮件、电脑屏幕、监控回放、CCTV 时间码、录音波形、弹窗提示。

**拆法**：
- 设备建立：锁设备型号、屏幕方向、边框、持有关系和角度。
- 信息读取：`overlay_content` 给清晰文字/数字，屏幕平面稳定 0.6-1.2 秒。
- 反应承接：切到手部、眼神、角色反应或证据物。

**额外必填字段**：`device_lock`、`screen_content_ref`、`text_layer`、`overlay_content`、`reflection_policy`、`hand_pose_lock`、`readability_beats`

**提示词规则**：默认 `text_layer=overlay`。AI 只画设备底、反光、模糊 UI 或空白屏；聊天气泡、时间码、金额、账号、车牌、定位坐标等清晰信息由 n2d-compose 叠字。

---

## 5.7 `evidence_search` 搜证 / 物证发现

**使用场景**：翻抽屉、查档案、发现血迹/指纹/脚印/钥匙/票据/录音笔/照片、证物袋入画、照片墙/线索板、现场勘查。

**拆法**：
- 搜查路径：手从哪里进画、翻开什么、遮挡顺序如何。
- 物证露出：`reveal_frame` 给物证完全露出的半拍。
- 证据链承接：拍照、装袋、标号、角色反应或贴到线索板。

**额外必填字段**：`clue_object`、`search_path`、`reveal_frame`、`evidence_chain`、`hand_pose_lock`、`occlusion_order`、`contamination_guard`、`readability_beats`

**提示词规则**：物证进 `PROP/EVIDENCE` 资产库，锁血迹/指纹/票据/钥匙/证物袋编号和位置。不要一镜连续翻找、发现、追人、打斗；后两者拆到 `chase` / `fight_exchange`。

---

## 5.8 `tribulation_breakthrough` 渡劫 / 突破

**使用场景**：天劫、雷劫、破境、境界突破、突破光柱、护体光抵挡主雷。

**额外必填字段**：`tribulation_stage`、`lightning_path`、`impact_frame`、`shield_lock`、`breakthrough_light`、`vfx_asset_lock`、`intensity_curve`、`readability_beats`、`degrade_plan`

**提示词规则**：劫雷/护体光/突破光柱必须进 VFX 资产库。每个 Clip 只做一道主雷、一次抵挡或一次突破释放；雷击命中帧单独可读，突破后留白。完整拆法见 `仙侠场面分镜.md` 与 `玄幻穿越场面分镜.md`。

---

## 5.9 `meditation_cultivation` 打坐 / 静坐 / 冥想 / 闭关

**使用场景**：打坐、静坐、入定、冥想、吐纳、调息、闭关、周天运转、内视丹田、灵气入体、心魔压制。

**拆法**：
- 姿态建立：先锁坐姿、手印、蒲团、衣摆和重心，不让角色站起或换动作。
- 吐纳周天：呼吸节奏和灵气路径清楚，气海/丹田/经脉只走一条主路径。
- 内在响应：内视、心魔压制、灵气波纹、境界松动或环境共鸣只给一个结果。
- 收功留白：睁眼、气息平复、灵气沉静或下一镜钩子，结果明确。

**额外必填字段**：`posture_lock`、`breath_cycle`、`energy_flow`、`aura_vfx_lock`、`inner_state_beat`、`environment_stillness`、`micro_motion`、`readability_beats`、`degrade_plan`、`keyframe_plan`、`post_cue_points`、`physics_guard`

**提示词规则**：静修的高级感来自“少动但有层次”：人物只给呼吸、指节、发丝、衣摆、灵气粒子和环境响应；不要把打坐写成乱舞法术。内视画面可用叠层/光雾/短插入镜，失败时退到静态坐姿 + 后期 aura overlay。

---

## 5.10 `alchemy_forging` 炼丹 / 炼器

**使用场景**：丹炉火候、材料入炉、开炉成丹、炼器、铸剑、法器淬炼、失败炸炉。

**额外必填字段**：`furnace_or_forge_lock`、`material_sequence`、`process_stage_ladder`、`flame_control`、`heat_curve`、`material_state_ladder`、`tool_hand_pose`、`product_lock`、`reveal_frame`、`vfx_asset_lock`、`success_or_failure_beat`、`failure_or_success_safety`、`readability_beats`、`degrade_plan`、`keyframe_plan`、`post_cue_points`、`physics_guard`

**提示词规则**：丹炉/锻炉、火焰、主材料、成品入库。材料顺序、阶段梯、火候曲线、材料状态变化和成品形态分开写；成品或失败形态给 `reveal_frame` 可读半拍，不让模型临场发明。炼丹按投料→升温→凝丹→开炉；炼器按熔炼→锻打→淬火→出器，别一镜跳完全部工序。

---

## 5.11 `array_ritual` 阵法 / 结界 / 封印

**使用场景**：阵法起势、传送阵、护宗大阵、结界、封印、祭坛仪式、破阵、阵眼点亮。

**额外必填字段**：`array_geometry_lock`、`activation_steps`、`symbol_layer_policy`、`caster_positions`、`energy_flow`、`purpose_lock`、`vfx_asset_lock`、`readability_beats`

**提示词规则**：阵图先做俯视锚或 overlay 几何层。视频只按 `activation_steps` 点亮阵眼/光轨，不自由改几何；多人施法写 `caster_positions`，防阵内站位漂。

---

## 5.12 `soul_manifestation` 神魂 / 元神 / 夺舍

**使用场景**：神识探查、元神出窍、魂魄显形、识海、夺舍、搜魂、神魂攻击。

**额外必填字段**：`body_soul_identity_lock`、`soul_form_lock`、`opacity_curve`、`emergence_path`、`host_body_lock`、`conflict_or_probe_target`、`readability_beats`、`degrade_plan`

**提示词规则**：元神必须由肉身定妆派生，同脸同发型。视频只做透明度、光边、气流和上升路径；夺舍/相争/神魂命中给成败帧，别一镜自由魂战。

---

## 5.13 `realm_portal` 穿越 / 传送 / 秘境入口

**使用场景**：现代人穿到异界、魂穿/身穿、时空裂缝、传送门、传送阵、秘境入口、遗迹门。

**额外必填字段**：`portal_lock`、`source_world_anchor`、`destination_anchor`、`entry_exit_path`、`body_continuity_lock`、`transition_vfx`、`readability_beats`、`degrade_plan`

**提示词规则**：入口、源世界、目的地三者都要锁。穿越前后不要一镜换脸换衣；进入动作和落地显景拆开，换装/系统绑定放后续镜头。

---

## 5.14 `contract_summon` 契约召唤 / 魔兽认主

**使用场景**：灵兽契约、魔兽契约、血契、认主、召唤阵、法灵显形、契约印记落定。

**额外必填字段**：`summon_entity_lock`、`contract_mark`、`ritual_anchor`、`summon_path`、`scale_relationship`、`ownership_transition`、`readability_beats`、`degrade_plan`

**提示词规则**：召唤兽/法灵必须入库，锁剪影、颜色、尺度和标志。契约成立后若要骑乘，下一镜切 `mount_ride`；不要在契约镜里直接召唤并骑跑。

---

## 5.15 `talent_test` 测灵 / 天赋测试 / 血脉觉醒

**使用场景**：测灵石、灵根测试、资质测试、学院入学测试、血脉觉醒、武魂觉醒、天赋等级暴露。

**额外必填字段**：`test_artifact_lock`、`measurement_rule`、`result_stage`、`overlay_policy`、`crowd_reaction_chain`、`vfx_asset_lock`、`readability_beats`

**提示词规则**：天赋名、等级、数值、系统提示走 `overlay_policy`，AI 只画测试道具、光柱和反应底图。结果出来后可接 `reveal_reaction_chain` / `public_confrontation`。

---

## 6. `dual_cultivation` 双修 / 合修 / 疗伤共修

**使用场景**：双修、合修、共修、双人疗伤、掌心相抵、背靠背运功、阴阳调和、灵力交汇、共同破境。只用于成年人、双方明确自愿、非露骨的修炼/疗伤/能量循环表达。

**拆法**：
- 边界建立：成年人、双方自愿、非露骨边界先写清，避免裸露、性行为动作、未成年人语义。
- 双人姿态：掌心相抵、背靠背、并坐手印或安全距离，锁左右/前后站位和接触点。
- 呼吸同步：两人吐纳节奏同频，身体动作保持克制。
- 能量循环：灵力路径、光色、疗伤/共鸣/破境目的清楚，奇观交给 aura/VFX。
- 结果留白：疗伤见效、关系信任、气息平复或光雾淡出，不做露骨表现。

**额外必填字段**：`adult_consent_lock`、`non_explicit_boundary`、`paired_posture_lock`、`distance_boundary`、`contact_points`、`energy_circulation`、`breath_sync`、`aura_vfx_lock`、`relationship_state`、`readability_beats`、`degrade_plan`、`keyframe_plan`、`post_cue_points`、`physics_guard`

**提示词规则**：双修不是情色模板；它是双人能量循环/疗伤/关系信任镜。必须写“adult characters only / consensual / non-explicit / no nudity / no sexual action”。接触不稳或边界风险上升时，立即改手部特写、背影、光雾、反应镜或淡出。

---

## 6.1 `kiss_or_near_kiss` 接吻 / 亲吻 / 近吻

**使用场景**：接吻、亲吻、近吻、额头吻、脸颊吻、吻别、差点吻上、唇边停住。用于关系转折、告白兑现、诀别、重逢、救赎等非露骨亲密动作。

**拆法**：
- 边界建立：年龄语境、双方自愿、非露骨边界先写清。
- 靠近路径：谁先靠近、从哪个方向、停在什么距离，不突然贴脸。
- 脸部角度：脸的朝向、视线、闭眼/睁眼、发丝遮挡和身份锚不漂。
- 接触/近接触帧：闭口轻吻、额头吻、脸颊吻或近吻停顿给可读半拍。
- 分开/留白：停到表情、手部、错肩或拥抱承接，服务下一镜。

**额外必填字段**：`age_context_lock`、`consent_lock`、`non_explicit_boundary`、`relationship_state`、`approach_path`、`face_angle_lock`、`contact_or_near_contact_frame`、`hand_position_lock`、`body_overlap_limit`、`breath_pause`、`micro_expression_beats`、`readability_beats`、`degrade_plan`、`keyframe_plan`、`post_cue_points`、`physics_guard`

**提示词规则**：接吻镜优先 closed-mouth gentle kiss / forehead kiss / cheek kiss / near-kiss pause。禁止露骨舌吻、脱衣、性行为暗示和未成年人性化。两张脸接近时必须用首尾帧或接触帧约束；脸融合、手穿模、边界不清时改近吻停顿、额头/脸颊吻、手部/发丝/反应镜或错肩。

---

## 6.2 `intimate_interaction` 亲密互动

**使用场景**：搀扶、牵手、抚脸、扶肩、疗伤、暧昧靠近、近距离低幅度互动。这里的“亲密”指人物距离与接触调度，不等于情色；若有拥抱、抓腕、推开、拉扯，改用 `hug_or_pull`。

**拆法**：
- 距离变化：远→近或近→分开，只选一个。
- 接触点：手腕、肩、背、额头、衣袖等必须写清。
- 视线/呼吸：用眼神、停顿、身体微动表现关系。
- 落幅：停在接触点或表情反应。

**额外必填字段**：`consent_boundary`、`contact_points`、`distance_boundary`、`body_overlap_limit`、`occlusion_order`、`body_part_ownership`、`relationship_state`、`readability_beats`、`degrade_plan`

**提示词规则**：多角色接触最容易崩手和肢体重叠；优先用半身/手部特写、过肩、遮挡，而不是全身复杂拥抱。合规边界不清时回避情色化描述。直接生成近距离接触时，manifest 必须写 `contact_points`、`occlusion_order`、`body_part_ownership`。

---

## 7. `hug_or_pull` 拥抱 / 拉扯 / 抓腕

**使用场景**：拥抱、抱住、抓腕、拉袖、推开、拉扯、拽住、扶起时带明显力量方向的接触互动。它比普通亲密互动更容易崩手、穿模、肢体重叠，所以单独模板化。

**拆法**：
- 接触前：先锁两人左右、距离、手的位置。
- 接触点：抓腕/抱肩/拉袖/推胸口等只选一个主接触点。
- 力量方向：谁拉谁、往画左/画右/前景/后景移动，必须写清。
- 释放/停住：落到松手、被拉近、被推开、半步踉跄等可剪点。

**额外必填字段**：`consent_boundary`、`contact_points`、`force_direction`、`body_overlap_limit`、`occlusion_order`、`body_part_ownership`、`hold_or_release_frame`、`release_frame`、`relationship_state`、`readability_beats`、`degrade_plan`

**提示词规则**：优先用首尾帧或手部特写承接，不要让 AI 在一个 Clip 里完成“冲上去、抓住、拥抱、挣扎、推开”全链路。拥抱先锁谁主动、手臂绕到哪里、谁在前景、肩胸是否重叠、何时松开；抓腕/拉扯必须写力量方向和释放帧。全身拥抱不稳时，拆成靠近反应、手臂/肩背特写、拥抱半身、分开反应。没有 ready Motion Control manifest 时，不直接生成全身拥抱/拉扯，写 `status=degrade_only` 并按拆镜执行。

---

## 7.1 `relationship_turn` 关系转折 / 告白 / 决裂 / 和解

**使用场景**：告白、心动兑现、决裂、误会爆发、和解、救赎、互相救场、吃醋、护短。若核心是明确肢体接触（抓腕、拥抱、拉扯），仍用 `hug_or_pull` / `intimate_interaction`；本模板锁“关系状态变化”和表演节拍。

**拆法**：
- 关系前态：两人本场前的距离、称谓、视线和阵营。
- 触发动作：承认、停步、递物、挡刀、收回信物、改称谓、转身离开。
- 潜台词反应：嘴上说的与动作表达的矛盾。
- 新关系态：更近、更远、暂时结盟、正式决裂、重新信任或留下误会。
- 落幅：停在手、眼神、背影、门关上、称谓变化或救场后的沉默。

**额外必填字段**：`relationship_state_before`、`turning_action`、`subtext`、`relationship_state_after`

**提示词规则**：关系转折镜头吃脸和微表情，优先 MCU/CU 正反打，不硬塞双人大头同框。动作要小而明确，避免“一镜完成争吵、拥抱、和解、亲吻”这类多段复杂表演。大表情跨度按 `continuity.expression_span=大` 规划首尾帧或 MCU 保真实现。

---

## 8. `multi_character_same_frame` 多人同框

**使用场景**：两名及以上具名角色必须同时清楚出现在同一画面：双人同框、三人同框、主角与关键配角同场对峙、同桌/同车/同殿但需要看清主角脸。2-3 张清晰脸只是相对省钱/稳定的构图区，不是免登记区；>2 或多人近景/遮挡/强交互应优先拆反打、景别分层或分区构建。

**拆法**：
- 角色槽位：每个具名角色固定画左/画右/中央/前景/后景。
- 脸优先级：只允许 1-2 张脸作为清晰焦点，其余侧脸/背影/虚焦。
- 遮挡规则：谁可以被前景遮挡，谁不能被挡脸。
- 焦点切换：若要 A→B 反应，优先拆正反打，不在同框里乱转。

**额外必填字段**：`character_slots`、`face_priority`、`overlap_rules`

**提示词规则**：多人同框的目标是“槽位稳定”，不是让每个人都完整表演。任一清晰同框 ≥2 具名角色都必须写 `character_slots` + 执行策略（`native_subject_slots` / `regional_construct_required` / `split_composite_required` / `shot_reverse_shot`）。角色身份从 `identity_registry` 继承；如果出现脸互换、左右互换、服装串色，必须拆成两人 OTS / 反打 / 手部插入。

---

## 9. `ensemble_blocking` 群像站位

**使用场景**：三人以上群戏、群臣/门徒/追兵围堵、宴席、审讯、围观、队伍站位、宗门大殿群像。重点是主次层级和空间关系，不要求人人清脸。

**拆法**：
- 建立层级：主角、对手、群体分别在前/中/后景。
- 站位图：画左/画右/中央/前景/背景写清。
- 焦点层级：一次 Clip 只突出一个说话者或动作点。
- 人群简化：背景人用剪影/虚焦/背影/局部，别要求人人清脸。

**额外必填字段**：`screen_positions`、`focus_hierarchy`、`crowd_simplification`

**提示词规则**：三人以上不追求每张脸都精确；主要人物用定妆参考，次要人物按视觉层级处理为虚焦、剪影或局部。若主角与两名以上角色都要清脸互动，优先拆正反打。

---

## 10. `multi_person_blocking` 多人站位（legacy）

旧项目已写 `multi_person_blocking` 的 Clip 仍可通过 gate；新项目优先按镜头目的拆成：

- 2-3 名具名角色同框：`multi_character_same_frame`
- 3 人以上群戏 / 队列 / 围堵 / 群像：`ensemble_blocking`

**额外必填字段（legacy）**：`screen_positions`、`speaker_focus`、`crowd_simplification`

---

## 12. `system_panel` 系统面板（母题·首样板）

**使用场景**：穿越/系统流的系统面板出现、主角升级、系统刷新/进化、签到、抽奖——任何"虚拟面板/光幕浮现 + 数值刷新 + 主角反应"的复现桥段。面板家族母题（system_panel/level_up/system_refresh/signin/gacha）统一套本模板。是 §0.2 通用 motif 形态的首个落地实例。

**拆法（beats）**：
- 面板浮现：发光光幕/符纹底框从主角视野浮现（位置/透明度先锁）。
- 数值刷新：面板内容跳变（升级镜在此给等级/属性跳变的特写时机——但数值本身是 overlay 层，AI 不画）。
- 主角反应：惊喜/狂喜/凝重——爽点释放点，可标 `钩子=climax`。
- 落幅：面板稳定悬浮 或 收起，主角进入下一动作。

**camera_rule**：面板正对屏幕、不随镜头透视畸变、不位移变形；机位可给主观镜（主角视角看面板）或过肩（面板 + 主角侧脸同框）。面板悬浮位置跨镜稳定。

**continuity_must**：① 面板跨集锁色锁形（继承 `VFX_系统面板` 的 color_target/frame_shape）；② 等级/档位只增不减（progression 单调）；③ **面板内部文字区留空**给 overlay；④ 面板进化只能往前（v1_素纹→v2_流光→v3_符纹，lifecycle 不回退）。

**negative（关键·混合渲染）**：面板内**不出现任何可读文字 / 数字 / 汉字 / 等级数值 / 属性条刻度 / 进度条数字**；不要 AI 自行编造面板内容；只画发光光幕底框 + 符纹边框 + 内部空白发光区。

**额外必填字段**：`motif_id`（=`MOTIF_系统面板`）、`vfx_asset`（=`VFX_系统面板`）、`text_layer`（=`overlay`）、`growth_ref`（progression 当前 at_clip）、`panel_tier`（当前面板档位/form）。

**提示词规则**：系统面板是高频复现奇观，**必须进 `出图/共享/图片/特效定妆`（`VFX_系统面板`）锁色锁形**，不要每镜换颜色/形状/符纹。升级/进化只换 `panel_tier`（form 档位）+ overlay 数值，不重画风格。台词走系统音腔（机械简短，`narrator_role=系统`）。完整链路与 registry schema 见 `题材母题框架.md`。

---

## 11. 选择速查

| 镜头意图 | 首选模板 | 备注 |
|---|---|---|
| 两人拳脚/刀剑/掌法 | `fight_exchange` | 同步读 `打斗分镜.md` |
| 逃跑/追杀/移动压力 | `chase` | 飞行追逐同时读 `仙侠场面分镜.md` |
| 逼问/谈判/眼神压迫 | `dialogue_shot_reverse` | 只在需要轴线反打时套 |
| 身份曝光/掉马/证据揭穿 | `reveal_reaction_chain` | 锁证据物、知情顺序和反应链 |
| 公堂/朝堂/当众对质/审讯/智斗 | `public_confrontation` | 锁赌注、证据阶梯、权力翻转和围观反应 |
| 符阵/灵力/光束/雷落 | `magic_burst` | 高频特效入库 |
| 御剑/腾空/坠落 | `flight` | 锁姿态动背景 |
| 御兽/骑坐骑/兽背奔跑 | `mount_ride` | 锁骑手接触点、步态循环和鞍具/缰绳 |
| 马车/车驾/轮式载具行进 | `vehicle_ride` | 锁车体、轮转、牵引连接和屏幕方向 |
| 飞舟/灵舟/大型御物飞行 | `vessel_flight` | 锁载具剪影、航线、高度曲线和视差层 |
| 现代汽车/高架车流/追车急刹 | `road_vehicle` | 锁车体、轮转、驾驶控制、车道和车流 |
| 尾随/潜入/门缝窥视/暗走廊 | `stealth_stalk` | 锁距离曲线、遮挡层、光影和露出/躲开拍 |
| 手机/电脑/监控屏幕/聊天记录 | `screen_insert` | 文字/数字走 overlay，锁设备和屏幕角度 |
| 搜证/物证发现/证物袋入画 | `evidence_search` | 锁物证、搜查路径、露出帧和证据链 |
| 渡劫/天劫/破境/突破光柱 | `tribulation_breakthrough` | 锁劫雷、命中帧、护体光和突破光柱 |
| 打坐/静坐/冥想/闭关/吐纳 | `meditation_cultivation` | 锁坐姿、呼吸周天、灵气路径和内在结果 |
| 炼丹/炼器/开炉/铸剑 | `alchemy_forging` | 锁炉体、阶段流程、火候曲线、材料状态和成品揭示 |
| 阵法/结界/封印/祭坛仪式 | `array_ritual` | 锁阵图几何、点亮顺序和用途结果 |
| 神魂/元神出窍/夺舍/神识探查 | `soul_manifestation` | 元神由肉身派生，锁透明度和显形路径 |
| 穿越/传送门/秘境入口 | `realm_portal` | 锁入口、源世界、目的地和人物连续性 |
| 灵兽契约/召唤/认主 | `contract_summon` | 锁召唤物、契约印记、尺度和归属关系 |
| 测灵/天赋测试/血脉觉醒 | `talent_test` | 锁测试道具、规则、结果阶段，文字走 overlay |
| 双修/合修/双人疗伤/掌心相抵 | `dual_cultivation` | 成年自愿、非露骨边界，锁双人姿态、接触点和能量循环 |
| 接吻/亲吻/近吻/额头吻/脸颊吻 | `kiss_or_near_kiss` | 锁年龄语境、同意边界、脸部角度、接触/近接触帧和手部位置 |
| 搀扶/牵手/疗伤 | `intimate_interaction` | 明确接触点和肢体边界 |
| 拥抱/抓腕/推开/拉扯 | `hug_or_pull` | 明确接触点、力量方向、释放帧 |
| 告白/决裂/和解/互相救场 | `relationship_turn` | 锁关系前态、转折动作、潜台词和后态 |
| 两到三名具名角色同框 | `multi_character_same_frame` | 固定角色槽位和脸优先级 |
| 三人以上群戏/队列/围堵 | `ensemble_blocking` | 主次分层，背景简化 |
| 旧项目多人站位 | `multi_person_blocking` | legacy 兼容；新项目少用 |
| 系统面板/属性升级/签到抽奖 | `system_panel` | 母题模板（§0.2/§12）：数值走 overlay，面板入库锁色锁形 |

**收尾自查**：如果一句 prompt 里同时出现三类以上复杂动作，先拆 Clip；如果需要解释“AI 应该自己理解怎么打/怎么追/怎么站”，说明模板契约还没写够。
