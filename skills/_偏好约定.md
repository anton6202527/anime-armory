# 偏好约定（所有自定义 skill 共用 · 通用机制）

> 这是**共享代码侧**的通用约定，不含任何具体用户的选择。
> 用户的实际选择是**私有的**，落在用户自己的空间，绝不写进 skill 源码。
> 任何 skill 涉及「让用户选」的地方，都按本约定读写，不要把某个平台/后端写死成唯一路径。

## 三句话原则
1. **skill 通用**：不写死平台/后端/分辨率；这些是「选择点」，由用户私有文件决定。
2. **选择私有**：用户的选择存在用户自己的空间（见下），不进 git、不进共享 skill。
3. **持续记录 · 最新为准**：选择点首次遇到 → 问一次 → 写入 `_设置.md` → 同项目之后**沉默沿用**；用户随时可改（说一声或直接改文件）。**用户在任何一句输入里显式点名某选择点的值（哪怕之前已设过、哪怕在流程中途、哪怕和当前任务无关）→ 立刻以新值覆盖 `_设置.md` 对应行 + 追加一条 `## 记录`，最新选择永远优先于旧值 / 全局默认 / 缺省。** 听到即落档，不要"等下次用到该选择点再写"。

## AI 代理交互约定 (Interactive Flow)

**原则：把枯燥的脚本命令藏在背后，让用户只做决策。**

当一个工作流包含**「机器负责结构计算（如拆分 Clip），紧接着需要大模型负责语义理解（如写 Prompt）」**的组合操作时，**绝对不要**把两段割裂的 CLI 脚本硬塞给用户让他们自己去敲。

- **正确的做法**：在第一个脚本跑完后，建立明确的**【AI 代理交互节点】**。
- **用户体验**：AI 代理应主动弹出高亮提示，或者用自然语言询问：“已完成基础切分。是否需要我启动 XXX 引擎为你自动补全提示词？”
- **后台执行**：用户回答“是”之后，AI 代理在后台默默**跑脚本 → 读取提示词 → 调用自身的 LLM 生成 JSON 结果 → 注入项目**，一气呵成。使用户感受到的是一个连贯、智能的“创作助理”，而不是一本枯燥的代码说明书。

## 两层私有存储

| 层 | 路径 | 谁写 | 作用 |
|---|---|---|---|
| **作品级** | `<作品根>/_设置.md` | skill 自动读写，用户可手改 | 本作品/曲/小说的实际选择（**权威**，覆盖全局） |
| **全局默认** | 私有全局默认 `创作偏好-默认.md`（仓库根或 `.agents/` / `.codex/` 等用户空间） | 用户维护 | 跨项目的个人默认（如「生图后端 / 视频后端 / 720p / CosyVoice / Suno」），开新项目时**预填** `_设置.md` |

- `<作品根>`：`制漫剧/<剧名>/`、`制MV/<曲名>/`、`写歌/<曲名>/`、`写小说/<项目>/` 等，与 `_进度.md` 同级。
- 全局默认使用工具中立路径：优先读仓库根 `创作偏好-默认.md`，再读 `.agents/创作偏好-默认.md`、`.codex/创作偏好-默认.md`；旧项目的 `.claude/创作偏好-默认.md` 仅作兼容读取，不作为新的共享入口。skill 文本引用「全局默认偏好文件」即指这些私有候选文件。

## 解析顺序（每次要用某选择点时）
0. **（最高优先）用户本轮输入显式指定了该选择点** → 立刻按新值覆盖 `_设置.md` + 记一行，**用新值**，跳过下面的读取。这条对**所有**选择点恒成立，且不限于"正要用到"时——只要用户一句话点名某选择，当场落档（见三句话原则 #3）。
1. 读 `<作品根>/_设置.md` 里该选择点 → 有就**直接用，不问**。
2. 没有 → 读全局默认 `创作偏好-默认.md` → 有就用其值**预填**，并**首次告知一句**（"默认 X，要改说一声"），随即写进 `_设置.md`。
3. 全局也没有 → **问用户一次**，写进 `_设置.md`。
4. 之后同项目同选择点：**沉默沿用**，不再问。
5. 无作品根的独立调用（如 novel-title / song-lyrics 单用）：能定位项目就读其 `_设置.md`，否则用全局默认 / 现问，不强行落文件。

> 例外：**合规/不可逆/花钱多**的点（换脸源脸合法性、真人嗓授权、版权来源、删除回流）即便记录过也要确认——安全 > 省事。

## `_设置.md` 格式（人类可读 · 可手改）
```markdown
# _设置 · <作品名>   （你的私有选择；skill 自动读写，也可手改。改主意随时说）

## 选择
- <选择点>: <值>        # <可选值>
...

## 记录
- 2026-06-03 初始化（继承全局默认）
- 2026-06-03 生视频AI 即梦→可灵（用户指定）
```

## 选择点目录（各家族暴露给用户的选择 + 缺省回退）

> 缺省值仅作**全局默认的初始建议**；真正生效的是 `_设置.md` 里的值。

### 制漫剧 novel2drama / n2d-*
- `制作模式`: 配音先行 \| 先出视频后配音 \| 原生音画　（默认 **配音先行·推荐**：真实配音时长驱动镜头/Clip，音画准、返工少。`先出视频后配音`=**快速demo·不推荐**：镜头时长只能按台词文本估算锁死，后期补真实配音对不上 → 音画不同步/语速怪/可能重切重出视频；仅纯视觉demo/比稿用，**或配音素材/音色/凭证一时没就绪时先把画面推起来**，过审/补齐后仍要回头补真音重定时。`原生音画`=**native AV·按剧选**：Seedance 2.0/Veo 3/Sora 类后端对说话镜一次出同步音画（台词+口型+环境声），绕过配音先行链路与对口型，规避代差与占位返工，代价是少了逐句音色控制；合规仍需 AI 标识、仿真人音色需授权。三种流程图 + 完整理由 + 闸门放行方式见 **novel2drama SKILL「制作模式」节**——选 `先出视频后配音` 时各阶段入口必复述其警告。**⚠️ 例外——首跑必给菜单**：此点是影响全程的出片顺序决策，**不**走"全局默认静默预填"那条；**新作品首次拆集时必须把三模式 + 一句话原因念给用户选一次**（即便全局默认是配音先行），用户选后落 `_设置.md`、之后沉默沿用、随时可改。菜单原话见 novel2drama SKILL「制作模式 · 首跑选择」段）
- `基础视觉风格`: 写实电影感 \| 国漫写实 \| 二次元赛璐璐 \| 水墨国风 \| 厚涂幻想 \| 赛博霓虹 \| Q版轻喜 \| 自定义　（默认 **写实电影感**，但它只是预选，不是 skill 铁律。此点决定 `设定库/global_style.md`、`storyboard.json.style_contract`、出图/出视频总览「本集基础视觉风格契约」和逐镜 prompt 的画风词/负面词。**新作品首次写 global_style 前必须给菜单**：若 `_设置.md` 已有或用户本轮明说则直接用；若只有全局默认，把默认标为预选但仍展示菜单；选后写 `_设置.md`，同项目沉默沿用。旧项目里的 `cinematic_contract` /「本集真实电影感契约」可兼容，但下次改故事板/总览时迁到 `style_contract` /「本集基础视觉风格契约」。菜单与派生规则见 `novel2drama/references/visual_styles.md`）
- `单集时长`: 前长后短 \| 均衡 \| 快节奏 \| 长集 \| 自定义（均为**时长区间·验收窗口**）　（默认 前长后短：第1集 120–180s / 其余集 60–120s。**只锚①粗胚字数 band，不锚最终边界**——边界靠爽点/钩子等节拍重切、真实时长靠配音实测落进区间；三层模型与各预设区间见 n2d-script 第 1 步）
- `生视频AI`: 即梦 \| 可灵 \| Seedance \| Veo　（默认 即梦）
- `视频模型路由`: 自动按镜头路由 \| 固定生视频AI　（默认 **自动按镜头路由**：`生视频AI` 只作为普通镜/兜底后端，不再固定每个 Clip。打斗/追逐/对话反打/飞行/空镜/法术爆发/亲密互动/多人站位由 `n2d-model-router` 按镜头类型、专项模板、身份注册层、原生音画策略和时长上限选择 primary/fallback；`固定生视频AI` 仅用于账号/预算/交付约束明确要求单后端时，仍必须写每 Clip 路由字段和 fallback/degrade plan）
- `生图AI`: Codex \| OpenAI \| Dreamina/即梦官方 CLI \| Seedream \| 可灵主体库 \| Nano Banana \| Sora Cameo \| 自定义官方后端　（默认 **Codex**。生产闸门只阻断两类：① 同一作品/同一集混用多个生图后端；② `同视频AI` 含糊口径、第三方逆向或未授权出图路径。官方 Dreamina/即梦 CLI、官方 Seedream API、官方 OpenAI/Codex、可灵主体库、Nano Banana、Sora Cameo 等可作为选择点，但必须全集统一一个后端并能落 PNG；所选后端不可用时停下报告，不偷偷换后端兜底。旧项目若写 `生图AI: 同视频AI`，先改成显式后端名。）
- `配音后端`: CosyVoice \| GPT-SoVITS \| MiniMax \| 火山 \| say占位　（默认 CosyVoice，缺凭证回退 say）
- `出视频规格`: 预算充足 \| 预算一般 \| 预算不够　（默认 **预算一般**·对齐既有 720p 默认。三档各预设*分辨率·帧率·每Clip跑几条挑稳·平台质量档*：充足=1080p/30fps/关键镜2-3条·普通镜2条/高质量档；一般=720p/24-30fps/关键镜2条·普通镜1条/标准档；不够=720p/24fps/全1条/省积分档。**调生视频 AI 前每次告知当前档**·首次问一次记档之后沉默沿用可随时改；`视频分辨率`等单项可单独覆盖。详见 n2d-video「出视频规格」三档表）
- `视频分辨率`: 720p \| 1080p　（默认 720p；通常由 `出视频规格` 档预设，亦可单独覆盖）
- `画幅`: 9:16 \| 16:9　（默认 9:16 竖屏短剧）
- `对口型`: 关闭 \| 配音对齐 \| 后期pass　（默认 关闭；仅正面说话特写且预算允许时启用。`配音对齐` = `voice_conditioned_lipsync`，把已生成的克隆配音作为口型条件喂给支持音频参考口型的后端，成片音轨仍走配音轨；旧值 `平台原生` 兼容为 `配音对齐`，但新项目不再使用这个含糊命名，避免和 `原生音画` 混淆）
- `BGM来源`: 占位 \| 文件 \| Suno　（默认 占位）
- `视频原生音轨`: 丢弃 \| 低音量混入环境声 \| 保留原片音轨　（默认 **丢弃**：Veo/Seedance 出的 clip 可能自带原生音轨[环境音甚至台词]，n2d-compose 转码时剥掉，音频全由 配音+BGM+SFX 受控链路提供，避免双人声；`低音量混入环境声`=确认无原生台词时压低混作底；`保留原片音轨`=仅无配音/测试预览/明确要原片声时，有配音轨时必先提醒双人声风险。n2d-video 阶段不剥音轨，本点只由 n2d-compose 执行。详见 n2d-compose「clip 原生音频处理」）
- `接缝兜底`: 硬切 \| 微溶解 \| 报警　（默认按"有意硬切硬切、跳变溶解、缺空镜报警"：n2d-compose 按 `故事板.md` 每个接缝的 `转场类型` 决定接法——match cut/动作切/有尾帧接力的硬切=直接硬切；接点没焊住又非有意硬切的明显跳变=加 0.1–0.3s 微交叉溶解兜底；契约要空镜缓冲但缺料=停下报警，不默默糊过。爽点/反转的有意硬切**不加**溶解。详见 n2d-compose「按转场类型接 clip」）
- `水印`: 不打 \| AI合规标识 \| 品牌 \| AI合规+品牌　（默认 **AI合规标识**：含 AI 配音/生图/换脸的成片投放前应打可见 AI 标识+元数据，只加不去；`品牌`=片头/全程 logo 或账号文字防盗用，附子项 `水印文字`/`水印logo文件`/`水印位置`(tl·tr·bl·br·center，默认 br)/`水印透明度`(默认 0.8)/`水印大小`。本项只表达制作偏好，**正式放行以 `合规/compliance_manifest.json` 为准**：compose gate 要看到水印策略 `planned/ready`，review gate 要看到 `done` 且登记最终水印资产；纯本地预览可在合规包声明 `distribution_intent=internal_only` 才可跳过投放水印。详见 `n2d-compliance` + n2d-compose「加水印」节 + `shared-watermark` skill）
- `目标平台`: 抖音 \| 快手 \| B站 \| 小红书 \| 红果 \| YouTube \| TikTok \| ReelShort \| 跨平台 \| 未定　（默认 未定；影响合规包 `platform_review.targets`、画幅、字幕、AI 使用披露、隐式水印/平台自动标识策略。正式投放前不得停留在 未定；若跨平台，逐平台写 gate profile，不用一个泛化“全平台通过”代替）
- `发行地区`: 中国大陆 \| 港澳台 \| 北美 \| 东南亚 \| 全球 \| 自定义　（默认 中国大陆；影响 `localization`、字幕语言、平台审核、AI 标识法规口径。海外/全球投放必须在合规包里声明本地化状态和目标语言）
- `合规用途`: internal_only \| publish_candidate \| paid_distribution　（默认 publish_candidate；internal_only 只允许内部预览，gate 会跳过平台投放项但保留 WARN；publish_candidate/paid_distribution 必须完成版权、角色授权、声音克隆、AI 标识、水印、平台审核和出海本地化）
- `字幕语言`: 中文 \| 中英双语 \| 仅英文　（默认 中文·国内投放；海外投放 TikTok/ReelShort/YouTube/北美短剧 选 中英双语 或 仅英文。**翻译是投放选择，不写死在 skill**——选中英/仅英时 n2d-script 才产 `字幕_英文.srt`，否则只产 `字幕_中文.srt`；海外投放时合规包 `localization.target_languages` 必须覆盖实际字幕）
- `一致性增强`: 锚点+参考图 \| +LoRA　（默认 锚点+参考图，LoRA 不启用。**LoRA 训练参数（底模 `--base-model`、训练后端 `--provider`、`--steps/--rank/--lr` 等）刻意不做静默预填选择点**，属 n2d-lora 的算子级 CLI 旋钮，由操作者每次显式传参并落进验证报告/数据集 manifest 审计留痕——它们影响一致性与许可风险，不应被"全局默认沉默沿用"。要不要启用 LoRA 这一开关才是选择点，怎么训是算子决策。）
- `重抽预算策略`: 预算充足 \| 预算一般 \| 预算不足　（默认 预算充足：每张抽到出图自检三项全过为止，主要人物脸/妆造零漂移容忍；`预算一般`=保主要人物/关键镜过、配角差不多放行；`预算不足`=全程差不多就行最省 credit。详见 n2d-image「重抽预算铁律」三档表）
- `生成粒度`: 逐个 \| 小批 \| 按场景分批 \| 整集　（默认 **逐个**：每出 1 张图/1 个 Clip 就停下让用户审、可即时重抽/改 prompt 再继续；`小批`=每批 N 个一停，N 默认 5 可改；`按场景分批`=同场景连续镜一组一停；`整集`=旧行为一次过、最后统一报告。**任何档都先过共享定妆硬闸门**。⚠️**例外——每次都问**：本点属花钱/token 敏感点，**每次进出图/出视频前都要把四档菜单念给用户选一次，不沉默沿用**；`_设置.md` 里的值只作「默认建议/上次选择」预选，不替代询问。出图/出视频通用，菜单原话见 n2d-image / n2d-video「生成粒度 + 优先序」段）
- `生成优先序`: 关键镜优先 \| 分镜顺序 \| 先易后难　（默认 **关键镜优先**：🔑爽点/反转/钩子/封面候选先出，先定基调先锁主角脸；`分镜顺序`=Clip1→N 叙事序；`先易后难`=单人静态/空镜先、复杂打斗/多人镜后。共享定妆库永远排在最前，与本点无关）
- `脚本批次`: 逐集 \| 小批 \| 整批　（默认 **逐集**：n2d-script 量产剧本改编/分镜设计时每 1 集停下报**边界决策 + 物料清单**再继续；`小批`=每 N 集一停 N 默认 5 可改、附 5-10 集边界复核窗口；`整批`=指定范围一次过最后统一报告。文本不烧钱，**问一次后沉默沿用**，不像 `生成粒度` 每次弹菜单；只管"几集一停"不改三层拆集/P0→P6 边界铁律；第1集永远单独逐集。详见 n2d-script「脚本批次」段）
- `首切范围`: 部分先切 \| 全篇粗切　（默认 **部分先切·推荐**：首次拿到小说时**不**一次性粗切整本，只粗切前 N 集（默认 N=3，可改）→ 精修第1集验证节拍/画风/角色卡 → 满意后重跑 split 加大 `--limit` 续切。粗拆只是脚手架、边界后面还要按节拍重切，先切全本只是徒增一堆待返工的占位目录；先切一小段把第1集打磨对、把全局/角色卡立稳，再批量续切更省返工。`全篇粗切`=一次粗切整本（适合短篇、或边界已在 `_拆集复核.md` 定稿只差落词）。**仅首次拆集这一步用**，问一次写回 `_设置.md` 之后沉默沿用；不改三层拆集/P0→P6 边界铁律。详见 n2d-script 第 1 步）

### 制MV mv-*
- `MV用途`: 短视频Hook \| 歌曲Demo \| 正式MV草稿 \| 投放版 \| 自定义　（默认 **歌曲Demo**；影响目标节奏密度、clip 数量、字幕/水印/合规留痕要求。短视频Hook 偏 15-45s 高 hook 密度；投放版更重字幕、AI 使用披露和画幅平台适配）
- `MV视觉风格`: 电影叙事 \| 舞台演出 \| 国风写意 \| 赛博霓虹 \| 二次元 \| 抽象视觉器 \| 写实旅拍 \| 自定义　（默认 **电影叙事**，但只是预设，不是 skill 铁律。写进 `_设置.md` / `_meta.visual_style`，驱动 `视觉蓝图.md`、`mv-plan` prompt、出图/出视频风格继承；用户可自定义）
- `MV规划粒度`: 粗略 \| 标准 \| 精细　（默认 **标准**；影响 `mv-plan` clip 数量上限和 verse/chorus 切点密度。粗略=少 clip 省视频积分；精细=副歌更碎切、卡点更强但生成成本更高）
- `卡点策略`: 副歌强卡点 \| 全程强卡点 \| 叙事优先 \| 歌词叙事优先 \| 人工指定 \| 自定义　（默认 **副歌强卡点**；影响 `mv-plan` 如何从 beatgrid/downbeats 取切点。人工指定时以 `节拍/beatgrid.json.sections` 或手改 `分镜/clip_plan.json` 为准）
- `生视频AI`: 即梦 \| 可灵 \| Veo \| Seedance \| Runway \| Kling \| manual　（默认 即梦；`manual`=网页/外部工具/人工剪辑后用 `mv-video/scripts/video_jobs.py --register` 登记）
- `生图AI`: Codex \| OpenAI \| Dreamina/即梦官方 CLI \| Seedream \| 可灵主体库 \| Nano Banana \| Sora Cameo \| 自定义官方后端　（默认 **Codex**。MV 线同样只拦项目内混用、`同视频AI` 含糊口径和第三方逆向/web 自动化；官方/已登录多参考后端可选，但一支 MV 必须统一一个生图后端。所选后端不可用时停下报告，不偷偷换后端兜底）
- `MV一致性增强`: 共享定妆+锚点 \| 指定参考图 \| 后端主体库 \| +LoRA　（默认 **共享定妆+锚点**。进入共享定妆或分段组图前必须提示用户可升级：`指定参考图`=用户提供主角/服装/场景/道具参考图并登记到 `设定/reference_images/` 或 `出图/共享/图片/`；`后端主体库`=Seedream/可灵/Sora 等官方主体/角色 ID 能力；`+LoRA`=仅在用户已有或明确授权的 LoRA `.safetensors`、触发词、底模和许可说明时启用。MV 线不默认训练 LoRA，也不静默沿用旧模型；任何模式仍要求一支 MV 统一一个生图后端）
- `出视频规格`: 预算充足 \| 预算一般 \| 预算不够　（默认 **预算一般**。三档各预设*分辨率·帧率·每clip跑几版挑稳·平台质量档*：充足=1080p/30fps/关键镜2-3版·普通镜2版/高质量档；一般=720p/24-30fps/关键镜2版·普通镜1版/标准档；不够=720p/24fps/全1版/省积分档。MV 关键镜=副歌高光/爽点/人脸特写/卡点镜。**调生视频 AI 前每次告知当前档**·首次问一次记档之后沉默沿用可随时改；`视频分辨率`等单项可单独覆盖。与 n2d 同义，详见 mv-video「出视频规格」三档表）
- `视频分辨率`: 720p \| 1080p　（默认 720p；通常由 `出视频规格` 档预设，亦可单独覆盖）
- `合成画幅`: 16:9 \| 9:16　（默认 16:9 横屏 MV）
- `AI视觉使用披露`: AI-generated \| AI-assisted \| 未使用AI视觉　（默认 AI-generated；发布/交平台前跑 `mv-craft/scripts/ai_usage.py` 写 `合规/AI使用说明.md`。AI 生图/生视频主导通常按 AI-generated；人工拍摄/剪辑且 AI 只辅助修图/字幕可按 AI-assisted）
- `发行目标平台`: 抖音 \| B站 \| 小红书 \| YouTube \| Spotify \| 网易云 \| QQ音乐 \| 跨平台 \| 未定　（默认 未定/跨平台；影响合成画幅、字幕位置、AI 使用披露和水印策略）
- `重抽预算策略`: 预算充足 \| 预算一般 \| 预算不足　（默认 预算充足：每张抽到出图自检全过为止，主角脸/画风零漂移容忍；`预算一般`=保主角/副歌高光镜、其余差不多放行；`预算不足`=全程差不多最省 credit。与 n2d 同义，详见 mv-image「重抽预算铁律」三档表）

### 写歌 song-*
- `歌曲用途`: 短视频Hook \| 完整Demo \| 发行母带前草稿 \| MV源歌 \| 自定义　（默认 **完整Demo**；影响目标时长、hook 密度、作词结构、作曲 prompt、是否优先服务 MV 卡点）
- `目标时长`: 30s \| 45s \| 60s \| 90s \| 120s \| 180s \| 自定义　（默认随 `歌曲用途` 推导：短视频Hook≈45s、完整Demo/MV源歌≈120s、发行母带前草稿≈180s；写进 `_meta.target_duration_seconds`，song-review 用来做时长窗口检查）
- `语言`: 中文 \| 英文 \| 中英双语 \| 其他　（默认 中文；影响作词、作曲 prompt、字幕/MV交接）
- `BPM/速度`: 慢速 \| 中速 \| 快速 \| 自定义BPM　（默认 中速；也可写具体 BPM。影响歌词行长、作曲 prompt、MV beatgrid 预期）
- `调性`: 未定 \| C \| D \| E \| F \| G \| A \| B \| Am \| Dm \| Em \| 自定义　（默认 未定；给作曲 prompt/编曲沟通使用，不强制必须有）
- `作曲后端`: Suno \| Udio \| ACE-Step \| DiffRhythm \| manual　（默认 Suno 云；无云凭证回退 ACE-Step 本地；`manual`=外部工具/人工录制后用 `compose_song.py --register` 登记）
- `生成版数`: 1 \| 2 \| 4 \| 6 \| 8　（默认 **4**；音乐生成随机性高，正式定稿不默认一版定，用 `歌/takes_manifest.json` 记录每版）
- `挑版策略`: 最佳hook \| 最佳人声 \| 最贴蓝图 \| 最适合MV \| 人工挑版　（默认 人工挑版；影响 `compose_song.py --score` 的听感评价侧重）
- `翻唱后端`: RVC \| so-vits-svc　（默认 RVC）
- `演唱音色`: 自有嗓 \| 授权音色 \| 合成音色　（**合规点，需声明**，无默认）
- `AI音频使用披露`: AI-generated \| AI-assisted \| 未使用AI音频　（默认 AI-generated；发布/交平台/交 MV 前确认并写 `合规/AI使用说明.md`；若整首音频由 AI 音乐模型生成，通常按 AI-generated 留痕）
- `发行目标平台`: 抖音 \| B站 \| 小红书 \| YouTube \| Spotify \| 网易云 \| QQ音乐 \| 跨平台 \| 未定　（默认 未定/继承 `目标平台`；影响合规披露、响度/时长建议、MV交接要求）

### 写小说 novel-*
- `目标平台`: 起点 \| 番茄 \| 晋江 \| 抖音漫剧 \| 红果 \| 历史向 \| 跨平台　（无默认，立项时定）
- `权利来源`: 公版 \| 自有 \| 授权　（默认 公版；**合规点**，非公版需声明）
- `输出格式`: txt \| docx \| outline \| n2d　（默认 txt+docx；`n2d`=导出可直接喂 novel2drama 的入口骨架）
- `篇幅档`: short \| medium \| long \| 抖音漫剧　（按项目定）
- `小说生成模式`: 极速初稿 \| 稳妥初稿 \| 商业连载 \| 漫剧源书　（默认 **稳妥初稿**：Demo 过审后每章先出 `写作任务/第NN章.md`，按状态账本写章并分批 review。`极速初稿`=更少 gate、更快得到可读草稿；`商业连载`=开篇三章/留存/市场评分更重，每章状态增量更严；`漫剧源书`=正文服务后续 novel2drama，事件密度、可视化动作、n2d 导出优先。此点首次立项问一次或全局默认预填，同项目沉默沿用）
- `章节生成粒度`: 逐章 \| 小批 \| 全书草稿　（默认 **逐章**：每章一任务包一回扫；`小批`=每批 N 章，N 默认 3-5，可在任务中说明；`全书草稿`=仅极速草稿或短篇可用，仍需先有 Demo gate 和任务包。此点影响 `novel-craft/scripts/draft_packets.py` 的 `--chapter/--range/--next` 用法）
- `AI使用披露`: AI-generated \| AI-assisted \| 未使用AI文本　（发布/交平台前确认并写 `合规/AI使用说明.md`；若正文由 AI 直接生成，即便人工改过，通常按 AI-generated 留痕；若人写 AI 只润色/纠错/头脑风暴，记 AI-assisted。**发布合规点**，正式发布前复核目标平台最新规则）

### 拍广告 ad-*
> 作品根 `拍广告/<项目名>/`。**不拆集**：多时长/多比例/A·B 是交付件，不是集。机器取值域见 `skills/ad-craft/scripts/contract.py`。
- `广告类型`: TVC \| 信息流短视频 \| 品牌片 \| 产品demo \| 电商详情视频 \| 直播切片 \| 自定义　（默认 信息流短视频）
- `创意路线`: 功能卖点 \| 情感共鸣 \| 幽默 \| 悬念反转 \| 名人代言 \| 场景种草 \| 自定义　（默认 情感共鸣；**首次给菜单选一次**，影响全程调性）
- `基础视觉风格`: 写实电影感 \| CG质感 \| 定格动画 \| 二次元 \| 极简产品 \| 国风写意 \| 自定义　（默认 写实电影感；首次给菜单）
- `主片时长`: 6s \| 15s \| 30s \| 60s \| 自定义　（默认 30s）
- `交付比例`: 16:9 \| 9:16 \| 1:1 \| 多比例　（默认 16:9；主比例出图出视频，其它比例 ad-compose reframe）
- `cutdown版本`: 主片+15s+6s \| 主片+15s \| 仅主片 \| 自定义　（默认 主片+15s+6s）
- `生图AI`: Codex \| OpenAI \| Seedream \| 可灵主体库 \| Nano Banana \| Sora Cameo \| 自定义　（默认 Codex；放行官方多参考后端，**项目内不混用 + 禁逆向出图**）
- `一致性增强`: 共享定妆+锚点 \| 指定参考图 \| 后端主体库 \| +LoRA　（默认 共享定妆+锚点；产品/代言人优先后端主体库）
- `生视频AI`: 即梦 \| 可灵 \| Veo \| Seedance \| Runway \| Kling \| manual　（默认 即梦）
- `视频模型路由`: 自动按镜头路由 \| 固定生视频AI　（默认 自动按镜头路由；产品 hero 路由主体一致后端）
- `出视频规格`: 预算充足 \| 预算一般 \| 预算不够　（默认 预算一般）
- `视频分辨率`: 720p \| 1080p \| 4K　（默认 720p）
- `配音后端`: CosyVoice \| GPT-SoVITS \| MiniMax \| 火山 \| say占位 \| 自定义　（默认 CosyVoice；克隆真人嗓需 `VOICE_CLONE_AUTHORIZED=1`）
- `音乐来源`: 授权曲库 \| 原创定制 \| AI生成 \| 占位　（默认 占位；**合规点**，授权曲库/真人嗓需授权痕迹，每次确认）
- `品牌包装模板`: 标准片尾 \| 角标常驻 \| 片尾+角标 \| 无 \| 自定义　（默认 标准片尾；end card=logo+slogan+CTA）
- `字幕语言`: 中文 \| 中英双语 \| 仅英文 \| 无字幕　（默认 中文）
- `广告法地区`: 中国大陆 \| 海外 \| 关闭　（默认 中国大陆；**合规点·每次确认**。`ad-script/ad_law_check.py` 据此拦违禁词；`关闭` 仅非大陆投放且用户明确时）
- `交付规格`: 平台默认 \| 广电TVC \| 自定义　（默认 平台默认 -16 LUFS；广电TVC -23 LUFS。响度归一 + 安全框）
- `水印-AI合规标识`: 投放前必打 \| 始终打　（默认 投放前必打；**合规·不可逆点·每次确认**，调 `shared-watermark`，只加不去）
- `水印-品牌账号`: 打 \| 不打　（默认 不打）
- `生成粒度`: 逐个 \| 小批 \| 按场景分批 \| 整片 \| 自定义　（默认 逐个；出图/出视频每次问）
- `目标平台`: 抖音 \| 快手 \| 视频号 \| B站 \| 小红书 \| 朋友圈 \| OTT电视 \| 电梯分众 \| 电商 \| YouTube \| TikTok \| 跨平台 \| 未定　（默认 未定）
- `发行地区`: 中国大陆 \| 港澳台 \| 北美 \| 东南亚 \| 全球 \| 自定义　（默认 中国大陆）

### 换脸 shared-video-faceswap / shared-image-faceswap
- `换脸后端`: FaceFusion　（唯一；CoreML/Mac）
- `swap模型`: inswapper_128_fp16 \| hyperswap_256　（默认 inswapper_128_fp16）
- `增强模型`: gfpgan_1.4 \| codeformer　（默认 gfpgan_1.4）
- `源脸合法性`: 本人 \| 授权 \| 合成　（**合规点，每次确认**，见各 SKILL 第 0 步闸门）

## 新增选择点 / 新家族
在对应家族下加一行（选项 + 缺省回退），并在该 skill 的「偏好」段引用本文件即可。**不要**在 skill 正文把新选项写成唯一硬路径。
