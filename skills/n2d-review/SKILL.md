---
name: n2d-review
description: 漫剧质检 + 流程自审（n2d 的 QA 环节，不生产内容只审）。双模——模式①「作品质检」：对已产出的某集/整部漫剧做穿帮体检（角色崩脸/场景漂移/构图景别/字幕错位/音画不同步/双人声/节奏留存/口型），机检+人判，出严重度分级·定位到镜头的报告。模式②「流程自审」：联网拉当前市场基准，对照 n2d 各 skill + Q&A，产出"差距清单 + 该改哪个 skill 哪段"的优化建议，让整条流水线可重复自我体检。Use when asked to 漫剧质检, 审片, 查崩脸, 查穿帮, 字幕对账, 成片体检, 流程自审, 自我优化, n2d 还能优化啥, n2d-review. Triggers 漫剧质检, 审片, 崩脸, 穿帮, 字幕错位, 音画同步, 成片体检, 验收, 流程自审, 流程优化, 自我优化, n2d-review, QA.
---

# n2d-review — 漫剧质检 + 流程自审

不生产内容，只**审**。是 n2d 家族的 QA 环节。两个模式：

- **模式①「作品质检」**——审**某集/整部漫剧的产物**：扫穿帮 → 定位（镜头 + 时间码）→ 定级 → 给修法 → 出报告。出成片前 / 各阶段闸门跑。
- **模式②「流程自审」**——审**流水线本身**：联网拉市场基准，对照 n2d 各 skill + 累积 Q&A，产出"差距清单 + 建议改哪个 skill 哪段"。让"整套流程不断自我优化"成为一条可复跑的命令，而非靠人想起来。

> 行业把 **一致性 · 效率 · 可控性** 定为漫剧三大验收维（《AI漫剧工业化白皮书》）；商用"AI审片系统"自动识别约 12 类问题（角色断层/崩脸/构图错/糊/字幕错…）。本 skill 把这套体检在 n2d 产线里落成可跑流程。

---

## 输入 / 输出 / 读写边界

- **输入**：某集/整部的脚本、分镜、图包、视频、字幕、配音、dashboard/gate 数据、identity/score/review-ui 产物；流程自审还读 n2d skill 文档和模型矩阵。
- **输出**：机械检查、一致性 findings、gate findings、质检报告、流程自审报告；按需触发 score/review-ui/batch 的下游产物。
- **读写边界**：不生产图/视频/配音；作品质检只写 QA/报告/ findings，修复必须回对应 stage 重跑；流程自审默认 report-only，用户确认后才改 skill。
- **契约关系**：gate stage、finding kind、回流字段、官方生图后端白名单等从 `skills/n2d/_lib/n2d_contract.py` 读取；生产入口统一推荐 `n2d-dashboard gate`。

## Review 结果回灌矩阵

作品质检不是一次性报告。每次 review/score 通过但留下 warn 或 insufficient_data，都要按根因回灌到对应 skill 的前置证据要求里，避免下一集重复同类问题：

| review 暴露的问题 | 加强位置 | 固化要求 |
|---|---|---|
| 钩子弱、长镜连排、节奏密度低、承诺未闭环 | `n2d-script` | 阶段2 强制补 `script_quality_contract` 的首 3 秒钩、留存承诺、逐 Clip 戏剧功能；需要状态/交互/因果链的题材，落 `state_transition_manifest`、`interaction_graph`、`causal_event_graph` |
| UI/系统面板/画中文字漂、缺 expected text/OCR sidecar | `n2d-image` + `n2d-compose` | 图侧登记 `ui_asset_registry`/`screen_text_lines`/expected text，正式文字由 compose overlay；score/review 缺 OCR 证据时只算缺数据，不当通过 |
| 中段锚帧、尾帧到底有没有被视频后端消费不可见 | `n2d-video` | 每个最终 MP4 的 manifest 必写 `anchor_consumption`、`frame_control_mode`、`multiframe_images_rel` 或 split relay 段落，review/score 读它判“已消费/仅参考/被吞” |
| 运动质量、物理接触、多人对话音画缺侧证 | `n2d-video` | 高动态/接触/说话镜必须产 `video_eval_manifest` 或同等 sidecar，含 motion、camera、S2V、DAV、CG1 的抽帧/判题/人工复核状态 |
| 字幕、口型、原生音画对齐、声纹一致性缺证据 | `n2d-compose` | review/release 前补 `native_av_subtitle_alignment`、`dialogue_av_alignment`/SyncNet 或降级签收、`native_voice_identity` |
| 调色、响度、BGM/拟音、系列包装缺交付证据 | `n2d-compose` | 母带旁必须有 `final_timeline_probe`、`grade_applied`、混合后端 color match、`tension_mix`/BGM gain、room tone/foley、loudness 报告 |
| 成本/供应商/配方/重抽账不全 | `n2d-dashboard` + 各生产 skill | 每次出图/出视频/合成都记 provider、cost、recipe/prompt/reference hash、backend version；缺账进入 production ops warn |
| 画布缺媒体或深链不可用 | `n2d-review-ui` | 静态 UI 必优先读登记路径，登记路径失效时按 Clip 编号兜底真实产物；board 只链接已生成的单集 UI，不假装已部署 app |

# 模式①：作品质检

## 机检 / 人判分工

- **机检（确定性，先跑）**：`scripts/mechanical_check.py <作品根> 第N集` —— 秒级出确定性问题：字幕↔配音文本/时间码对账、中英字幕错位、占位未精修、单行溢出、配音占位音色未替换、产物完整性对账、钩子/集尾留存信号缺失、**脏标点 lint（中英）**（中文 `||` 气口残留 `。，`/`，，`/行首逗号 + 英文 标点前空格/多空格/叠逗号/行首逗号·省略号 `...` 豁免——即便字幕与配音"同样脏"对账能过也单独抓；源头 `voice_text.clean_text` + `finalize._clean_punct/_clean_en` 已修，旧集 stale 数据靠它兜）。**审查第1步即自动跑，无需人记得扫。**
  ```bash
  python3 <skill>/scripts/mechanical_check.py <作品根> 第N集          # 人读
  python3 <skill>/scripts/mechanical_check.py <作品根> 第N集 --json   # 喂回 LLM 汇总
  ```
- **算法层一致性（P0/P1/P2，已接入 `consistency_audit.py` + stage gate）**：
  - **P0 语义谱系 Diff**：`semantic_continuity.py` 把 `voiceover.txt → storyboard.json → 出图 prompt → 出视频 prompt` 抽成关键词谱系，检查角色、场景、状态、风格、专项模板、continuity 是否逐层继承；匹配层是精确词 + 常见同义别名 + 中文 bigram 重叠，仍保持无依赖可进 gate。
  - **P1 n2d 动态百科 / 状态哨兵**：`state_continuity.py` 读取 `storyboard.json.visual_contract.角色状态演进` + `出图/共享/visual_state_ledger.json`，抓状态提前泄露、区间结束后泄露、开始后漏继承；`until/至 ClipN/本镜` 会被当作状态区间，不再默认延到集尾。跨集账本可用 `state_ledger_build.py <作品根> --episodes 1-10 --write` 从 storyboard 确定性生成。**像素在场增强档（SP1-V）**：`state_continuity` 是最严的文本 BLOCK 字段，但只查「状态锁写没写进 prompt 文本」——`scripts/state_pixel_contract.py <作品根> 第N集 --write` 进一步查「图里到底处于这个状态吗」：按 `角色状态演进` 逐镜区间，对每帧的状态角色双向判——① 区间内应有该状态、像素却检不出=`state_pixel_missing`；② 区间起点之前不该有、像素却已出现=`state_pixel_premature_leak`（最危险：图提前泄露未到点的觉醒/伤/变身，文本对、像素错）。状态→检测词经 `identity_registry.identity_marks` 的 `detect_phrase` 桥接（金瞳觉醒态↔MARK_金瞳/"golden glowing eyes"），无注册 mark 退化用中文状态描述。复用 `backends/presence_owlv2.py`（env `N2D_MARKS_PRESENCE_BATCH_CMD`，已兼容 `expect:"absent"` 双向）写 `生产数据/state_pixel_第N集.json`；`state_continuity` 自动读该 sidecar 把像素证据并进状态哨兵（**一律 warn**——OWLv2 对小标记/侧脸/遮挡噪声大，不硬 block，文本档仍是 BLOCK 权威）。缺检测器命令则只产 manifest，交装好后端复跑。
  - **P2 多模态视觉语义 / 道具与武器漂移 / 场景结构唯一性**：`multimodal_consistency.py` 按资产分组查离群；`scene_consistency.py` 进一步集成 `asset_registry.json`，自动提取场景/道具/武器的**结构唯一性约束**（如：LOC_01 必须单门单窗、PROP_02 毒酒瓶必须圆口无嘴、WEAPON_01 必须一柄一刃且握柄/剑穗不漂），在 QC 报告中显式列出作为人判/机检的**强约束基准**，防止 AI 在视频中随机增减背景结构或把主角武器换形。
- **阶段 gate（确定性，生产前跑）**：正式生产入口统一用 `python3 skills/n2d-dashboard/scripts/dashboard.py gate <作品根> 第N集 --stage image_preflight|video_preflight|image|video|compose|review`。默认在正式调用后端前跑 `image_preflight` / `video_preflight`，生成落档后跑 `image` / `video` 回验。它会调用底层 `scripts/gate.py --json`，把高风险流程规则脚本化检查并把 `block/warn/info`、回流 stage、重跑范围写进 `生产数据/`，同时外发 `gate_findings_<stage>_第N集.json`（kind=`n2d_consistency_findings`，可直接交给 `n2d-batch --from-consistency-findings`；验收总账可用 `--from-consistency-ledger` 按 root cause 入队）；`block` 退出码 1，先修再进入该阶段。底层 `scripts/gate.py --json` 只作调试/机器消费入口，不作为生产推荐入口。检查范围包括：合规包、`storyboard.json` continuity、尾帧、prompt 检查段、共享定妆、资产身份注册层、**人物在场链（`entity_schedule`/`required_presence`/`offscreen_presence`/`forbidden_presence` + `continuity.entry_exit`，连续接缝未解释的实体凭空出现/消失会 BLOCK 回 script_stage2）**、核心角色 `performance_signature`、主角/核心动作角色 `signature_equipment` 与 `WEAPON_xx.weapon_profile`、高频 LOC 平面/门窗/轴线/左右站位规则、占位配音、clip 原生音轨/原生音画 opt-in/时长、基础视觉风格契约、复杂镜头模板、Motion Control、生图模型/渠道一致性、风格化脸 StyleID release gate、POS 持有账本前置、保留/混入原生音轨的物理 sidecar、生成配方证据、海外字幕译名表、**物料新鲜度（出图/出视频花钱前体检 skill 漂移→旧物料过期）**、**P0 语义谱系 / P1 状态百科前置**；`review` gate 另跑 **P2 多模态漂移**。≥3 集长线项目若核心/常驻角色仍用无持久主体生图后端且缺 native subject / Face Lock / face_embedding / LoRA，`image_preflight` 直接 BLOCK；registry 未标核心/常驻角色也 BLOCK。风格化项目（二次元/国漫/韩漫/日漫/厚涂/水墨/Q版/美漫/动态漫画/低多边形/纸片定格/插画/动漫）若进入正式投放/production profile，或 CU/MCU/反打角色镜占比高，缺 `N2D_STYLEID_MODEL` 会 BLOCK，除非写结构化 `生产数据/styleid_release_signoff_第N集.json`。若 `_设置.md`/env 声明 `一致性严格度=production`，或处在 `compose/review` 交付边界、已有多集 storyboard/成片交付产物且未显式写 `demo/宽松`，轴线/视线、音画同步、节奏密度、持有账本、场景平面、译名一致、生成配方、运动质量、音乐/环境声、人审校准集等 advisory 在重复、关键场景或交付边界会升级为 BLOCK。确认为可接受时，需写结构化 `生产数据/consistency_advisory_signoff_第N集.json` 后复跑；字符串或只写维度不再放行。
  ```json
  {
    "accepted": [{
      "accepted": true,
      "finding_hash": "由 gate blocker 文案给出，或改用 dimension+message_contains/loc_contains/shot",
      "dimension": "轴线视线(X1)",
      "message_contains": "关键对峙",
      "reviewer": "qa",
      "reason": "导演有意越轴，已人工确认叙事动机",
      "expires_at": "2099-01-01"
    }]
  }
  ```
  - **输入首帧脸一致性硬闸**：`video_preflight`/`video` 读取 `生产数据/image_qc/<ep>/image_qc_<ep>.json`，不重跑像素引擎但严格消费证据。缺 image_qc、旧版 QC 缺 `face_reference_coverage`、`qc_environment.precision_level!=full`、`summary.hard_blocks>0`、`face_reference_coverage.missing>0`、PNG 晚于 QC，全部 BLOCK 回 `image`。每张已落档角色 PNG 必须逐张对定妆/身份主参考过 full 精度脸部比对；这是图生视频前置铁律，不能降级成 WARN。
  - **原生音画物理一致性硬闸**：`制作模式=原生音画`，或 `_设置.md`/总览声明 `视频原生音轨=保留原片音轨|低音量混入环境声`，`video_preflight` 都要求出视频总览包含「原生音画物理一致性契约」，并同步写 `生产数据/native_av_physics_第N集.json`。字段齐全：声源归属、口型策略、材质/动作声、可见动作证据、空间声学/混响、字幕/后期策略。缺任一项 BLOCK 回 `video_prompt`；这是防止原生台词/口型/动作声/混响各自漂移的前置契约。
  - **对白事实锁硬闸**：`制作模式=原生音画` / `native_speech` 说话镜进入 `video_prompt_preflight` 或 `video_preflight` 前必须有 `生产数据/dialogue_fact_contract_第N集.json`。`scripts/dialogue_fact_guard.py <作品根> 第N集 --write` 从 `voiceover.txt`、`storyboard.json`、角色卡生成三轨合同并检查：物理 Clip 的 `allowed_voiceover_indices` 互斥；`allowed_character_dialogue_indices` / `allowed_narration_indices` 分轨；`screen_text_lines[]` 只允许 `compose_overlay_only`；split relay 上下半段不重复对白、旁白或屏幕文案；年龄/身高/灵根/趟数等数字或设定事实不漂移（如十四岁不得变 15/16 岁，二十趟不得变十五/十六趟）。执行边界也要审：角色对白可由 video native_speech 出口型；旁白必须回 `compose` 生成/混音，视频 prompt 不得要求生成旁白音频；屏幕文案/字幕必须回 `compose` overlay，不得烤进视频帧。缺合同、重复索引、重复屏幕文案、屏幕文案非 overlay 或事实漂移 → BLOCK 回 `script` / `video_prompt`；成片缺旁白音频、字幕或 overlay → BLOCK/WARN 回 `compose`。
  - **原生音画字幕对齐硬闸**：`制作模式=原生音画` 的说话镜不跑前期配音 SRT，compose 可先出无字幕 draft；但 review / release / `paid_distribution` 前必须有成片后 whisperx 或等效**词级对齐**证据。落 `生产数据/native_av_subtitle_alignment_第N集.json`（`kind=n2d_native_av_subtitle_alignment`、`status=pass|aligned|ready`、`alignment_tool/source`、`word_level=true` 或 `alignment_granularity=word`、`subtitle_path`，可选逐 Clip 状态），并确保 `subtitle_path` 指向真实 SRT。缺 sidecar、缺词级证据、字幕文件不存在或任一 Clip 未通过 → `原生音画字幕对齐` BLOCK 回 `compose`；非付费 compose 阶段只 WARN，避免阻断 draft。
  - **原生音画声纹身份硬闸**：`制作模式=原生音画` 时，review / release / `paid_distribution` 前必须补 `生产数据/native_voice_identity_第N集.json`（kind=`n2d_native_voice_identity_segments`，逐片段写 `character_id`、`speaker_key`、`wav`），并跑 `n2d-identity/scripts/voice_print_consistency.py` 生成 `生产数据/identity_voice_print_第N集.json`。缺 sidecar、sidecar 无片段、音频不存在、声纹后端不可用且未显式 `N2D_ALLOW_DEGRADED_QC=1` → `原生音画声纹身份` BLOCK 回 `compose/video`；非付费 draft 只 WARN。
- **QA 入账铁律（P0）**：每次跑 gate 或作品质检，都要把 QA 结果写入 `n2d-dashboard`。生产前 gate 不再先跑裸 `gate.py` 再补记账；直接用上面的 `dashboard.py gate` 单入口。人判报告里的新增阻断用 `record --stage review --event qa --qa-sev block|warn|info` 补录；否则仪表盘无法统计 QA 阻断率。dashboard 现在拆分 `generation_pass_rate`（生产尝试效率）与 `deliverable_pass_rate`/`final_pass_rate`（验收口径）；只要有 QA block，可交付通过率即归零，不能用“生成有一半成功”掩盖不可交付。
- **长线弱生图后端硬闸**：第 3 集起或已有多集 storyboard 时，若 `生图AI` 无 native subject / 主体库能力，核心/常驻角色必须有可执行身份锁（native subject / Face Lock / face_embedding / 当前后端可消费的 LoRA）。LoRA 只有在 `target_backends` 覆盖当前生图后端，或当前后端是本地开放管线（ComfyUI/SDXL/Flux 等）时才算数；云端多参考后端不能借一个不可用 LoRA 通过。
- **自动审片评分（P2）**：作品质检完成后跑 `python3 skills/n2d-score/scripts/score.py <作品根> 第N集 --run-checks --threshold 85`，把语义继承、状态百科、多模态漂移、角色 DNA 一致性（脸/发型/服装/配饰）、场景一致性、字幕正确性、音画同步、音色一致性、节奏密度、风格一致性汇总成机器分。`--run-checks` 还会缓存 `score_inputs/第N集_visual.json` 与 `score_inputs/第N集_voice_print.json`，接入图像相似度、字幕 OCR、成片/配音/SRT/storyboard 时长对账、口型风险/检测报告、成片节奏密度和声纹机检，让机器分更贴近观感。若用户要求自动返工或批量回流，加 `--enqueue-low`，低分维度会转成 `n2d-batch` 的 rerun 队列。评分后需要人审时，跑 `python3 skills/n2d-review-ui/scripts/review_ui.py <作品根> 第N集 --write --markdown`，把机器分、QA flag、首尾帧、clip、接缝和定妆参考放进可视化画布。
- **人判（LLM 判断题）**：机检覆盖不了的语义维度——崩脸/场景漂移、**clip 接缝跳切/闪烁**、构图景别（对 `n2d-script/references/分镜语法.md`）、节奏体感（对 `n2d/references/导演节奏.md`）、口型、原生音频双人声。逐维见 `references/checklist.md`。
  - **`音画同步` advisory 机检（AV1·2026-06 补齐）**：`scripts/lipsync_consistency.py` 用 SyncNet / 字节 LatentSync / AVSyncNet 量**口型↔配音 AV 偏移**——补上「无 SyncNet」这个早已过时的旧缺口。诚实边界：风格化嘴型 + SyncNet 弱置信不稳，故基础审计 `verdict` 仍封顶到 `warn`，低置信镜判 `inconclusive`（不罚分）；但 `一致性严格度=production` 下，重复同维度、关键场景、compose/review 交付边界、**或带对白的近景/特写（口型穿帮最刺眼，即便孤例）** 的 AV1 WARN 会由 gate 升 BLOCK，除非写入 `consistency_advisory_signoff_第N集.json` 签收。**实测严重档**另存喂 `n2d-score`（非 gate 退出码）。重活（脸轨抽取 + 推理）需 out-of-repo conda 环境——生产端是 **`scripts/lipsync_measure.py`**（SyncNet 桥接），在 syncnet 环境跑成片→逐 Clip 偏移→写 `生产数据/syncnet_raw_第N集.json`：`conda run -n syncnet python skills/n2d-review/scripts/lipsync_measure.py <作品根> 第N集`（SyncNet 仓库默认 `~/syncnet_python`，env `SYNCNET_HOME` 可改）。消费端缺后端/ffmpeg 且无外部报告时优雅跳过交人判，绝不臆造分。main() 还写 `生产数据/lipsync_第N集.json` 供 `n2d-score` 的 `check_lip_sync` 自动消费（把仅数关键词的启发式升级为实测）。**修复 cheap-fix-first（G2·2026-06-22）**：测出偏移的镜 finding 自带修复阶梯——`return_to_stage=video` + ①先跑 `python3 skills/n2d-video/scripts/lipsync_pass.py <作品根> 第N集 --apply` 做后期对口型（LatentSync 优先·保漫剧脸身份·不重出整 clip·最省）②救不回才回 video 用首尾双帧 + 口型同步后端重出（贵）；inconclusive/ok 不给修法。别一上来就重渲染整 clip。
  - **`节奏密度` advisory 机检（Rhythm·2026-06 补齐）**：`scripts/pacing_retention.py` 读 `storyboard.json` 的镜头时长序列/rhythm 标注做**启发式留存预测**，覆盖 ① 黄金前3秒钩子信号强度（开场镜是否冷开场钩 + 是否过长）② pacing 密度（镜/分钟·过慢像 PPT/过密疲劳）③ 留存曲线代理（镜头时长方差·非铺垫长镜聚集=掉留存风险段）④ 对照可配置短剧基准（采集日期注释·env 可覆盖·非铁律），输出 0-100 advisory 分 + 风险镜。行业基准来自 `n2d-dashboard/references/industry_benchmark.json`，需通过 `n2d-dashboard/scripts/validate_benchmark_schema.py` 的 v2 provenance schema（每条留存信号必须有 source_ids/definition/checked_at/expires_at/confidence）。诚实边界（与 `lipsync_consistency` 同级·advisory）：这是**启发式代理非真观感模型**，故脚本自身 `verdict` 封顶到 `warn`，证据不足（无 storyboard/clips 太少）→ `inconclusive` 交人判**绝不臆造分**。现在它已接入 `consistency_audit.py` 的 `节奏密度(Rhythm)` 和 `n2d_schema.audit_labels`；常规 profile 只提示，`一致性严格度=production` 下，重复同维度、关键场景或 compose/review 交付边界的 Rhythm WARN 会由 gate 升 BLOCK，除非写入 `consistency_advisory_signoff_第N集.json` 签收。新增一致性维度若不接 `audit_labels` runner，schema 模块加载断言会要求显式登记进 `GATE_UNAUDITED_DIMENSIONS`，杜绝机检静默蒸发；当前该 allowlist 为空。
  - **视频侧一致性补强（VLM1/VSEM/DAV/CG1/CAM1/MOT1/S2V/SPECV）**：`consistency_audit.py` 已接入 8 个纯标准库 sidecar 消费器：`video_vlm_consistency.py` 读 VLM/LMM 成片判断题报告并校验 judge schema/抽帧/问题链/投票元数据（**G-V4：VLM 只作 triage，自报 block 钉死降为 advisory warn·原判定留 `vlm_raw_verdict`；崩脸/穿帮终判走确定性层或交付 gate 升级——Artifact-Bench 2026-05 最强模型细粒度穿帮识别<10%**），`video_semantic_consistency.py` 读 DINO/CLIP/DreamSim 主体/背景相似度，`dialogue_av_consistency.py` 查原生多人对话的说话人、台词轮次、turn-taking、幻觉说话人、口型和镜头对人，`causal_event_consistency.py` 按规则库查碰撞/重力/破碎/燃烧/液体/接触连续/状态守恒等物理因果链证据，`camera_trajectory_consistency.py` 查相机运动、越轴、深度和抖动轨迹，`motion_quality_consistency.py` 查运动平滑、动态程度、冻结帧、jerk 和动作完成度，`subject_video_consistency.py` 查图生视频主体保真、自然度、文本相关、背景解耦和多主体串换，`spectacle_video_qc.py` 汇总打斗/追逐/腾云驾雾/大场景的成片证据（SPECV）并可同步写 MOT1/CAM1/S2V sidecar，且把动作镜成片质检拆成**八维+高级制作三项核验**（光流方向↔意图对账/肢体畸变多手多脚/运动平滑完成度/运动模糊合理性/首尾 match-on-action/跨镜连续+180°轴线/主体身份保持/时序闪烁 + 关键帧覆盖/命中峰值可读/剪辑音效同步；单一真值源 `n2d_spectacle.SPECTACLE_QC_DIMENSIONS`），逐镜标 verified/unverified，并给**高光流帧重点采样计划** `sampling_plan`（动作峰值加密抽帧·FMG-DFS）。其中光流方向/肢体畸变/运动模糊三个**新增维**由 out-of-repo 动作-artifact runner 写 `生产数据/spectacle_motion_artifacts_第N集.json`（桥接见 `references/动作镜QC机检.md`），未测时 SPECV warn 提示、production 交付边界升 BLOCK。`motion_quality`/`spectacle_video_qc` 对高动作镜要求 `speed_curve`、`spatial_path`/`distance_curve`、`impact_frame`、`altitude_curve`、`parallax_flow`、`scene_layer_pack`、`keyframe_coverage`、`impact_apex_readability`、`edit_sfx_sync` 等后验证据，避免 prompt 写得精彩但成片速度/距离/命中帧或空间层次不成立。成片通过实测后，跑 `motion_reference_library.py <作品根> 第N集 --write`，只把 `measured/external_report/vlm_verified/tracked` 且有真实视频文件的小样沉淀进 `生产数据/motion_reference_library.json`，供后续 route 的 `execution_recipe.reference_inputs.motion_reference` 调用。`video_eval_runner.py` 先生成抽帧/问题/sidecar manifest；重模型 runner 不绑进主审计，只要把报告写到 `生产数据/*_第N集.json` 或对应 `出视频/合成` sidecar，审查链路会统一产出 findings。若项目已有成片/视频且文本明显触发原生多人对话、物理因果、相机运动、主体视频、运动质量或高动态证据风险但缺报告，审计给 WARN；production profile 下 MOT1/SPECV/CAM1/S2V（高动态四维成片证据）在重复同维度、关键场景或 compose/review 交付边界会随交付 gate 升 BLOCK，让"动作镜缺成片证据"本身在交付时可见，除非写入 `consistency_advisory_signoff_第N集.json` 签收。
  - **角色 DNA 用图判**：先读 `设定库/角色圣经.md`（人读总入口）和 `出图/共享/identity_registry.json`（机器真值），再把该集 `出图/第N集/图片/镜头*.png` 与 `出图/共享/图片/定妆_<角色>.png` / 半身 / 侧背 / 表情参考 **并排读图比对**，按七层判：脸（脸型/五官/疤鳞痣）、发型（发色/发髻/披发轮廓/发饰）、服装（款式/主色/剪影）、配饰（发簪/腰牌/钥匙/护甲等）、专属装备（`signature_equipment` 绑定的 `WEAPON_xx/PROP_xx/VFX_xx`）、表演签名（微表情/惯用动作/站姿/眼神反应）、说话节奏。核心/常驻角色的 `identity_registry.forms[].performance_signature` 在 production profile 下是必填；核心动作角色还必须绑定 `signature_equipment`，其 `WEAPON_xx.weapon_profile` 用于审武器审美、剪影尺度、握持方式和战斗用法。任一层漂到观众认人断层都标问题。装了 `insightface` 时跑 `scripts/face_consistency.py <作品根> 第N集` 给脸部余弦相似度分——**不写死阈值**，而是**自标定 flag-band**：用本作定妆组内部互相余弦（同角色 正脸↔侧脸/半身 本就该高度相似）当"同一人下限"地板，每镜低于 地板−margin 标 🔴、地板带标 🟡（写死 0.45 对风格化脸要么误杀要么放过；业界 ArcFace 同人≈0.5–0.68 且因画风而异）。**远景/群像人小**：最大脸短边占比 < `N2D_FAR_SHOT_FACE_MIN_RATIO`(默认 0.045) 时脸太小到识别器不可靠，该镜标 `verdict=unverifiable`（不强行下崩脸判·避免对远景误判漂移），不计入一致性均值，交近景镜/人判核身份——与 `scene_layer_pack` 的 `identity_recognizable=false`（远景不喂脸锚、用阵营色/剪影）同策略。**脸 embedder 是选择点 `脸一致性机检后端`（arcface 默认 / styleid·见 `n2d/references/选择点与偏好.md`）**：ArcFace 按真人脸标定，**风格化/漫剧脸 TPR 系统性掉到 ~0.76**（StyleID 论文 arXiv:2604.21689·2026-04），风格化项目切 `styleid`（CLIP-L+StyleID LoRA·需 `N2D_STYLEID_MODEL` 权重，缺权重自动回退 arcface 标 `arcface_fallback`，绝不用裸 CLIP——裸 CLIP 更差 0.17–0.25）；CLI `--encoder arcface|styleid`，解析顺序 CLI>`_设置.md`>env `N2D_FACE_EMBEDDER`>默认 arcface。`n2d/doctor.py` 与 `image_preflight` 会对二次元/国漫/韩漫/日漫/厚涂/水墨/Q版/美漫/动态漫画/低多边形/纸片定格/插画/动漫项目主动提示配置 StyleID；正式投放、production profile 或 CU/MCU/反打角色镜占比高时，缺 `N2D_STYLEID_MODEL` 从降级 KPI 升为 release BLOCK，除非 `styleid_release_signoff` 结构化签收。**fidelity-gate**：跑 `vlm_verify --write` 落 canonical 通过表后，崩 canonical 的镜不进一致性均值（防"稳定地错"刷高分·EntityBench 2026），单列 `render_errors` 回 `n2d-image`。**报告型一致性 KPI**：`n2d-score` 的 `character_consistency_kpi`（intra/cross 对照可投放线 `N2D_CONSISTENCY_RELEASE_LINE`·须随 encoder 重标定）。注意：进入视频前不只靠审片人“看过”，必须先由 `image_qc.face_reference_coverage` 证明每张已落档角色 PNG 都有 full 精度定妆/身份主参考比对记录；缺库、降级、warn/noface、缺比对行都回 `n2d-image`。
  - **服装/配色漂移用机检（脸之外的漂）**：脸锁住不等于服装锁住——2026 公认"夹克色第 4 镜就漂"。装 Pillow 时跑 `scripts/outfit_consistency.py <作品根> 第N集`：对人物镜取**加权色相直方图**（饱和度×明度加权，压低灰暗背景），与该角色定妆组（优先半身）比，同样**自标定 flag-band**（定妆组内部直方图余弦设地板）。缺库由人判并排比服色/发型。
  - **发型/发色漂移用机检（脸和服装之外的漂）**：装 Pillow 时跑 `scripts/hair_consistency.py <作品根> 第N集`，对头部区域取发色 + 发型轮廓复合指纹，专抓披发变盘发、长发变短发、黑发漂成白/红发、发饰丢失这类“脸过了但角色 DNA 换了”的问题。缺库由人判并排比发型和发饰。
  - **片内时序用机检（单 clip 内身份漂移 + flicker）**：n2d-review 只查首帧 + 接缝，漏查 clip 内部"几秒后脸渐变/发际线闪"。装 ffmpeg(+insightface) 时跑 `scripts/temporal_consistency.py <作品根> 第N集`：每条 clip **按时长自适应抽帧**（≈1帧/秒，近景镜 ×1.5 加密，floor=6·封顶24——固定 6 帧对 10–60s 长镜太稀，片中段渐变脸漂会从采样缝漏过，2026 各家 long-range 时序仍是软肋），量 ① 相邻帧人脸余弦最小值（片内身份漂移）② 相邻帧亮度绝对差均值=flicker/TCI，超阈标 🔴/🟡。对标行业 scene-stability 记分卡；缺库由人判抽帧看。
  - **定妆主参考质量门（N3·锚点不能脏）**：锚点一脏下游每镜继承错。装 insightface+cv2 跑 `scripts/face_consistency.py <作品根> 第N集 --audit-anchor`：每个 `定妆_<角色>.png` 主参考须**恰好 1 张清晰、够大的正脸**（0 张/多张=🔴，脸太小=🟡）——出图共享先行闸门前就拦，别让脏锚点污染全集。
  - **糊/低质无参考质检（N4）**：装 Pillow 跑 `scripts/quality_check.py <作品根> 第N集`：用 **Laplacian 方差**测清晰度，**自标定本集中位数**（绝对阈值因画风漂），显著低于中位数=相对糊（关键镜更严），标 🔴/🟡。与一致性正交，同属「崩脸/糊」族。
  - **风格漂移用机检（S1·补 style_contract 落地后零机检的盲区）**：装 Pillow 跑 `scripts/style_consistency.py <作品根> 第N集`：每镜取风格指纹（饱和直方图+明度直方图+边缘密度），**自标定 median-中心 flag-band**——内聚度显著低于本集中位=某镜突然偏离所选风格（突然照片感/插画/高饱和）。配合 `style_contract` 与逐镜负向风格禁忌（gate 已强制）形成"契约定风格→负向堵→S1 兜底机检"闭环。缺库人判读「本集基础视觉风格契约」并排看。
  - **接缝跳切先机检再人判（N2接力·把"逐接缝并排读图"降成初筛）**：装 Pillow 跑 `scripts/temporal_consistency.py <作品根> 第N集 --seam`，**两个互补量化指标**：① 尾帧 `镜头N_end.png` vs 下一首帧 **dHash 距**（灰度结构）抓构图/姿态错位；② **RGB 直方图 cosine 距**抓"同构图但灯光/色温跳"的剪辑点闪光（dHash 看不到颜色）。任一超阈即报、取较重者定级；色彩端缺 Pillow 时静默退化为纯 dHash（不臆造）。距大=尾帧没对上下一首帧→出视频必跳切/闪；距小不代表姿态完美仍需人判，但距大几乎必跳——先机检筛出可疑接缝再逐个人判。
  - **2026 新增一致性机检（8 类·全部由 `scripts/consistency_audit.py` 一键编排自动跑，各自缺库优雅跳过）**。**出视频前硬挡**：这套总审已前移接进 `gate.py --stage image`（出图后闸门），手部多指/天气硬跳/极端身高/禁用称谓/动作硬断等**确定性 🔴 维度**在最近的出图闸门即 BLOCK，挡在最贵的「出视频」之前（不再只在 compose/review 才拦）；降级精度（缺 insightface）在 image 闸门只降 WARN（不硬拦无依赖产线），交付边界 compose/review 才对降级精度 BLOCK，逃生口统一 `N2D_ALLOW_DEGRADED_QC=1`（留痕自负）。各维度：
    - **手部/解剖畸形（N5）**：`scripts/hand_anatomy.py`——AI 生图头号翻车点（多手/多肢、缺肢、畸形手脚/关节、身体与地面/道具/光效融合），face/outfit/hair/糊 都不看它。装 cv2（可选 mediapipe 定位更稳）时按**手轮廓凸缺陷数指尖**，≥6 指尖=多指铁证 🔴；握拳/遮挡不误杀。`n2d-image/image_qc` 已把 N5 前移到出图落档：像素铁证 block，并在 prompt lint 中硬拦缺 `人体完整性`、`手部归属`、脚/身体接触面与不穿模不融合约束的高风险人物镜。粗筛初筛→人放大看，绝不自动改图。
    - **辨识标记持久性（MK1）**：`scripts/marks_consistency.py`——疤痕/胎记/纹身/异瞳/痣/义体等**载剧情**辨识标记（认亲胎记、战损疤、血统异瞳、禁术印记），脸/发型/服装三个机检都不专门盯它，丢一次既崩脸又崩剧情。读 `identity_registry.forms[].identity_marks`，对每镜在场角色查标记词有没有写进 storyboard/出图 prompt：永久/已获得标记缺失=🟡（疑似漂移/丢失或合理遮挡，人核对），未获得标记在获得集之前出现=🔴时间线穿帮。纯文本/结构机检无依赖，无 `identity_marks` 登记则优雅跳过。**出图前同款预检**已 vendored 进 `n2d-image/image_qc`（角色镜漏写标记锁=warn、获得型标记提前写=block），把标记漂移挡在出图前。**像素在场增强档**：`scripts/marks_presence_runner.py <作品根> 第N集 --write` 按"在场角色×本集应存在标记×出图PNG"配 probe，复用 `backends/presence_owlv2.py`（env `N2D_MARKS_PRESENCE_BATCH_CMD`）逐帧判图里有没有这道疤/这对异瞳，写 `生产数据/marks_presence_第N集.json`；`marks_consistency` 自动读该 sidecar 把像素缺席并进 MK1（缺席按 warn——小标记/遮挡/侧脸像素证据噪声大，不硬 block）。
    - **景深/虚化一致（DOF1·镜头光学）**：`scripts/dof_consistency.py`——`shot_scale_contract` 只管景别(脸占画幅)，不管**背景虚化程度**。装 Pillow 时按"背景区清晰度/主体区清晰度"取景深代理，同场景分组自标定中位数，某镜偏深焦↔浅景深超 margin=🟡（同场景景深档横跳像换相机）。焦段侧另由 `gate.py` 焦段↔景别一致性 + `n2d_contract_diff.diff_cinematic_focal`（出图→出视频镜头焦段继承·opt-in·warn）覆盖。
    - **角色身高/体型比例（R1）**：`scripts/scale_consistency.py`——`shot_scale_contract` 只管景别(脸占画幅)，不管**两人相对高矮**。读 `设定库/身高表.json` 的登记身高比，双人同框时比观测身高比，极端偏离标 🟡/🔴。装 insightface。
    - **轴线/视线像素核验（X1）**：`scripts/axis_geometry.py`——`场景轴线视线` 此前只查 prompt **写没写**，这里从脸朝向(鼻-眼几何)/屏幕位**像素验越轴**（180° 反向/主体跳边）。warn-only（正反打/弧线运镜合法翻转，误报多不硬拦）。装 insightface。
    - **天气/时辰推进连续性（W1）+ 光位方向（W2）+ 光照物理（W3）**：`scripts/world_continuity.py`——`scene_dna` 能锁 `光色天气`/`主光方向` 却无检查器。装 Pillow 测每镜亮度/暖度归 day/dusk/night，同场景相邻镜 day↔night 硬跳=🔴、隔一档=🟡，违 scene_dna 锁=🔴。W2 从最亮区质心估主光左右方位，同场景左右硬翻转=🟡。**W3 补 W2 只管左右的垂直盲区**：① 光照俯仰——估顶光/底光，同场景顶光↔底光(自下而上的鬼影光)硬翻转=物理可疑🟡；② 光位锚自洽——实测主光方位/俯仰 与出图 prompt 声明的「光位锚」矛盾（声明画左却实测右打光）=🟡，抓"光打反/锚写错"。全 advisory（光位比时辰更易合理变化），并入 天气时辰(W1) 段评分。`--write` 或 `consistency_audit.py` 会同步写 `生产数据/scene_world_ledger_第N集.json`（kind=`n2d_scene_world_ledger`），把场景、天气/时辰、光位、世界状态 findings 按时间线汇总，供后续集继承和总账引用。
    - **为什么同一「翻转」轴 BLOCK/WARN 并存（C3 设计说明·别误以为不一致）**：轴线/光位「翻转」在不同 detector 故意不同档，是**按证据强度而非按轴**分档：① **确定性·像素硬契约处 = BLOCK**——`inherit_contract`（出图→出视频首帧焊接，光位锚/轴线漂移逐字段 Diff）、`camera_trajectory_consistency`（视频运动越轴）、`n2d_cross_episode` **核心场景**跨集光位/轴线反转，这些是「同一锚点/同一首帧/标核心的场景」上的明确违锁，FP 低，硬挡最省。② **整帧 histogram 估计·易合理变化处 = WARN**——同集 `world_continuity`(W2/W3) / `axis_geometry`(X1) 从像素估主光左右/俯仰/脸朝向，正反打、弧线运镜、戏剧重打光都会合法改变，估计噪声大，故 warn-only、production 交付边界才按 strict-advisory 升 BLOCK。一句话：**硬契约违锁=BLOCK，整帧估计=WARN**；这不是 round3「升 BLOCK 最激进」与 creative-dims「WARN 封顶防误杀」打架，而是同一条「证据够硬才硬挡」原则的两端。确为有意翻转时统一走 `intentional_discontinuity_第N集.json` 签收（见上，gate 侧已接·仅限连续性轴）。
    - **字幕安全区构图（L2）**：`scripts/subtitle_safearea.py`——脸落进底部字幕带会被烧字幕遮挡。装 insightface 比脸框下沿 vs 字幕带顶（默认 0.82·据 render_subs 字幕位）。warn-only。
    - **称谓/口头禅一致性（A1·剧本层人设漂）**：`scripts/address_consistency.py`——AI 改写最隐蔽的塌人设：称呼忽「师姐」忽「姐姐」、口头禅安错人。读 `设定库/称谓表.json`（每角色 canonical/allowed/forbidden + 口头禅）扫 voiceover：禁用称谓=🔴、口头禅错位/正称缺席=🟡。纯文本无依赖。
    - **跨镜动作接力（match-on-action）**：已并入 `temporal_consistency.py --seam`——接力尾帧 vs 下一首帧加**主体边缘质心位移**轴（构图/色/身份之外），仅 strict 接力对判，主体大幅移位=动作没接上 🟡/🔴。
  - **跨集人物一致性（⑦·身份闭环报表）**：优先跑 `python3 skills/n2d-identity/scripts/identity.py <作品根> --episodes 1-10 --write`，生成 `identity_adapter_matrix` + `identity_drift_report`；它把 registry 的 Face Lock / Character ID / LoRA / reference group binding 与逐集 🔴/🟡 漂移合并。旧命令 `scripts/face_consistency.py <作品根> 第N集 --cross-ep` 仍可单独看脸相似度，但不含 adapter 闭环。落档后跑 `python3 skills/n2d-dashboard/scripts/dashboard.py identity <作品根>` 把跨集漂移滚成一条**工作级 KPI**（漂移角色数 / 系统性退化最早自第几集 / 相似度趋势 + 软门 advisory），让"第几集开始系统性退化"被监控而非只躺在报表里。
    - **报告新鲜度闸（`check_drift_report_freshness`·堵静默退化）**：实测跨集脸漂的 BLOCK 环依赖这份 `identity_drift_report.json`，而它只由 `identity.py --write` 手动生成、gate 只读不刷——漏跑/陈旧时环会读旧数据误判全绿。`image_preflight`/`image` gate 现把「历史已出图集是否都被报告覆盖」钉成闸：报告**在但漏覆盖**部分历史出图集 = present-but-stale → 🔴 BLOCK（最危险，留 `N2D_ALLOW_DEGRADED_QC=1` 逃生口·留痕自负）；报告缺失/未实测（无 insightface）→ WARN（给重跑命令，不硬拦无依赖产线）。修法：重跑 `identity.py <作品根> --write` 覆盖全部历史出图集。
    - **物料新鲜度闸（`check_skill_freshness`·花钱前体检 skill 漂移）**：skill 升级后，生产前期物料的 skill 可能已变，旧物料就过期了；可此前 skill 漂移检测**只在用户手动跑 `n2d-update` 时发生**，真正花钱的 `image_preflight`/`video_preflight` 从不查它。现把同一套 **git-free SHA256 内容快照**检测（基线 `生产数据/skill_update_snapshot.json`，核心 `n2d/_lib/skill_freshness.py`）下沉进四道 preflight（`image_prompt_preflight`/`image_preflight`/`video_prompt_preflight`/`video_preflight`）：生产 owner skill（n2d-script/voice/image/video 或 `n2d/_lib` 运行期契约）自上次基线后有改动 = **WARN「前期物料可能已过期」**，路由回 `update_plan.py check <作品根> 第N集` 评估精确重制范围再生成；仅横切/QC/gate-only 改动 = INFO；**无基线 = 静默**（无可比对对象，不 nag）。是 advisory **不硬阻断**（是否重制是判断题，交给 `n2d-update` 的精确规划器；`n2d-update` 主管文件→阶段映射与有界重制计划，gate 侧只读不写、永不自动落基线）。要全程自动生效：产完一阶段、验收通过就 `update_plan.py record` 一次（见 `n2d-update/SKILL.md`）。
  - **接缝跳切用图判（逐接缝过）**：沿接力链逐个查相邻 Clip 的接缝——取 Clip K 末帧 vs Clip K+1 首帧**并排读图**，对照 `故事板.md` 该接缝契约：① 契约说"姿态连续硬切/有尾帧"但两帧人物姿态/站位/视线/光线明显对不上 → 跳切/闪烁，标问题；② 契约 `需要尾帧?=是` 却没出 `镜头N_end.png`（n2d-image 漏做）→ 标接力断链；③ 服装/发型/道具在接缝处突变 → 接缝崩。轻微姿态差走容错铁律放行。修法：回 n2d-image 补尾帧 / 回 n2d-video 用首尾双帧重出该 Clip。
  - **导演一致性契约用人判（整段读）**：审视频 prompt 或成片时先读 `出视频/第N集/prompt/00_总览.md` 的「本集导演一致性契约」，再逐 Clip 判断是否违反主色调、镜头语法、轴线、剧情状态锁、场景状态。典型问题：爽点特效提前泄露、同场景灯位/雨雾跳变、人物左右站位反复跳轴、铺垫段乱甩镜、落幅接不上下一镜。修法：回 n2d-video 改 prompt；若首尾状态源头不对，回 n2d-script 改故事板接力契约，必要时回 n2d-image 补尾帧。
  - **原生音画物理一致性用人判**：审原生音画成片，或任何保留/低音量混入原片音轨的成片时，先对照总览的「原生音画物理一致性契约」和 `native_av_physics_第N集.json` 抽查说话镜、动作声和环境声：谁发声是否来自画内主体、口型是否随台词变、脚步/衣料/器物声是否只随可见动作触发、远近/遮挡/混响是否随景别和空间变化、whisper 字幕是否能对上原生台词。修法优先回 `video_prompt` 明确音画策略并重出受影响 Clip；后期只补字幕，不用成片硬剪掩盖双人声或错口型。
  - **基础视觉风格契约用人判（整段读）**：审图包、视频 prompt 或成片时先读「本集基础视觉风格契约」（旧项目读「本集真实电影感契约」），再判断画面是否稳定遵守所选风格：角色比例/材质或线条是否一致、光色是否守策略、运动是否在边界内、是否触犯该风格的禁忌。修法：源头回 n2d-script 改 `style_contract`，再让 n2d-image/video 继承；只在单镜末尾补某个风格词视为无效修复。

## 工作流（模式①）

1. **定位 + 确认范围**：作品根 = `创作区/制漫剧/<剧名>/`。问审一集还是整部、审到哪个阶段（出图后 / 成片后）。读 `_进度.md` 知各集进度。
2. **跑机检 + 一致性编排 + 身份闭环 + 合规包 + QA 入账** → 确定性问题清单（按集）：先 `scripts/mechanical_check.py`（字幕/对账/占位），可先用 `scripts/video_eval_runner.py <作品根> 第N集 --write` 生成视频抽帧/问题/sidecar manifest，再 **`scripts/consistency_audit.py <作品根> 第N集` 一键串跑视觉/语义一致性检测器**（**语义谱系 P0 · 状态百科 P1 · 多模态 P2 · 视觉契约继承** · 锚点门 N3 · 脸 G1 · 服装配色 N1 · **辨识标记 MK1**[疤痕/胎记/纹身/异瞳等载剧情标记持久性·`scripts/marks_consistency.py`·获得型标记穿帮=block] · 片内时序 N2 · 场景 O2 · 糊 N4 · **风格 S1** · **景深一致 DOF1**[镜头光学:同场景深焦↔浅景深横跳·`scripts/dof_consistency.py`] · **接缝接力** · **字幕对齐 L1**[双语短语边界/阅读速度/译文完整性，补 mechanical_check 条数对账盲区；`scripts/subtitle_align.py` 可单跑] · **音画同步 AV1**[口型↔配音偏移·advisory·`scripts/lipsync_consistency.py`] · **音频白区 VEA/ACC/BGM**[配音情绪弧:台词强情绪×标注平淡/缺标注 **+ 设计情绪×配音声学能量 reconcile**(强情绪台词却 emotion_flow.json 声学能量垫底=情绪没演出来·能量太平/样本<4 自动跳过) · 口音方言:voicemap accent 锁+冲突 · 音乐衔接:bgm.txt 相邻段速度whiplash·`scripts/audio_continuity.py`·文本/结构·缺 voiceover 跳过] · **空间站位 B1**[跨镜站位/遮挡序四维坐标连续性·`scripts/scene_blocking_continuity.py`·违注册站位锁=block] · **视频侧一致性补强**[视频 VLM 判题 VLM1、视频语义一致 VSEM、多人对话音画 DAV、物理因果链 CG1、相机空间轨迹 CAM1、运动质量 MOT1、主体视频一致 S2V；读 sidecar，不绑重模型依赖] · **生产一致性补强**[物件常驻 O3、持有账本 POS、视线/状态回读 X2、状态转场视频证据 ST1、交互接触 I1、结构化交互图谱 I2、成片统一 C1、成片时间线探针 FT1、生成配方 RCP、强配方 Schema RCP2、系列包装 PKG、台词语域 D1（称谓/口癖/禁用词在场 **+ G-S2 文白横跳/正式度冲突/句长失稳统计漂移**·补脸锁住后语域漂移无人看守的盲区·全 warn）、场景平面 FP1、成本路由 K1、人审校准集 CAL（含误报/漏报→`scripts/calibrate_thresholds.py <作品根> --write --registry` 阈值方向建议 + 阈值注册表；另维护**金标集** `生产数据/consistency_golden_set.jsonl`→`scripts/calibrate_thresholds.py <作品根> --calibrate --write --registry` 按 FCB 多识别器投票反推 per-(维度,后端,风格) 推荐 floor，并同步写 `生产数据/consistency_threshold_registry.json`，治"SEMANTIC_FLOOR=0.55 等起步线跨后端复用假绿/假红"）、一致性探针包 PROBE（含后端 benchmark→`scripts/probe_route_recommend.py --write` 路由建议校验）；`scripts/production_consistency.py`，纯标准库] · **扩展一致性补强**[系统面板/HUD UI1（穿越系统流题材元素+图中文字渲染锁，读 `设定库/ui_asset_registry.json`）、音乐母题 LM1（角色/情绪 leitmotif，`设定库/leitmotif_registry.json`，voice_key 的声音侧类比）、系列调色 GRD（剧级 LUT/白平衡/对比基线，`设定库/series_grade.json`）、环境声 AMB（每场 ambient bed，`设定库/ambient_map.json`）、跨集体型 R2（identity_registry.character_dna 身形锁，补"只有脸有跨集追踪"缺口）、在场检测 O3V（读 `object_presence_第N集.json` sidecar 的视觉级对象持久性，关掉 O3 只有契约文本的 backlog）、外观判官 VAP（读 `appearance_judge_第N集.json` VLM-Appearance sidecar，补 ArcFace 在 insightface 降级时的盲区）、**文字渲染 OCR1**（图中中文字渲染校验：系统面板数值/牌匾/卷轴，`text_render_runner.py` + EasyOCR 后端实读 vs 预期，数字不符=block；2026 仍未解的 AI 生图弱项）、**译名一致 TX1**（专有名词/称谓跨集 EN 译法统一，`设定库/translation_glossary.json`，纯标准库·海外投放）、**表情连续 EXP1**（脸/发/服 DNA 锁住后的下一前沿 performance consistency：该镜表情贴脚本情绪 + 跨镜同角色表情不 OOC 跳变；优先读 `生产数据/expression_第N集.json` 判官 sidecar，缺则纯文本 first-pass 抓「情绪近景无表情参考」「相邻镜情绪硬跳」·dim=character_consistency）、**表情过锁 EXP3**（EXP1 的反面·report-only·IPRO/PixelSmile 减害：主动检出「脸该随情绪动却纹丝不动」的 copy-paste 冻脸——高 face-cosine 假高分恰是身份锁过紧的征兆，身份分越高表演越差；复用已算的 expr 形变+face 余弦零新增推理，A 系统性像素零形变=warn / B 宽情绪角色脸跨镜余弦近乎恒定=info copy-paste 指纹 / C 文本兜底=info 过锁风险；永不 block，给「解耦表情 AU/FACS 或下调身份权重后整体重出」而非单镜重抽的建议·dim=character_consistency）、**伏笔兑现 SP1**（setup→payoff 跨集闭环：读 `设定库/setup_payoff_ledger.json` 校验坑没填/兑现早于种下/缺种下集；账由 n2d-script `setup_payoff_ledger.py` 拆集时脚手架·dim=semantic_continuity·补直接给小说文件不经上游小说线时漫剧侧的叙事连续性兜底）、**叙事状态 NS1**（知识/位置/关系跨集一致性：读 `设定库/narrative_state_ledger.json` 校验**知识倒流**[声明第K集才知道但更早集该角色已提及该 keyword]/**位置瞬移**[同角色相邻集换地点、两集都无转场词]；账由 n2d-script `narrative_state_audit.py` 拆集时脚手架·dim=semantic_continuity·补"视觉 state_ledger 只管伤/泪/妆/服、知识/位置/关系无人看守"的洞·校验只跑字段填全的条目）；`scripts/extended_consistency.py`，纯标准库 + 可插拔 sidecar]）——默认同时**结构化外发** `生产数据/consistency_findings_第N集.json`（kind=`n2d_consistency_findings`，逐条带维度/严重度/镜头定位/`return_to_stage`，`n2d-feedback` 读它做一致性回灌；`--no-export` 关闭），并生成 `生产数据/generation_recipe_第N集.json` 的配方 hash 账本、登记一条 dashboard `consistency_findings` 事件；脸 G1 在无 insightface 时自动降级 **Pillow 基础机检**（图存在/可解码/分辨率/清晰度，标 `pillow_fallback`+`insufficient_precision`，绝不臆造相似度，n2d-score 按降权分消费），再跑 `python3 skills/n2d-identity/scripts/identity.py <作品根> --write` 生成 adapter matrix、跨集漂移报表和 `consistency_findings_voice_print_第N集.json` 声纹一致性 findings；缺 `合规/compliance_manifest.json` 时先跑 `python3 skills/n2d-compliance/scripts/compliance.py <作品根> 第N集 --init` 并人工补齐。阶段 gate 结果必须同步跑 `python3 skills/n2d-dashboard/scripts/dashboard.py gate <作品根> 第N集 --stage review` 或对应生产 stage 入账。别只跑单个检测器漏掉其余。
	   - **证据族防误升**：gate findings 会补 `evidence_family`（如 `face_embedding`、`text_contract`、`appearance_embedding`、`scene_geometry`、`motion_physics`、`audio_sync`、`human_signoff`）。自动关联升级为 BLOCK 时，不只看“≥3 条 WARN”，还必须跨 ≥3 个维度且至少来自 2 个独立证据族，避免同一张脸嵌入/同一段文本契约噪声被重复计数成系统性故障。
	   - **留存硬审计**：`beat_audit --strict` 在出图 prompt 前不再只看“有没有写钩子”。`first_3s_visual_hook` 必须是结构化字段 `visual_hook(冲突/悬念/欲望/反差/信息/危机皆可，旧名 visual_conflict 兼容)/content_proposition/onscreen_text/muted_safe_proof/expected_metric`，且目标指标、首 6 秒钩子和烧屏文字密度要过当前 benchmark；`retention_promise_ledger` 本集到期承诺必须有 `payoff_status + payoff_clip/payoff_evidence/paid_by_episode`；`creative_priors.json` 里每条第一方投放先验必须写 `applied` 或 `rejected + rejected_reason`。这些缺口是 must 级，直接阻断进入贵工位。系列 gate 不再等 ≥3 集才可见：第1-2集也必须维护 `设定库/pilot_arc_contract.json`，否则 compose/review 交付边界阻断。
	   - **生产一致性补强增量**：`scripts/production_consistency.py` 还会检查 `实体记忆(EMB)`（accepted-shot entity memory + 核心角色表情锚点/表演签名，且确认记忆在生成前被检索使用）、`真值源(TRUTH)`（多套设定源必须有 precedence map，生成配方只作 evidence 不能反写设定）、`多视角身份包(MVIEW)`（核心角色正/45度/侧或背/表情视角齐且失败行阻断）、`合法状态转场(STE)`（状态变化必须有 cause/trigger、visual evidence due，reset/heal/disappear 需合法重置原因）、`世界一致性(WCS)`（object permanence / relation stability / causal compliance / flicker penalty 汇总分，低于阈值阻断）、`声音空间(ASP)`（原生音画/成片交付要求 room tone、reverb、距离/遮挡透视）、`物理事件图(PHY)`（law/object/frame/violation 可归因）、`视频证据完整性(EVID)`（`video_eval_manifest` → 标准 sidecar 写回闭环），并加严系列包装平台矩阵、空间记忆和 `配方可复现(RCP3)`（输入指纹、设置/registry hash、产物 hash）。
	   - **裁决/例外/依赖闭环**：`n2d-model-router` 会产出 `consistency_policy_lattice.json` 与逐镜 `policy_resolution`，先解释固定模式、合规能力、身份锁、跨集基线、高动态效果、成本档之间谁让位；`consistency_ledger.py` 还会写 `consistency_dependency_graph_第N集.json`，把 CHAR/LOC/PROP/WEAPON、分镜、图片、视频、成片和 sidecar 串成影响图。若导演有意越轴、跳切、变色、变声或状态重置，先写 `生产数据/intentional_discontinuity_第N集.json` 签收（带 clip、dimension、reason、reviewer、expires_at）。**两处生效**：① 验收总账 `intentional_discontinuity.py` 把匹配 finding 降为 info；② **阶段 gate（C1/C2·2026-06 接线）**——`gate.py` 现也读同一 manifest，把**连续性轴的 native BLOCK** 降为带留痕的 WARN，让"有意不连续"真正在退出码层面松手（此前只改报告、gate 仍硬挡，与设计意图背道而驰）。可签收维度**仅限**世界/光线/轴线/调色/景深/状态转场这类创作者确可有意改变的轴：`天气时辰(W1)`/`光位方向(W2)`/`轴线视线(X1)`/`色温调色(GRADE1)`/`景深一致(DOF1)`/`合法不连续(DIS1)`/`合法状态转场(STE)`；**脸(G1)/辨识标记时序(MK1)/纯文生图/资产生命周期回退/契约继承等真穿帮永不可签收**，必须回源头修。过期、缺原因（<8字）、缺 reviewer 或越 scope 仍按问题处理。`consistency_advisory_signoff_第N集.json` 也被 gate 接受为同义签收（两份逃生口文件现都能咬住 gate）。
3. **生成机器分**：跑 `python3 skills/n2d-score/scripts/score.py <作品根> 第N集 --run-checks --threshold 85`，把机检、dashboard 阻断、一致性结果和 visual checks 落成 `生产数据/score_第N集.json/md`。若要自动回流，追加 `--enqueue-low`，让低分维度进入 `n2d-batch` 队列。
3.5. **验收总账（E3·唯一交付面）**：跑 `python3 skills/n2d-review/scripts/consistency_ledger.py <作品根> 第N集`，把散在各处的剧情、角色、资产、镜头、音频、字幕、合规、score、review-ui、scene/world ledger 和 gate findings 收进 `生产数据/consistency_ledger_第N集.{json,md}`。JSON 保留角色×资产三态表（事前/落档/契约）、`domains`、`delivery_surface`、`dependency_graph`、`discontinuity_audit`、`supplemental_reports`，并新增 `root_causes`：按维度 + `CHAR/LOC/PROP/WEAPON/OUTFIT/VFX_xx` 等锚点把多个症状聚成根因，避免同一个角色/场景漂移在十个报告里分散成十个孤立问题。总账会自动补跑 `motion_grammar_consistency.py`（运动语法 MG1）、`audio_space_consistency.py`（声音空间 ASP / 原生声纹 NV1）、`expression_state_consistency.py`（状态化表情 EXP2），并消费 `intentional_discontinuity_第N集.json`。`counts.block/high` 未清零时不得回写 `_进度.md` 的 `验收=✅`。返工优先用 `python3 skills/n2d-batch/scripts/queue.py plan <作品根> --from-consistency-ledger <作品根>/生产数据/consistency_ledger_第N集.json`，由 root cause + dependency graph 生成最小任务；只有临时单报告问题才退回 `--from-consistency-findings`。
3.6. **剧级验收总账（E3+·整季交付面）**：整季/多集要签收时，跑 `python3 skills/n2d-review/scripts/series_ledger.py <作品根> [--strict]`，把每集 `consistency_ledger_第N集.json` + 跨集 `identity_drift_report.json`（n2d-identity 产）+ 可选 `series_retention.json` **上卷**成 `生产数据/series_ledger.{json,md}`。剧级 `delivery_surface.status=blocked` 当且仅当：任一集 block/high>0、或有集**缺验收总账**（脚本已出未签收）、或有角色跨集脸漂到 block（给 `first_bad_episode`）；`blocking` 逐条指到"回去签收第几集 / 哪个角色从第几集起漂"，`weakest_episodes` 给返工次序。留存信号只观察不阻断（该不该改 ≠ 能不能交付，与 score 分工一致）。`--strict` 时 blocked 退出码 1，供 CI / 批量签收闸门。单集 ledger 是镜级真值，本总账是季级签收闸，不替代单集签收。
4. **生成人审画布**：跑 `python3 skills/n2d-review-ui/scripts/review_ui.py <作品根> 第N集 --write --export-findings --markdown`，输出 `生产数据/review_ui_第N集.html/json` 与 `review_ui_findings_第N集.json`（kind=`n2d_consistency_findings`）。先看全局机器分和 QA flag，再按画布逐个核首帧、尾帧、clip、接缝、定妆参考和缺素材；工业级批量审片不得只依赖文本报告。
5. **人判**：集多时**每集独立审查**（省主上下文），优先打开 `review_ui_第N集.html` 做视觉核查，再对照 `references/checklist.md` 逐维，**只记真问题**，每条带证据（引文 / 图路径 / 时间码）。
6. **汇总报告 + 修复回流** → 写 `创作区/制漫剧/<剧名>/_质检_第N集.md`（整部则 `_质检_全片.md`）：按严重度排序，每条 = 位置（镜头N·@时间码 / 文件）+ 维度 + 问题 + **修法** + 证据。附"健康度概览"表（各维度 通过/问题数 + 一致性度量分如有）。漫剧的修法**回源头改、重跑回流**，不在成片上剪；报告里每条修法都指明**回哪个 stage 重跑**（如"崩脸→回 n2d-image 重出该镜""字幕错位→重跑 finalize_storyboard""节奏塌→回 n2d-script 阶段2 重切镜头时长曲线"）。
   - 批量返工时，优先把 `consistency_ledger_第N集.json` 交给 batch：`python3 skills/n2d-batch/scripts/queue.py plan <作品根> --from-consistency-ledger <作品根>/生产数据/consistency_ledger_第N集.json`；它会按 `root_causes` 聚合 block/high 根因，避免同一角色/资产问题被拆成多条互相掣肘的任务。临时单报告问题再用 `consistency_findings_第N集.json`、`review_ui_findings_第N集.json`、`gate_findings_<stage>_第N集.json` 或 `consistency_findings_voice_print_第N集.json`：`python3 skills/n2d-batch/scripts/queue.py plan <作品根> --from-consistency-findings <作品根>/生产数据/review_ui_findings_第N集.json`。需要手工补充时，再把 finding 的 `return_to_stage` / `affected_artifacts` / 具体 Clip 转成定向任务：`python3 skills/n2d-batch/scripts/queue.py plan <作品根> --episodes 第N集 --rerun-from image|video|compose --affected-shot Clip_03 --affected-artifact <路径> --scope "<问题摘要>"`。只重跑受影响镜头，不整集重来。

## 严重度（定级 + 容错铁律）

| 级别 | 含 | 处置 |
|---|---|---|
| 🔴 阻断级 | 崩脸/角色断层、字幕错位或占位未精修、配音占位未替换、双人声打架、合规未授权克隆 | **必改**，回源头重跑 |
| 🟡 建议级 | 场景轻漂、clip 接缝跳切/闪烁、构图/景别违 `分镜语法`、节奏塌/钩子弱/集尾不够、字幕溢出、卡点没对上爽点时间戳 | 建议改 |
| 🟢 润色级 | 个别动态细节弱、留白差一拍、音效偏好 | 可改可不改 |

> **生图模型/渠道不一致单独提级**：生产前发现设置/prompt 口径混用多个官方/已登录模型或渠道，或出现 `同视频AI` 含糊口径、第三方逆向/web 自动化出图口径，按 🔴 阻断处理；成片后才发现，按画面结果定级，但报告必须写清"疑似因生图模型/渠道混用造成一致性税"，修法是回 `n2d-image` 统一到同一个官方组合并重出受影响定妆/分镜。

> **双人同框 × 无持久主体后端 硬阻断（脸漂真凶工程化）**：`image_preflight` gate 在「生图AI=无原生主体锁的后端（persistent_subject=False，如 Codex/OpenAI/Dreamina/Nano/Gemini）」时，对任一 ≥2 具名角色同框，若只写普通多参考、多图参考或锚点句，一律 **升 🔴 BLOCK**：普通多参考不是持久角色 ID，每镜仍会重新解脸；多人同框再缺硬位置/主体约束时尤其容易串脸。逃生门只有两类：① 本镜显式登记「分别出图+合成/单人分层出图」降级；② 项目统一切到支持原生主体/角色 ID 的官方后端后重跑 gate。Seedream/可灵/Sora 等有原生主体能力的后端**2026-06 起同样升 🔴 BLOCK**：同框 ≥2 具名角色若缺 `多人同框执行策略`（native_subject_slots / 区域绑定 / 分别出图+合成）或缺 `多人同框身份槽位`（逐主体绑 LEFT/RIGHT/FOREGROUND/BACKGROUND 槽位 + 各自参考），一律不放行——多主体后端虽有主体绑定能力，但无显式空间槽位时仍会把参考身份混到错位或把两张脸平均成一张（空间绑定是同框一致性硬约束，不再降级为建议）。逃生门：本镜写齐执行策略 + 逐主体身份槽位即放行。能力判定走契约 `persistent_subject`，不 hardcode 后端名。`若多主体仍不稳再分别出图` 这类条件式兜底不算降级登记；多人同框若出现 `参考图①/reference image 1` 泛化身份锁，按 Codex 锁脸 BLOCK，因为它会把第一张参考误当成所有主体身份锚。**多人「近景」同框另加硬闸（对所有后端）**：近景是单帧多主体 cross-attention 串脸最高发档（2026 SOTA：即便最强后端 ~85% 一致、近景更糟，公认最佳实践是「分开生成再合成」而非单帧绑定）——单帧身份槽位（一次生成多张脸）治不了，必须声明 `分别出图/分层合成/单人分层/景别分层` 等**真分开生成**的执行 token 才放行；只在视线对位/身份锁定里**捎带写「反打/正反打」字样不算**（真反打拆成单人镜、本就不该是同框）。

> **Codex 近景/特效锁脸硬阻断**：同一套 `image_preflight` gate 还会对无持久主体后端硬卡两类高风险镜：① 近景/反打/大表情角色镜缺同源脸部特写或 expressions 表情库参考；② 暗光、黑烟、烟雾、VFX 叠脸时缺「眼鼻嘴三角区清晰 / 特效不遮脸 / 不重画五官」约束。修法回 `n2d-image` 补逐镜 prompt，而不是等出完再靠审片发现。

**容错铁律**：只报"真问题"。轻微主观偏好不入报告——否则噪声淹没硬伤。

---

# 模式②：流程自审（让产线自我优化）

把"我这次手动做的 n2d 复盘"固化成可复跑流程。**节律**：用户主动要 / 每隔一批集 / 接了新视频·图·配音模型时跑一次。详细步骤见 `references/self_audit.md`，要点：

0. **本地静态自审**：先跑 `python3 skills/n2d-review/scripts/self_audit.py [--json]`。它不联网、不改文件，检查 `_进度.md` 并发安全、gate 单入口、横切覆盖率、行业基准外置、生图后端白名单文档一致性、文档体量和 **detector 治理清单**（内嵌 `detector_inventory.py`：枚举 ~40 个一致性 detector × 消费者矩阵[编排/gate/总账/子检测]，孤儿 detector 报 warn——治"加固多轮累积的死 detector/漏接线"，可单独跑 `python3 skills/n2d-review/scripts/detector_inventory.py [--json|--strict]`）；0 block / 0 warn 才进入市场对标。
   > 注：`self_audit.py` 是 **n2d 线特有的产线治理脚本**（检查的是 n2d 流水线专属约束）。
0.5. **并入现场摩擦信号（自我优化闭环）**：跑 `self_audit.py --work <作品根>` 时，会把该作品 `生产数据/优化信号.jsonl` 里**生产时各 skill 现场上报的「这个 skill 该改」信号**逐簇并进差距清单（loc=该改哪个 skill、severity 透传、block 让自审退 1）。信号由 `skills/n2d/_lib/n2d_friction.py` 的 `log_friction()` 在各 skill 撞到缺陷/被迫降级时一行写入（已接 `n2d-voice` 占位降级；其它 skill 按需照抄一行）。**只读不写**——信号是 per-work 生产数据不是自审报告，不破坏下面 report-only 立场。埋点/消费/落点详见 `references/friction_log.md`。
1. **拉基准**：联网搜当前（带年月）AI 漫剧/短剧主流做法，分三轴取证——**一致性**（定妆/参考/相似度 KPI、多参考/多视图/LoRA、同一生图后端贯穿）、**效率**（成本/周期/批量）、**可控性**（口型/音画/节奏工具）+ 各 stage 模型演进（图/视频/配音 SOTA）。
2. **对照**：逐 stage 把基准 vs `n2d-*/SKILL.md` + `n2d/Q&A.md` 比，找**真差距**（已做的别重复立项）。
3. **差距清单**：每条 = 差距 + 证据（带来源链接·日期）+ 落到哪个 skill 哪段 + 优先级（must/optional）+ 是否可脚本化。
4. **起草**：高价值项直接起草 `Q&A` 新条目 + 建议 edit；**改任何 skill 必同步 `skills/README.md` 索引**（仓库硬约定）。
5. **人确认后再写**：模式②**默认 report-only**，只产建议报告 + 可选 diff 草案，不自动改 skill / Q&A / 模型矩阵（改产线是高影响动作）。用户明确要求“落地/刷新矩阵/改 skill”后，才进入 `refresh-matrix` 或编辑模式。**报告是一次性的——只讲给用户、不在 skill 目录留存 `_流程自审_*.md` 这类存档**（已 gitignore）。**每次自审/重审都从头按本流程重跑**（拉基准→对照→差距），**绝不读旧报告当捷径**——市场会变，旧结论可能已过时或已落地。

> **防过期铁律**：市场建议带"采集日期 + 来源链接"，旧建议可能已被采纳或过时——写进来前先核对当前 skill 是否已有（本 skill 自己也按此自查）。

---

## 详细参考
- 作品质检两层维度全清单（看什么 + 定级 + 怎么判）：`references/checklist.md`
- 流程自审操作手册（拉基准 / 对照 / 起草）：`references/self_audit.md`
- 现场摩擦信号（自我优化闭环：各 skill 生产时上报 → 流程自审消费）：`references/friction_log.md`
- 本机 MLX-VLM 后端模板（`N2D_VLM_CMD` 单图设定核对 / `N2D_APPEARANCE_BATCH_CMD` 两图外观判官）：`references/vlm_backend.md`
- P0/P1/P2 算法一致性：`scripts/semantic_continuity.py`（语义谱系 Diff） / `scripts/state_continuity.py`（动态百科·状态哨兵） / `scripts/state_ledger_build.py`（从 storyboard 构建跨集视觉状态账本） / `scripts/multimodal_consistency.py`（视觉语义/道具 embedding 离群）
- 各轴 SOTA vs n2d 默认 vs 升级触发（防过期快照；report-only 只给刷新建议，用户确认后再改）：`n2d/references/模型矩阵.md`
- 定妆变更影响扫描（崩脸/换装重抽后，列出引用该资产的下游镜头一并重出）：`n2d-image/scripts/asset_impact.py`
- 正向标准（镜头空间 / 时间留存）：`n2d-script/references/分镜语法.md` + `n2d/references/导演节奏.md`
- 角色 DNA 全链：`n2d-image/references/角色一致性checklist.md`
- 角色身份闭环 + 跨集漂移报表：`n2d-identity/SKILL.md`
- 一致性验收总账与例外/传播：`scripts/consistency_ledger.py`（单集）/ `scripts/series_ledger.py`（剧级上卷·整季签收闸）/ `scripts/consistency_dependency_graph.py` / `scripts/intentional_discontinuity.py` / `scripts/motion_grammar_consistency.py` / `scripts/audio_space_consistency.py` / `scripts/expression_state_consistency.py`
- 翻车修正沉淀：`n2d/Q&A.md`

## 常见错误

| 错误 | 纠正 |
|---|---|
| 只跑机检不做人判 | 机检只覆盖确定性问题；崩脸/构图/节奏体感要 LLM 判（含并排读图比对） |
| 只人判不跑机检 | 字幕错位/占位/对账这类秒查，漏跑等于白审 |
| 鸡蛋里挑骨头堆润色项 | 违容错铁律，硬伤被淹没 |
| 报问题不定位不给修法 | 必须 镜头+时间码定位 + 指明回哪个 stage 重跑 |
| 在成片 MP4 上直接剪 | 回源头改重跑回流（同 Q27）；成片是产物不是源 |
| 模式②直接改 skill | 默认只出建议报告，人确认后改；改 skill 必同步 README 索引 |
| 模式②抄一堆已实现的"建议" | 先核对当前 skill 是否已有；建议带来源日期防过期 |
| 审完没有机器分 | 跑 `n2d-score`，让评分维度、visual checks 和回流 stage 进入 `生产数据/score_第N集.json` |
| 批量审片只看文本报告 | 跑 `n2d-review-ui` 生成 `review_ui_第N集.html/json`，用画布同时看首尾帧、clip、接缝、定妆参考、QA flag 和机器分 |
| 合规等成片后补救 | 先跑 `n2d-compliance` 建 `合规/compliance_manifest.json`，gate 会在 image/video/compose/review 前置阻断 |
| 把有意跳切/越轴/变调当 bug，或把 bug 说成导演选择 | 先写 `intentional_discontinuity_第N集.json`，带 clip、dimension、reason、reviewer、expires_at；没有结构化签收就仍按一致性问题处理 |
