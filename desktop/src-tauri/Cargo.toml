[package]
name = "anime-armory-desktop"
version = "0.1.0"
description = "Tauri desktop shell for the anime-arsenal creation factory"
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
portable-pty = "0.8"
base64 = "0.22"
tiny_http = "0.12"
notify = "6"
dirs = "5"      # cross-platform home dir (HOME / USERPROFILE)
trash = "5"     # cross-platform Trash / Recycle Bin

[[bin]]
name = "anime-armory-desktop"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
