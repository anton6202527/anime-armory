This dir is the desktop app's bundled skills/demos copy, generated by
`../sync-skills.cjs` and shipped via tauri.conf.json
`bundle.resources`. Its contents are gitignored — only this .keep is tracked,
so the resources glob always matches at least one file before bundle sync runs.
