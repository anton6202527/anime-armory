# n2d 跨项目资产库

这里存放可跨项目复用的本地资产包：

- `characters/`：角色原型、定妆组、`identity_registry` 片段
- `scenes/`：场景定妆模板
- `props/`：道具 / 法宝 / 特效模板
- `templates/model_routes/`：视频模型路由经验模板

使用时不需要记命令。对 agent 说“查资产库”“导入某模板为某角色”即可；内部流程见 `skills/n2d-asset-market/SKILL.md`。
