# TODO

## 把本库克隆为「干净的新项目模板」时（务必先做）

本库是**私有创作库**：既有通用 skill，也有个人创作成品和偏好，用于**多台电脑同步接着干活**。
若哪天要拿它当干净模板去开一个新项目，下面这些是**个人内容，要留在本地、不可提交**：

- [ ] 个人创作产物（成品文件夹，剥离/不提交）：
  - `写歌/`、`制MV/`、`制漫剧/`、`写小说/` 下的各作品文件夹（歌/MV/小说/漫剧成品，含音视频图片二进制）
- [ ] 私有偏好选择：各作品根下的 `_设置.md`
  - 注：全局默认 `创作偏好-默认.md` 本就在 `~/.claude/.../memory/` 私有区、不在本库，无需处理
- [ ] 可选：把上述路径加进 `.gitignore`，避免在模板里再次误传

**要带走的（模板的真正内容）**：`.claude/skills/`（通用 skill 全家族 + `_偏好约定.md`）。

> 现状（私有同步阶段）：以上个人内容**照常提交**，方便多机同步；本清单仅在「转为公开/共享模板」时才执行。
