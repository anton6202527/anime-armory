# TODO

## 把本库剥离成「干净的工具模板」时（可选）

本库**已公开**，仓库内的现有作品是 **demo 演示**，照常保留。
如果你（或 fork 的人）只想要这套通用 skill、**不带 demo 作品和个人偏好**，按下面剥离成干净模板：

- [ ] 删 demo 作品（成品文件夹）：
  - `写歌/`、`制MV/`、`制漫剧/`、`写小说/` 下的各作品文件夹（歌/MV/小说/漫剧成品，含音视频图片二进制）
- [ ] 删个人偏好：
  - 各作品根下的 `_设置.md`
  - 全局默认 `.claude/创作偏好-默认.md`（换成一份中性默认或留空）
- [ ] 可选：把上述路径加进 `.gitignore`，避免再次误传

**要带走的（模板的真正内容）**：`.claude/skills/`（通用 skill 全家族 + `_偏好约定.md`）。
