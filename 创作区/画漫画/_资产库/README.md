# 画漫画系列资产库

本目录只服务 `画漫画` 生产线，保存可在本系列作品之间复用的自包含角色、场景、道具、版式或视觉模板包。

跨系列、跨仓库或跨机器时只复制所需的单个 asset pack；目标系列导入后拥有自己的副本并自行适配，不得建立回指来源目录的长期依赖。

它不是各作品 `identity_registry.json` 的镜像，也不自动为每个项目生成一份。作品自己的故事与视觉规则放 `设定库/`，机器身份真值与参考图放 `出图/共享/`；只有经过整理、确实值得跨作品复用的独立包才进入这里。
