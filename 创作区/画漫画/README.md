# 创作区/画漫画

这里放漫画项目。每个项目一个子目录，通常由 `comic` 初始化：

```bash
python3 skills/comic/scripts/init_project.py "作品名"
```

轻量 MVP 的默认结构是：企划与设定、分话大纲、格脚本、页面排版、出图包、嵌字合成和审查记录。小说可以作为源本，但不是硬前置；原创梗概、现成脚本、漫剧脚本也可以进入同一套漫画生产流。
