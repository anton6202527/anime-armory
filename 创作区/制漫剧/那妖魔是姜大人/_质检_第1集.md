# 第1集质检报告

生成时间：2026-07-07 12:56（Asia/Shanghai）

本报告为本轮上游返工后的刷新汇总：`script_contract_applied` 收据、Codex/seedance/dreamina 后端证据、视频总览 `style_anchor`、S2V/fidelity/scene/resident/MOT1/节奏密度证据均已处理。本轮没有重复跑正式 `n2d-review` 验收；`_进度.md` 的 `验收` 仍保持 ⬜。

## 结论

第1集当前 demo/internal 结论：**无硬阻断，内部样片可进入人工预览**。正式验收或 production/公开投放仍有 warning/debt，不能等同于公开发布通过。

| 产物 | 结论 |
|---|---|
| `生产数据/release_verdict_第1集.json` | demo `internal-only`；`blocking_reasons=[]` |
| `生产数据/score_第1集.json` | pass；91/85 |
| `生产数据/gate_findings_review_第1集.json` | block 0 / warn 37 / info 5 |
| `生产数据/consistency_ledger_第1集.json` | pass；block=0 / high=0 / medium=27 |
| `生产数据/review_ui_findings_第1集.json` | block 0 / warn 92 / info 61 |
| `生产数据/failure_taxonomy_第1集.json` | production profile 仍 blocked；625 findings，升级 block 301 |
| `生产数据/generation_recipe_第1集.json` | pass；85 records，findings=0 |
| `生产数据/generation_recipe_manifest_第1集.json` | pass；当前 RCP/RCP2 不再阻断 |
| `生产数据/production_breakdown_check_第1集.json` | pass；required=3 / pass=3 / block=0 |

## 本轮已解决

| 项 | 结果 |
|---|---|
| CHAR_01 多视角身份证据 | 已补 `设定库/identity_eval_pack.json`；MVIEW 检查 findings=0。 |
| CHAR_01 角色资产包分区 | 已补 `设定库/character_assets/CHAR_01__姜月初/` 下 reference/prompts/lora/voice/adapters/qc 的索引或状态文件，并更新 `manifest.json`。 |
| 未登记 VFX | 已补 `VFX_百妖谱金光` 的 asset registry 条目与 `设定库/vfx_contracts/VFX_百妖谱金光.md`。 |
| 10 条 split 视频 generation_event / recipe | 已通过路径归一化恢复旧 production event：`Clip_02/03/05/06/07` 的 part1/part2 均追到 `production_events.jsonl:line 419-429`；`generation_recipe_manifest=pass`。 |
| production_breakdown | `production_breakdown_check_第1集.json=pass`，本轮刷新时间 2026-07-07T04:09:23Z。 |
| 角色 DNA/形体 review-ui block | 已刷新为 `block=0 / warn=2 / info=15`；旧 23 条 block 中，`block=0 warn=0` 的证据已按 info 处理，6 条角色资产包分区旧 block 已从 score/review-ui 清除。 |
| full image_qc | 已用 `/opt/homebrew/Caskroom/miniforge/base/envs/facefusion/bin/python` 跑 full 精度；`degraded=false`、`hard_blocks=0`、`face/hair/outfit ok=31`。 |
| RCP/RCP2 强配方 | 已补 `storyboard.json`、`voice_zh.wav`、`成片_第1集_zh.mp4` 的生产事件与强配方字段；`generation_recipe_第1集.json` findings=0。 |
| review-ui skill 误判 | 已修 `flatten_evidence`：带 `block=N warn=N` 的单条证据按自身计数定级，不再继承维度 fail；回归测试通过。 |
| script_contract_applied 收据 | 已补 `出图/第1集/prompt/01_分镜出图.md` 与 `出视频/第1集/prompt/01_clips.md` 两个 scope，`script_contract_verify` all scope 通过。 |
| 后端能力证据 | 已刷新 `codex.json`、`seedance__via_dreamina.json`、`dreamina__via_dreamina.json`；本机证据含 `codex-cli 0.142.5`、`image_generation stable=true`、`dreamina c58a6a2-dirty` 与 seedance/dreamina CLI 能力快照。 |
| 视频总览 style_anchor | `出视频/第1集/prompt/00_总览.md` 已继承 `storyboard.json style_contract.style_anchor`：`出图/共享/图片/风格锚_冷灰写实3D国风漫剧.png`。 |
| S2V / fidelity / scene / resident | 已刷新 `scene_embed_第1集.json`、`resident_presence_第1集.json`、`vlm_canonical_第1集.json`；DINOv2/VLM/resident detector 本机未启用，按 degraded/签收路径保留为 production 债务。 |
| MOT1 / 节奏密度 | 已刷新 `spectacle_video_qc_第1集.json`、`motion_quality_第1集.json`、`camera_trajectory_probe_第1集.json`、`subject_video_consistency_第1集.json`；`consistency_advisory_signoff_第1集.json` 已补 MOT1 与节奏密度签收，有效期至 2026-08-07。 |

## 仍然保留的 production 债务

| 严重度 | 位置/证据 | 问题 | 下一步 |
|---|---|---|---|
| warn / production block | `生产数据/release_verdict_第1集.json` / `contract_trace` | 源理解 trace_id 未完全贯通；demo/internal 可继续，production 不能只靠 confirmed。 | 回 `n2d-script` 贯通 source trace 到 contract、storyboard、prompt/recipe、final artifact。 |
| warn | `生产数据/release_verdict_第1集.json` / `compliance` | 当前发行意图为 internal_only，仍有字段级合规发布待办。 | 若公开/投放，补 AI 标识、授权/发行意图与平台合规证据。 |
| warn | `生产数据/gate_findings_*_第1集.json`、`review_ui_findings_第1集.json` | gates 合计仍有大量 warn；review-ui 为 warn=92，主要是节奏、场景/构图、成片包装、音画同步等。 | 内部样片可人工预览；正式发布前按 warning 分派回上游。 |
| production block | `生产数据/failure_taxonomy_第1集.json` | production profile 仍 blocked；625 findings，升级 block 301。 | 对 production profile 按 return_plan 分批返工，避免把 demo 证据直接当公开发布证据。 |
| warn / waiver | `scene_embed_第1集.json`、`resident_presence_第1集.json`、`vlm_canonical_第1集.json` | 本机缺 DINOv2/VLM/resident detector 等重型后端，当前靠 degraded/签收放行。 | 装配重型后端或提供外部 QC 结果后复跑；否则仅适合内部 demo。 |

## 本轮 skill 修复

- `skills/n2d/scripts/generation_recipe_manifest.py`：支持项目迁移后的旧绝对路径、带作品名前缀的相对路径恢复为项目内相对路径；新增回归测试。
- `skills/n2d-review/scripts/gate_core.py`：同样支持 review gate 的 production event 路径归一化，避免旧工作区路径误报缺 generation_event；新增 gate 回归测试。
- `skills/n2d-review-ui/scripts/review_ui.py`：修复证据展开定级，避免 `block=0 warn=0` 的证据被维度级 fail 误标为 block；新增回归测试。
- `skills/n2d-video/scripts/prompt_pack.py`：视频 prompt 总览缺失或使用泛化 `style_anchor` 时，自动从 storyboard 的 `style_contract.style_anchor` 继承。
- `skills/n2d-video/scripts/script_contract_receipt.py`：出图/出视频 scope 写入不同证据文案，避免出图收据误写“视频 prompt”。
- `skills/n2d-dashboard/scripts/dashboard.py`：新增 `N2D_IMAGE_QC_PYTHON`，避免 gate 子进程用系统 Python 覆盖 full InsightFace QC。
- 延续上一轮修复：split 逻辑镜头统计、score 假 `Clip_16`、compose split 缓存命名、pilot 证据校验都已覆盖测试。

## 当前最具体下一步

不要重复跑本轮证据刷新。若目标是内部 demo，下一步进入人工预览/抽样复核并决定是否交付样片；若目标是正式验收或 production/公开投放，先回 `n2d-script` 贯通 source trace，再补合规 AI 标识、重型 VLM/DINOv2/OWLv2/SyncNet/scene_embed/resident_presence 后端证据、音画同步/最终时间线/UI 资产/成本路由等 production warning，之后再跑正式 `n2d-review`。
