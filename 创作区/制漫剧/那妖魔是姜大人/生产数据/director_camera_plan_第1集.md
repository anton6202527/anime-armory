# 导演运镜审查与 Prompt 落地 - 第1集

- Clip 数：11
- WARN：11
- 缺运镜：1
- 自由散文运镜：8

> 用法：把每个 Clip 的「出图注入」抄进 `01_分镜出图.md` 对应正向 prompt，把「视频注入」抄进 `01_clips.md` 的导演调度七字段/镜头运动段。若与 storyboard 既有剧情状态冲突，以 storyboard 为准并人工改写。

## EP01_CLIP01
- 景别：ECU→ELS/LS
- 节奏：铺垫·长镜
- 原运镜：未声明
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_missing：storyboard 未声明镜头运动；出图/出视频容易退化成静态插画或随机漂浮。

出图注入：
```text
镜头/机位：ECU→ELS/LS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在ECU→ELS/LS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP02
- 景别：MCU→LS
- 节奏：铺垫·长镜
- 原运镜：先 MCU 锁姜月初囚服与脸，再 eyeline cut 到异界尸场，不做旋涡穿越闪回。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MCU→LS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MCU→LS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP03
- 景别：CU/MS 正反打
- 节奏：加速·碎切
- 原运镜：正反打守姜月初↔裴长青横轴，过肩只从姜月初肩后向画右拍。
- 建议运镜：轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU
- 后端控制写法：natural_language（source=no_video_route）
- 理由：大表情近景最怕脸被运动重画；轻微推近比环绕/甩镜更稳，也能把情绪怼近。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU/MS 正反打；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：大表情近景最怕脸被运动重画；轻微推近比环绕/甩镜更稳，也能把情绪怼近。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU/MCU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU
后端控制写法：自然语言运镜：轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP04
- 景别：MS→LS
- 节奏：铺垫·长镜
- 原运镜：MS 跟拍到 LS 留空间，清晰双脸同框不稳时按 character_slots 分层合成。
- 建议运镜：匀速移镜头，横移跟随主体，保持轴线方向不反转，落到MS→LS
- 后端控制写法：natural_language（source=no_video_route）
- 理由：动作过程需要空间方向清楚；匀速移镜比自由漂浮更容易守轴线和人物站位。
- Findings：
  - [warn] camera_speed_missing：已声明运镜但缺速度档；补缓慢/匀速/快速/轻微/急速冲击，模型才知道节奏强度。

出图注入：
```text
镜头/机位：MS→LS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「匀速移镜头，横移跟随主体，保持轴线方向不反转，落到MS→LS」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：动作过程需要空间方向清楚；匀速移镜比自由漂浮更容易守轴线和人物站位。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS→LS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：匀速移镜头，横移跟随主体，保持轴线方向不反转，落到MS→LS
后端控制写法：自然语言运镜：匀速移镜头，横移跟随主体，保持轴线方向不反转，落到MS→LS；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP05
- 景别：CU→LS低机位→CU
- 节奏：揭示·反应链
- 原运镜：先给姜月初/裴长青反应CU，再低机位拍虎妖复苏，最后反打裴长青骇然。
- 建议运镜：轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU
- 后端控制写法：natural_language（source=no_video_route）
- 理由：大表情近景最怕脸被运动重画；轻微推近比环绕/甩镜更稳，也能把情绪怼近。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU→LS低机位→CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：大表情近景最怕脸被运动重画；轻微推近比环绕/甩镜更稳，也能把情绪怼近。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU/MCU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU
后端控制写法：自然语言运镜：轻微推镜头，沿视线轴轻推，最后稳定停住，落到CU/MCU；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP06
- 景别：MS→CU命中→MS低机位手部/横刀反应
- 节奏：加速·碎切
- 原运镜：MS起手→CU命中帧→MS低机位落刀/手部反应；不环绕，不越轴，命中帧短促快推，不推成姜月初正脸近景。
- 建议运镜：命中瞬间短促快推，落地后低幅震动并停在横刀、姜月初手背/衣袖和侧背轮廓
- 后端控制写法：natural_language（source=no_video_route）
- 理由：本镜尾段不需要姜月初正脸近景；用横刀滑近、手部/衣袖僵住和侧背轮廓表达惊恐，避免小脸/侧背被视频模型升格成新脸。
- Findings：
  - [warn] camera_speed_missing：已声明运镜但缺速度档；补缓慢/匀速/快速/轻微/急速冲击，模型才知道节奏强度。

出图注入：
```text
镜头/机位：MS→CU命中→MS低机位手部/横刀反应；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「命中瞬间短促快推，落地后停在横刀、姜月初手背/衣袖和侧背轮廓」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间；不生成姜月初清晰正脸。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：本镜尾段不需要姜月初正脸近景；用横刀滑近、手部/衣袖僵住和侧背轮廓表达惊恐，避免小脸/侧背被视频模型升格成新脸。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS低机位手部/横刀反应；姜月初只露手背、衣袖或侧背/OTS轮廓，禁止清晰正脸近景。
镜头运动：命中瞬间短促快推，落地后低幅震动并停在横刀、姜月初手背/衣袖和侧背轮廓
后端控制写法：自然语言运镜：命中瞬间短促快推，落地后低幅震动并停在横刀、姜月初手背/衣袖和侧背轮廓；首帧锚定，不改变角色、光位、轴线和场景设定，不生成姜月初清晰正脸。
运动精修：张力=释放；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP07
- 景别：CU→POV
- 节奏：爽点·CU硬切
- 原运镜：POV/过肩，面板正对屏幕，不透视变形，不随镜头漂移。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU→POV；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU→POV，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP08
- 景别：POV/OTS→CU手部
- 节奏：铺垫·长镜
- 原运镜：过肩/主观镜，面板和刀柄同框时保持面板不变形。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：POV/OTS→CU手部；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在POV/OTS→CU手部，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP09
- 景别：LS→CU
- 节奏：加速·碎切
- 原运镜：远景压迫 + 姜月初CU + 裴长青低角度反应，不越轴。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：LS→CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS→CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP10
- 景别：ECU/CU
- 节奏：爽点·CU硬切
- 原运镜：ECU嘴唇/眼神→手部特写→裴长青瞳孔CU；不拍猎奇血腥正面。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：ECU/CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在ECU/CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP11
- 景别：CU 定格
- 节奏：留白·定格
- 原运镜：CU 缓推姜月初，不切换清晰三脸；裴长青和虎妖作为前景/背景状态锚。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU 定格；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU 定格，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```
