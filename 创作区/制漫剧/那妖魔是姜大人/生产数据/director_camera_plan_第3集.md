# 导演运镜审查与 Prompt 落地 - 第3集

- Clip 数：10
- WARN：9
- 缺运镜：4
- 自由散文运镜：5

> 用法：把每个 Clip 的「出图注入」抄进 `01_分镜出图.md` 对应正向 prompt，把「视频注入」抄进 `01_clips.md` 的导演调度七字段/镜头运动段。若与 storyboard 既有剧情状态冲突，以 storyboard 为准并人工改写。

## EP03_CLIP01
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：冷开钩子
- 原运镜：未声明
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_missing：storyboard 未声明镜头运动；出图/出视频容易退化成静态插画或随机漂浮。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP02
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：生存压力
- 原运镜：未声明
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_missing：storyboard 未声明镜头运动；出图/出视频容易退化成静态插画或随机漂浮。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP03
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：身份转身
- 原运镜：未声明
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_missing：storyboard 未声明镜头运动；出图/出视频容易退化成静态插画或随机漂浮。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=紧张；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP04
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：信息钩子
- 原运镜：未声明
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_missing：storyboard 未声明镜头运动；出图/出视频容易退化成静态插画或随机漂浮。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=紧张；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP05
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：外部势力入场
- 原运镜：固定远景到中景轻推，速度由火把变亮、蹄尘和缰绳停顿表现。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP06
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：误认爽点
- 原运镜：MCU/CU 反打，不越轴；姜月初外冷内慌，陈青源恭敬。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=紧张；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP07
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：责任反噬
- 原运镜：MS 到 LS 轻拉开，展示高低差和群体压力。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP08
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：危机加码
- 原运镜：陈青源 MCU 说明事实，姜月初 CU 吸收压力；不插入完整怪物战斗。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP09
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：选择困局
- 原运镜：CU 静压为主，火把影子压到黑衣赤纹。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=紧张；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP03_CLIP10
- 景别：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。
- 节奏：集尾钩子
- 原运镜：反打到硬切，尾句后留半拍黑场。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在LS/MS/MCU/CU 阶梯推进，关键反应落 CU；不连续三镜同景别。，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```
