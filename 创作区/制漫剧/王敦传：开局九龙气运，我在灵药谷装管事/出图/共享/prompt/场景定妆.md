# 场景定妆

## LOC_QI_PRISON_L7（`LOC_QI_PRISON_L7`）
**目标存档**：`出图/共享/图片/定妆_场景_LOC_QI_PRISON_L7.png`
**场景注册**：`asset_registry.json` -> `LOC_QI_PRISON_L7`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
LOC_QI_PRISON_L7
### 正向 prompt（英文）
LOC_QI_PRISON_L7 production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## LOC_WASTE_DITCH_BLACK_RIVER（`LOC_WASTE_DITCH_BLACK_RIVER`）
**目标存档**：`出图/共享/图片/定妆_场景_LOC_WASTE_DITCH_BLACK_RIVER.png`
**场景注册**：`asset_registry.json` -> `LOC_WASTE_DITCH_BLACK_RIVER`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
LOC_WASTE_DITCH_BLACK_RIVER
### 正向 prompt（英文）
LOC_WASTE_DITCH_BLACK_RIVER production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## LOC_QI_CITY_LOCKDOWN（`LOC_QI_CITY_LOCKDOWN`）
**目标存档**：`出图/共享/图片/定妆_场景_LOC_QI_CITY_LOCKDOWN.png`
**场景注册**：`asset_registry.json` -> `LOC_QI_CITY_LOCKDOWN`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
LOC_QI_CITY_LOCKDOWN
### 正向 prompt（英文）
LOC_QI_CITY_LOCKDOWN production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## LOC_BLACK_RIVER_EXIT（`LOC_BLACK_RIVER_EXIT`）
**目标存档**：`出图/共享/图片/定妆_场景_LOC_BLACK_RIVER_EXIT.png`
**场景注册**：`asset_registry.json` -> `LOC_BLACK_RIVER_EXIT`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
LOC_BLACK_RIVER_EXIT
### 正向 prompt（英文）
LOC_BLACK_RIVER_EXIT production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## LOC_LINGYAO_VALLEY（`LOC_LINGYAO_VALLEY`）
**目标存档**：`出图/共享/图片/定妆_场景_LOC_LINGYAO_VALLEY.png`
**场景注册**：`asset_registry.json` -> `LOC_LINGYAO_VALLEY`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
LOC_LINGYAO_VALLEY
### 正向 prompt（英文）
LOC_LINGYAO_VALLEY production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## LOC_LINGYAO_VALLEY_GATE（`LOC_LINGYAO_VALLEY_GATE`）
**目标存档**：`出图/共享/图片/定妆_场景_LOC_LINGYAO_VALLEY_GATE.png`
**场景注册**：`asset_registry.json` -> `LOC_LINGYAO_VALLEY_GATE`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
LOC_LINGYAO_VALLEY_GATE
### 正向 prompt（英文）
LOC_LINGYAO_VALLEY_GATE production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

