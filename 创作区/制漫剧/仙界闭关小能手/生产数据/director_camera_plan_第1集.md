# 导演运镜审查与 Prompt 落地 - 第1集

- Clip 数：25
- WARN：23
- 缺运镜：0
- 自由散文运镜：22

> 用法：把每个 Clip 的「出图注入」抄进 `01_分镜出图.md` 对应正向 prompt，把「视频注入」抄进 `01_clips.md` 的导演调度七字段/镜头运动段。若与 storyboard 既有剧情状态冲突，以 storyboard 为准并人工改写。

## EP01_CLIP01
- 景别：ELS→WS
- 节奏：冷开场·首屏压迫
- 原运镜：从大殿全景慢推到人物压迫关系，守张老大→贺平生横轴。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：ELS→WS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在ELS→WS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP02
- 景别：MS/CU
- 节奏：审问·反打起势
- 原运镜：反打不换轴，张老大视线始终向画左下。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS/CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS/CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP03
- 景别：CU
- 节奏：审问·低头承压
- 原运镜：接上张老大反打，贺平生视线向画右上后迅速垂下。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP04
- 景别：MS
- 节奏：审问·压缩追问
- 原运镜：短反打，保持横轴和低冷光。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP05
- 景别：CU
- 节奏：审问·羞辱触发
- 原运镜：用微表情完成回答，不新增玄幻强光。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP06
- 景别：CU碎切
- 节奏：审问·群嘲碎切
- 原运镜：碎切但不跳轴，笑声作声桥接下个解释镜。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU碎切；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU碎切，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP07
- 景别：ECU/抽象
- 节奏：信息增量·灵根代价
- 原运镜：快闪后回到大殿冷调，光效小而短。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：ECU/抽象；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在ECU/抽象，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP08
- 景别：WS
- 节奏：信息增量·无人愿收
- 原运镜：回忆镜头去饱和，切回大殿前不解析长老正脸。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：WS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在WS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP09
- 景别：MS/CU
- 节奏：命令·重压落下
- 原运镜：手部插入镜避免多人接触漂移，仍守横轴。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS/CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS/CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=释放；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP10
- 景别：CU
- 节奏：命令·短反应
- 原运镜：短反应后硬切前情快闪。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP11
- 景别：CU/WS碎片
- 节奏：前情·碎片快闪
- 原运镜：三段碎片各短切，避免单镜长解释拖慢。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU/WS碎片；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU/WS碎片，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP12
- 景别：MS
- 节奏：前情·托付收束
- 原运镜：中景静拍，不解析江剑正脸，接下一镜抬头看仙人。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP13
- 景别：WS→MS
- 节奏：前情·主角欲望
- 原运镜：抬头跟随但不做高速飞行镜，重点在主角选择。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_speed_missing：已声明运镜但缺速度档；补缓慢/匀速/快速/轻微/急速冲击，模型才知道节奏强度。

出图注入：
```text
镜头/机位：WS→MS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在WS→MS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP14
- 景别：WS
- 节奏：现实压迫·任务落地
- 原运镜：固定广角建立比例，不环绕，不交换左右站位。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。

出图注入：
```text
镜头/机位：WS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在WS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP15
- 景别：MS→WS
- 节奏：现实压迫·比例落点
- 原运镜：低角度压迫，切出前保持水缸纵深轴。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS→WS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS→WS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP16
- 景别：CU→MS
- 节奏：现实压迫·安置冷淡
- 原运镜：动作切连接空屋，不做复杂拉扯。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU→MS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU→MS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP17
- 景别：WS/CU
- 节奏：现实压迫·贫瘠空镜
- 原运镜：慢移扫过物件，保持月光画右后。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：WS/CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在WS/CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP18
- 景别：MS
- 节奏：主动选择·行动前置
- 原运镜：短自语不需要反打，保持人物低声克制。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=克制；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP19
- 景别：MS/CU碎切
- 节奏：加速·体力消耗
- 原运镜：碎切加速但不跳空间方向，前景树枝作自然擦切。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS/CU碎切；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS/CU碎切，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP20
- 景别：MS→CU
- 节奏：异常前奏·疲惫到钩子
- 原运镜：低机位慢压到水面，微光不要变强。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS→CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS→CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP21
- 景别：CU
- 节奏：异常·反应停顿
- 原运镜：一拍停顿，不拉长，不让微光抢全镜。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP22
- 景别：ECU
- 节奏：异常·道具揭示
- 原运镜：固定水下特写，水纹轻动，保持月光冷蓝。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。

出图注入：
```text
镜头/机位：ECU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在ECU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP23
- 景别：MS→CU
- 节奏：异常·误判松气
- 原运镜：动作切清楚交代捞起，破盆形态保持旧黑陶。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS→CU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS→CU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP24
- 景别：MS
- 节奏：异常·务实遮掩
- 原运镜：中景转身接最终道具特写，人物不看盆底。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：MS；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在MS，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=聚焦；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```

## EP01_CLIP25
- 景别：ECU
- 节奏：留白·集尾硬断
- 原运镜：极近特写，微光只一瞬，硬断到黑。
- 建议运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
- 后端控制写法：natural_language（source=no_video_route）
- 理由：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
- Findings：
  - [warn] camera_move_unstructured：镜头运动未命中 CAMERA_MOVE_LEXICON 或固定机位词；建议改为推/拉/摇/移/升降/跟拍/固定等结构化词。

出图注入：
```text
镜头/机位：ECU；按导演运镜预留起幅，首帧不是摆拍肖像。
起幅·运动余量：为「固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动」预留前景/背景运动余量；主体不要顶边，动作方向留 15%-25% 空间。
构图防呆：角色视线锁戏内目标；非 POV 镜不看镜头；光位、轴线、角色状态继承 storyboard visual_contract。
```

视频注入：
```text
导演意图：屏幕/面板镜以可读性为第一目标，固定机位避免文字和 UI 漂移。
起幅：继承首帧构图、光位、轴线和角色状态，不重定视觉设定。
落幅：落在ECU，动作/表情在最后 0.3-0.5 秒稳定住，方便接缝。
镜头运动：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动
后端控制写法：自然语言运镜：固定机位，锁定屏幕/光幕平面，不漂移，只允许轻微呼吸式微动；首帧锚定，不改变角色、光位、轴线和场景设定。
运动精修：张力=爆发；镜头运动只服务情绪，不追加未声明的旋转、漂浮、急甩。
动态细节：人物运动、服饰/发丝/尘雾/光效按本镜动作小幅响应，背景不闪烁、不重构。
```
