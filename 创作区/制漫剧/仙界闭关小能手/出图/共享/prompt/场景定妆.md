# 场景定妆

## 贺平生杂役小屋（`LOC_ZAYI_HUT`）
**目标存档**：`出图/共享/图片/定妆_场景_贺平生杂役小屋.png`
**场景注册**：`asset_registry.json` -> `LOC_ZAYI_HUT`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
冷灰写实3D国风漫剧，低矮旧木小屋，硬板床、窗格冷光、墙角黑陶破盆、粗糙木门、几乎无家具，清晨冷灰青或深夜冷月光，贫瘠但画面可读。
### 正向 prompt（英文）
贺平生杂役小屋 production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## 后山挑水路（`LOC_HOUSHAN_WATER_PATH`）
**目标存档**：`出图/共享/图片/定妆_场景_后山挑水路.png`
**场景注册**：`asset_registry.json` -> `LOC_HOUSHAN_WATER_PATH`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
秀竹峰后山崎岖挑水小路，湿石、竹影、水桶晃动，黄昏到入夜冷蓝天光，表现十五趟挑水的身体压力。
### 正向 prompt（英文）
后山挑水路 production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## 杂役饭棚（`LOC_ZAYI_FOOD_YARD`）
**目标存档**：`出图/共享/图片/定妆_场景_杂役饭棚.png`
**场景注册**：`asset_registry.json` -> `LOC_ZAYI_FOOD_YARD`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
杂役饭棚与粗木桌，旧大锅、木碗、低饱和脏暖晨光，底层劳动空间，背景只保留杂役生活轮廓，不新增清晰无名角色脸。
### 正向 prompt（英文）
杂役饭棚 production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## 杂役院水缸区（`LOC_ZAYI_WATER_JARS`）
**目标存档**：`出图/共享/图片/定妆_场景_杂役院水缸区.png`
**场景注册**：`asset_registry.json` -> `LOC_ZAYI_WATER_JARS`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
杂役院两口巨大水缸区域，粗糙石地、水痕、低矮旧屋边缘和木桶扁担，水缸体量压迫，清晨冷灰或低饱和脏暖侧光，不新增现代水箱。
### 正向 prompt（英文）
杂役院水缸区 production environment reference, cold gray realistic 3D Chinese fantasy comic-drama style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

