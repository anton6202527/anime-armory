# 场景定妆

## / 杂役破屋（`LOC_SERVANT_HUT`）
**目标存档**：`出图/共享/图片/定妆_场景_杂役破屋.png`
**场景注册**：`asset_registry.json` -> `LOC_SERVANT_HUT`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
/ 杂役破屋
### 正向 prompt（英文）
/ 杂役破屋 production environment reference, 国漫写实 style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## / 山泉（`LOC_MOUNTAIN_SPRING`）
**目标存档**：`出图/共享/图片/定妆_场景_山泉.png`
**场景注册**：`asset_registry.json` -> `LOC_MOUNTAIN_SPRING`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
/ 山泉
### 正向 prompt（英文）
/ 山泉 production environment reference, 国漫写实 style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## / 食堂水缸（`LOC_KITCHEN_YARD`）
**目标存档**：`出图/共享/图片/定妆_场景_食堂水缸.png`
**场景注册**：`asset_registry.json` -> `LOC_KITCHEN_YARD`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
/ 食堂水缸
### 正向 prompt（英文）
/ 食堂水缸 production environment reference, 国漫写实 style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

## / 内门远景（`LOC_INNER_SECT_DISTANCE`）
**目标存档**：`出图/共享/图片/定妆_场景_内门远景.png`
**场景注册**：`asset_registry.json` -> `LOC_INNER_SECT_DISTANCE`；scene_atlas 需有正机位与反打机位。
### 正向 prompt（中文）
/ 内门远景
### 正向 prompt（英文）
/ 内门远景 production environment reference, 国漫写实 style, stable spatial landmarks, stable light direction, vertical 9:16, no modern objects, no watermark, no platform UI.
### 负向 prompt
风格禁忌：现代物品、平台UI、水印、空间轴线随机跳变、丢失连续性锚点；不得改变空间轴线、地标位置、主光方向和连续性锚点。
### 检查清单（定妆自查）
- 目标存档、正机位、反打机位、平面图全部对应同一空间。
- 光位锚、地标、轴线、常驻物件是否与 asset_registry.scene_dna 一致。
- 不把场景画成现代空间、宫殿化空间或与 storyboard 不符的新地点。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 和 scene_atlas 真实图片 sha。

