# 道具定妆

## 黑陶破盆（`PROP_BLACK_BASIN`）
**目标存档**：`出图/共享/图片/定妆_黑陶破盆.png`
**资产注册**：`asset_registry.json` -> `PROP_BLACK_BASIN`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
旧黑陶破盆，边缘缺口，粗糙陶土裂纹，廉价杂役旧物，不像宝鼎，不发神器光。
### 正向 prompt（英文）
黑陶破盆 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让黑陶破盆结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 极品金米（`PROP_GOLD_RICE`）
**目标存档**：`出图/共享/图片/定妆_道具_极品金米.png`
**资产注册**：`asset_registry.json` -> `PROP_GOLD_RICE`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
满盆金灿灿极品灵米，真实米粒颗粒，微弱暖金灵气，堆在旧黑陶盆里，不是金币不是宝石。
### 正向 prompt（英文）
极品金米 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让极品金米结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 灰败灵米残影（`PROP_GREY_RICE_MEMORY`）
**目标存档**：`出图/共享/图片/定妆_道具_灰败灵米.png`
**资产注册**：`asset_registry.json` -> `PROP_GREY_RICE_MEMORY`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
白里泛灰绿的劣质灵米，米粒粗糙干瘪，在月光下显得不值钱，能看出不是上等米。
### 正向 prompt（英文）
灰败灵米残影 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让灰败灵米残影结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 门栓铁锁（`PROP_DOOR_LOCK`）
**目标存档**：`出图/共享/图片/定妆_旧钥匙与生锈铁锁.png`
**资产注册**：`asset_registry.json` -> `PROP_DOOR_LOCK`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
旧钥匙和生锈铁锁，小件交接道具，铁色暗哑。
### 正向 prompt（英文）
门栓铁锁 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让门栓铁锁结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 破屋木门（`PROP_DOOR`）
**目标存档**：`出图/共享/图片/定妆_场景_贺平生杂役小屋.png`
**资产注册**：`asset_registry.json` -> `PROP_DOOR`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
低矮旧木小屋，硬板床、窗格冷光、墙角黑陶破盆、粗糙木门、几乎无家具，清晨冷灰青或深夜冷月光，贫瘠但画面可读。
### 正向 prompt（英文）
破屋木门 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让破屋木门结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 旧裤筒枕头（`PROP_TROUSER_PILLOW`）
**目标存档**：`出图/共享/图片/定妆_道具_旧裤筒枕头.png`
**资产注册**：`asset_registry.json` -> `PROP_TROUSER_PILLOW`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
破旧裤筒两头用粗绳扎紧，伪装成丑陋旧布枕头，粗布褶皱明显。
### 正向 prompt（英文）
旧裤筒枕头 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让旧裤筒枕头结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 山泉（`PROP_MOUNTAIN_SPRING`）
**目标存档**：`出图/共享/图片/定妆_后山山泉浅潭.png`
**资产注册**：`asset_registry.json` -> `PROP_MOUNTAIN_SPRING`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
后山崎岖小路尽头，一尺宽小瀑布、巨石、清澈浅潭、竹影和冷月，水底可见黑陶破盆。
### 正向 prompt（英文）
山泉 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让山泉结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 挑水木桶（`PROP_WATER_BUCKETS`）
**目标存档**：`出图/共享/图片/定妆_水桶与扁担.png`
**资产注册**：`asset_registry.json` -> `PROP_WATER_BUCKETS`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
杂役挑水用两只沉重大木桶，湿木纹，粗木扁担，水滴晃动，写实材质。
### 正向 prompt（英文）
挑水木桶 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让挑水木桶结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 食堂水缸（`PROP_WATER_JAR`）
**目标存档**：`出图/共享/图片/定妆_秀竹峰杂役院.png`
**资产注册**：`asset_registry.json` -> `PROP_WATER_JAR`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
杂役院后景两口巨大水缸，体量压迫，不新增现代水箱。
### 正向 prompt（英文）
食堂水缸 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让食堂水缸结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 内门冷灯笼（`PROP_INNER_SECT_LANTERN`）
**目标存档**：`出图/共享/图片/定妆_道具_PROP_INNER_SECT_LANTERN.png`
**资产注册**：`asset_registry.json` -> `PROP_INNER_SECT_LANTERN`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
夜色中远处仙门内门门楼和廊下的冷蓝灯笼/宫灯，青黑旧金属灯架，冷蓝白光，只作远景灯火锚和权力压迫信号；不是现代电灯，不是霓虹，不是巨大法器。
### 正向 prompt（英文）
内门冷灯笼 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让内门冷灯笼结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移、不要现代电灯、不要霓虹招牌、不要巨大发光法器、不要清晰人物脸
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

