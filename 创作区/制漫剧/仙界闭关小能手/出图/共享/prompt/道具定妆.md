# 道具定妆

## 黑陶破盆（`PROP_HEI_TAO_PEN`）
**目标存档**：`出图/共享/图片/定妆_黑陶破盆.png`
**资产注册**：`asset_registry.json` -> `PROP_HEI_TAO_PEN`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
旧黑陶盆，黑乎乎陶质，边缘磨损小缺口，像普通洗衣盆；异常状态只在盆底出现很小的微绿亮点，不是华丽法器，不发强光，不出现符号文字。
### 正向 prompt（英文）
黑陶破盆 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让黑陶破盆结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 碧绿灵水（`PROP_GREEN_WATER`）
**目标存档**：`出图/共享/图片/定妆_道具_碧绿灵水.png`
**资产注册**：`asset_registry.json` -> `PROP_GREEN_WATER`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
满盆碧绿清水，颜色异常但水面安静，清晨冷光反射，观众能感到不寻常；不要画成化学荧光或强烈能量效果。
### 正向 prompt（英文）
碧绿灵水 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让碧绿灵水结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 水桶与扁担（`PROP_SHUI_TONG`）
**目标存档**：`出图/共享/图片/定妆_水桶与扁担.png`
**资产注册**：`asset_registry.json` -> `PROP_SHUI_TONG`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
木水桶与粗木扁担，少年挑水工具，旧木色低饱和。
### 正向 prompt（英文）
水桶与扁担 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让水桶与扁担结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 杂役饭碗（`PROP_FOOD_BOWL`）
**目标存档**：`出图/共享/图片/定妆_道具_杂役饭碗.png`
**资产注册**：`asset_registry.json` -> `PROP_FOOD_BOWL`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
粗木饭碗或旧陶饭碗，盛着底层杂役的稀饭/粗饭，碗沿磨损，低饱和脏暖晨光，不华丽、不现代。
### 正向 prompt（英文）
杂役饭碗 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让杂役饭碗结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 两口巨大水缸（`PROP_WATER_JARS`）
**目标存档**：`出图/共享/图片/定妆_秀竹峰杂役院.png`
**资产注册**：`asset_registry.json` -> `PROP_WATER_JARS`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
杂役院后景两口巨大水缸，体量压迫，不新增现代水箱。
### 正向 prompt（英文）
两口巨大水缸 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让两口巨大水缸结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 灵米布袋（`PROP_SPIRIT_RICE_BAG`）
**目标存档**：`出图/共享/图片/定妆_道具_灵米布袋.png`
**资产注册**：`asset_registry.json` -> `PROP_SPIRIT_RICE_BAG`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
旧粗布粮袋，袋口磨损，能倒出小半袋灰白米粒，张老大拿在手里显得像施恩。
### 正向 prompt（英文）
灵米布袋 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让灵米布袋结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

## 灰败灵米（`PROP_GRAY_RICE`）
**目标存档**：`出图/共享/图片/定妆_道具_灰败灵米.png`
**资产注册**：`asset_registry.json` -> `PROP_GRAY_RICE`。
**关键道具结构唯一性规则**：形状、材质、尺寸、状态和剧情位置必须稳定；同名道具不得每镜重画成不同物。
### 正向 prompt（中文）
白里泛灰绿的劣质灵米，米粒粗糙干瘪，在月光下显得不值钱，能看出不是上等米。
### 正向 prompt（英文）
灰败灵米 production reference asset, restrained realistic Chinese comic-drama style, stable structure, no text, no logo, vertical 9:16 reference.
### 负向 prompt
风格禁忌：现代物件、文字水印、结构漂移；资产禁漂：不要让灰败灵米结构/颜色/尺寸漂移、不要现代物件、不要文字水印、不要结构漂移、不要数量漂移
### 检查清单（定妆自查）
- 目标存档路径和 asset_registry 一致。
- 结构、材质、状态、归属和剧情生命周期可读。
- 不出现文字、水印、现代替代物。
**自检（生成后逐张过）**：通过后回填 asset_registry 的 `self_check_passed=true` 与真实图 sha。

