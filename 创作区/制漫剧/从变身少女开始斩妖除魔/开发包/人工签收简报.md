# 人工签收简报（当前未签）

## A. P-1 开发包

- 待签内容：`series_bible.md`、`adaptation_strategy.json`、`season_arc.json`、`production_feasibility.json`、`pilot_greenlight.md`。
- 当前证据总哈希：见 `开发包/signoff.json.evidence_fingerprint.sha`。
- 需要两个职责组：creative（director / head_writer / showrunner 任一）与 production（producer）。
- 建议回复：`我批准当前 P-1 开发包；以导演/总编剧/主创身份签收，并以制片人身份签收。`
- 同一自然人可承担两个职责，但必须明确写出两个角色；agent 不代签。

## B. 首批拆集边界

- 推荐：第1–10集对应原著第1–18章；第19–20章进入下一窗口。
- 逐集映射、source unit、字符负荷、核心戏与集尾钩见 `脚本/_拆集复核.md`。
- 建议回复：`我批准《_拆集复核.md》的第1–10集边界，允许按该映射重建仍为 raw-only 的首10集。`
- 批准后才覆盖当前机器粗胚；执行时必须生成旧/新 raw SHA、source mapping 和 applied receipt。

## C. 权利确认

- P-1/边界批准不等于改编授权。
- 请另行按 `合规/rights_clearance_checklist.md` 明确自己是作者/权利人，或已获何种范围的改编授权。
- 未确认前，文本开发可继续到人工方案层；图片、视频、声音付费生成与发布保持 BLOCK。

## D. 制作模式首次确认

- 当前 `_设置.md` 中的“混合自动路由”来自对用户请求的推断（`source=inferred_from_request`），不能冒充用户显式首选。
- 建议回复：`我确认本项目制作模式采用“混合自动路由”。`
- 确认后用 `n2d-settings` 写成 `source=explicit_user`，同项目后续沉默沿用；这不解除权利、P-1、拆集边界或任何付费生成闸门。
