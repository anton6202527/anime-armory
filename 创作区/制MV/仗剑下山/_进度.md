# 进度 — 制MV《仗剑下山》

> 平台=抖音 画幅=9:16 段落=10。输入歌=待放入 歌/。

## 输入
- [x] 歌/song.wav（**20s ACE-Step 真歌**，来自 写歌/仗剑下山，BPM~144 立体声；已替换占位歌。正式整首待 Suno 后再换）
- [x] 词/lyrics.md（卡拉OK对齐用）

## 制MV 阶段
| 阶段 | skill | 状态 |
|---|---|---|
| 视觉蓝图 + 设定 | 本调度 + mv-image | [x] 蓝图/角色卡/场景卡 已写 |
| 卡点 beatgrid | mv-beat | [x] **真歌测到节拍**(cosyvoice env librosa)：BPM 143.55 / 40 beats / 10 downbeats；clips 已按 downbeat 切 |
| 出图 | mv-image | [~] 5/20 已出(定妆少年 + 镜头02/09/10/19)；其余待 dreamina 交互生成 |
| 视频任务包 | mv-video/scripts/video_jobs.py | [x] 20s demo 的 5 个既有 clip 已登记到 `出视频/jobs_manifest.json`，完成评分与 selected_take；正式版仍需真实 image2video 重制 |
| 出视频 | mv-video | [~] Clip02 真生成(裁到 downbeat)；Clip00定妆/09/10/19 = **本机 ffmpeg ken-burns 占位**(demo 替身，待正式 image2video 生成)。已补 `生产数据/video_inherit_contract` 与 `生产数据/video_qc`，demo 机检 ok。**去抖按镜头意图分治**：定妆亮相/回眸定格=改**静止**(零抖)；山巅/拔剑=**3×超采样**(源4320x7680)快推。zoompan 整数取整=抖动根因，超采样把取整压到亚像素 |
| 歌词字幕 | mv-lyric-sync(变体) | [x] **已烧词**：whisperx 未装→用 cosyvoice env 的 openai-whisper **转写20s实唱**(唱的是副歌)→按已知词校正错别字→6行 字幕/(lyrics.lrc+karaoke.ass)；已补 `字幕/alignment_report.json` 标记 demo_excerpt；本机 ffmpeg 无 libass/drawtext→Pillow 渲透明PNG+overlay 烧录 |
| 合成成片 | mv-compose | [x] **demo 成片已出**(5 clip 踩 downbeat 切 20.0s + 真歌轨 19.97s + 烧录歌词) |
| 正式版 readiness | mv-craft/scripts/formal_readiness.py | [x] 已生成 `生产数据/formal_readiness/`；当前 status=blocked（demo excerpt、歌长 19.97s、5 clips、正式 reference pack 0/9 ready），避免误当正式整首 MV |
| 传统制片包 | mv-craft/scripts/production_pack.py | [x] 已生成 `分镜/animatic_manifest.json`、`制片/shot_list.json`、`setup_schedule.md`、`take_log.csv`、`picture_lock_color_checklist.md` |
| （可选）换脸 | video-faceswap（公共） | [ ] |

> 卡点环境备忘：本机系统 Py3.14+PEP668 装不了 librosa/numba；改用 `conda run -n cosyvoice python` 即可跑 mv-beat / whisperx 类依赖。

## 导出
- [x] **成片_MV.mp4**（20s · 1080×1920 9:16 · h264+aac · **真歌轨+烧录歌词** · 5 clip 踩 downbeat 切：定妆亮相→立山门→山巅仗剑→拔剑→回眸）

> 字幕踩坑(已验证)：本机 homebrew ffmpeg 是精简版**无 libass 也无 drawtext/libfreetype**→.ass 烧不了。降级路径=Pillow(conda env)渲透明PNG + ffmpeg `overlay` 时间窗叠加，烧到已有歌轨的成片上(`-c:a copy`)。重跑命令见 字幕/_build_subs.py + 烧录用 字幕/_overlay.filter。

## demo 与正式版的差距（拿到正式歌后回跑）
1. 替换 `歌/song.wav` 为正式整首歌（~3min）→ 出 20 clip 填满
2. `conda run -n cosyvoice python .../mv-beat/scripts/beat_detect.py 制MV/仗剑下山` → beatgrid 真鼓点（占位歌为静态音无 beat，正式歌才有）
3. 剩余 15 张 dreamina 出图 + image2video 正式生成（替换 ken-burns 占位）
4. `mv-lyric-sync`（whisperx，同 cosyvoice env）→ karaoke.ass → compose 自动烧逐字字幕
5. 重跑 `mv_compose.sh`

## 维护记录

- 2026-06-09：共享出图目录约定已迁移为中文名 `出图/共享`；本 demo 的共享定妆目录已从旧英文目录改名，生产状态不变。
- 2026-07-08：按 MV 全流程审查结果补齐 `_设置.md`、`分镜/clip_plan.json`、`timeline_manifest.json`、`semantic_prompts.json`、身份/资产/参考注册表、`jobs_manifest.json`、image_qc demo 人审放行、video inherit contract、video_qc、alignment_report 与 AI 使用披露。当前仍定位 20s demo excerpt；正式 3min 版换歌后重跑全链路。
- 2026-07-08：补正式版 readiness、reference_requirements、传统制片包和帧级 video_qc（15 张 start/mid/end 样本 + 4 个接缝指标）。当前 demo 机检通过，但 formal_readiness 明确阻止直接当正式版发布。
- 2026-07-08：收紧 reference_requirements 判定：文字设定不再等同正式参考图 ready；当前图像参考覆盖为 0/9 ready、4 partial、5 text_only。新增 `生产数据/formal_readiness/formal_upgrade_plan.md`，把 demo 升级正式整首 MV 的 10 步返工顺序落档，并已纳入 mv-review 快照。
